/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.api

import java.io.IOException
import okhttp3.Call
import okhttp3.HttpUrl

import ai.hanzo.cloud.model.AddDomainReq
import ai.hanzo.cloud.model.AppView
import ai.hanzo.cloud.model.BuildBoard
import ai.hanzo.cloud.model.CreateAppReq
import ai.hanzo.cloud.model.DeployLogs
import ai.hanzo.cloud.model.DeployReq
import ai.hanzo.cloud.model.DeploymentView
import ai.hanzo.cloud.model.DomainView
import ai.hanzo.cloud.model.DriftBoard
import ai.hanzo.cloud.model.EnvironmentBoard
import ai.hanzo.cloud.model.PipelineBoard
import ai.hanzo.cloud.model.PreviewReq
import ai.hanzo.cloud.model.PreviewView
import ai.hanzo.cloud.model.ProjectView
import ai.hanzo.cloud.model.PromoteReq
import ai.hanzo.cloud.model.Push
import ai.hanzo.cloud.model.Readiness
import ai.hanzo.cloud.model.ReleaseBoard
import ai.hanzo.cloud.model.RestartRef
import ai.hanzo.cloud.model.Restarted
import ai.hanzo.cloud.model.RollbackReq
import ai.hanzo.cloud.model.RunReq
import ai.hanzo.cloud.model.RunView
import ai.hanzo.cloud.model.RunnerBuildReq
import ai.hanzo.cloud.model.RunnerBuildResp
import ai.hanzo.cloud.model.SetEnvReq
import ai.hanzo.cloud.model.Verdict

import com.google.gson.annotations.SerializedName

import ai.hanzo.cloud.infrastructure.ApiClient
import ai.hanzo.cloud.infrastructure.ApiResponse
import ai.hanzo.cloud.infrastructure.ClientException
import ai.hanzo.cloud.infrastructure.ClientError
import ai.hanzo.cloud.infrastructure.ServerException
import ai.hanzo.cloud.infrastructure.ServerError
import ai.hanzo.cloud.infrastructure.MultiValueMap
import ai.hanzo.cloud.infrastructure.PartConfig
import ai.hanzo.cloud.infrastructure.RequestConfig
import ai.hanzo.cloud.infrastructure.RequestMethod
import ai.hanzo.cloud.infrastructure.ResponseType
import ai.hanzo.cloud.infrastructure.Success
import ai.hanzo.cloud.infrastructure.toMultiValue

class PlatformApi(basePath: kotlin.String = defaultBasePath, client: Call.Factory = ApiClient.defaultClient) : ApiClient(basePath, client) {
    companion object {
        @JvmStatic
        val defaultBasePath: String by lazy {
            System.getProperties().getProperty(ApiClient.baseUrlKey, "https://api.hanzo.ai")
        }
    }

    /**
     * DELETE /v1/platform/projects/{project}/apps/{app}
     * Deletes an application and tears down what it runs.
     * Deletes an application and tears down what it runs.  It removes the application record and tears down what it owns in the org&#39;s tenant namespace — its operator Service CR and its KMSSecret — then answers 204. An app this org and project do not have is 404, never a silent success.  Teardown is best-effort by design: a cluster that refuses or is unreachable does not block the delete, so the record cannot be left orphaned behind a broken cluster; the failure is logged for operators and the orphan reaper reconciles it. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return void
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun deletePlatformProjectsByProjectAppsByApp(project: kotlin.String, app: kotlin.String) : Unit {
        val localVarResponse = deletePlatformProjectsByProjectAppsByAppWithHttpInfo(project = project, app = app)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> Unit
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * DELETE /v1/platform/projects/{project}/apps/{app}
     * Deletes an application and tears down what it runs.
     * Deletes an application and tears down what it runs.  It removes the application record and tears down what it owns in the org&#39;s tenant namespace — its operator Service CR and its KMSSecret — then answers 204. An app this org and project do not have is 404, never a silent success.  Teardown is best-effort by design: a cluster that refuses or is unreachable does not block the delete, so the record cannot be left orphaned behind a broken cluster; the failure is logged for operators and the orphan reaper reconciles it. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return ApiResponse<Unit?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Throws(IllegalStateException::class, IOException::class)
    fun deletePlatformProjectsByProjectAppsByAppWithHttpInfo(project: kotlin.String, app: kotlin.String) : ApiResponse<Unit?> {
        val localVariableConfig = deletePlatformProjectsByProjectAppsByAppRequestConfig(project = project, app = app)

        return request<Unit, Unit>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation deletePlatformProjectsByProjectAppsByApp
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return RequestConfig
     */
    fun deletePlatformProjectsByProjectAppsByAppRequestConfig(project: kotlin.String, app: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        
        return RequestConfig(
            method = RequestMethod.DELETE,
            path = "/v1/platform/projects/{project}/apps/{app}".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * DELETE /v1/platform/projects/{project}/apps/{app}/domains/{host}
     * Detaches a hostname and releases the claim.
     * Detaches a hostname and releases the claim.  It drops the host from the app&#39;s ingress and releases any custom claim on it, so the name becomes claimable again — by this org or any other. Answers 204.  The default host is permanent and cannot be removed: that is 400, not 404. A host that is neither attached nor claimed here is 404. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param host Host is the hostname, from the path.
     * @return void
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun deletePlatformProjectsByProjectAppsByAppDomainsByHost(project: kotlin.String, app: kotlin.String, host: kotlin.String) : Unit {
        val localVarResponse = deletePlatformProjectsByProjectAppsByAppDomainsByHostWithHttpInfo(project = project, app = app, host = host)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> Unit
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * DELETE /v1/platform/projects/{project}/apps/{app}/domains/{host}
     * Detaches a hostname and releases the claim.
     * Detaches a hostname and releases the claim.  It drops the host from the app&#39;s ingress and releases any custom claim on it, so the name becomes claimable again — by this org or any other. Answers 204.  The default host is permanent and cannot be removed: that is 400, not 404. A host that is neither attached nor claimed here is 404. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param host Host is the hostname, from the path.
     * @return ApiResponse<Unit?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Throws(IllegalStateException::class, IOException::class)
    fun deletePlatformProjectsByProjectAppsByAppDomainsByHostWithHttpInfo(project: kotlin.String, app: kotlin.String, host: kotlin.String) : ApiResponse<Unit?> {
        val localVariableConfig = deletePlatformProjectsByProjectAppsByAppDomainsByHostRequestConfig(project = project, app = app, host = host)

        return request<Unit, Unit>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation deletePlatformProjectsByProjectAppsByAppDomainsByHost
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param host Host is the hostname, from the path.
     * @return RequestConfig
     */
    fun deletePlatformProjectsByProjectAppsByAppDomainsByHostRequestConfig(project: kotlin.String, app: kotlin.String, host: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        
        return RequestConfig(
            method = RequestMethod.DELETE,
            path = "/v1/platform/projects/{project}/apps/{app}/domains/{host}".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())).replace("{"+"host"+"}", encodeURIComponent(host.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/apps
     * What this organization has declared, and what CD did with it
     * Returns the declarations in the caller&#39;s own org directory, each joined with the Hanzo CD Application reconciling it — sync verdict, health, the universe commit last applied. &#x60;cd&#x60; is null for a declaration the delivery plane has no Application for, which is the normal state of one that exists only on a branch.  If the delivery plane cannot be read, the declarations are still returned and &#x60;cdUnavailable&#x60; says why. An unreadable plane never renders as \&quot;nothing has been reconciled\&quot;.
     * @return void
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformApps() : Unit {
        val localVarResponse = getPlatformAppsWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> Unit
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/apps
     * What this organization has declared, and what CD did with it
     * Returns the declarations in the caller&#39;s own org directory, each joined with the Hanzo CD Application reconciling it — sync verdict, health, the universe commit last applied. &#x60;cd&#x60; is null for a declaration the delivery plane has no Application for, which is the normal state of one that exists only on a branch.  If the delivery plane cannot be read, the declarations are still returned and &#x60;cdUnavailable&#x60; says why. An unreadable plane never renders as \&quot;nothing has been reconciled\&quot;.
     * @return ApiResponse<Unit?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformAppsWithHttpInfo() : ApiResponse<Unit?> {
        val localVariableConfig = getPlatformAppsRequestConfig()

        return request<Unit, Unit>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformApps
     *
     * @return RequestConfig
     */
    fun getPlatformAppsRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        
        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/apps",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/apps/{app}
     * One declaration
     * The values file for one app as git declares it: image repository and tag, hosts, replicas, and whether CD is automated on it. 404 when this organization declares no such app.
     * @param app 
     * @return void
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformAppsByApp(app: kotlin.String) : Unit {
        val localVarResponse = getPlatformAppsByAppWithHttpInfo(app = app)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> Unit
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/apps/{app}
     * One declaration
     * The values file for one app as git declares it: image repository and tag, hosts, replicas, and whether CD is automated on it. 404 when this organization declares no such app.
     * @param app 
     * @return ApiResponse<Unit?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformAppsByAppWithHttpInfo(app: kotlin.String) : ApiResponse<Unit?> {
        val localVariableConfig = getPlatformAppsByAppRequestConfig(app = app)

        return request<Unit, Unit>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformAppsByApp
     *
     * @param app 
     * @return RequestConfig
     */
    fun getPlatformAppsByAppRequestConfig(app: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        
        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/apps/{app}".replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/apps/{app}/cd
     * One app&#39;s reconciliation
     * The Hanzo CD Application for one declaration, on its own — the poll a deploy view makes while it waits, without re-reading the whole inventory. 404 while the declaration exists only on a branch, because the generator reads main.
     * @param app 
     * @return void
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformAppsByAppCd(app: kotlin.String) : Unit {
        val localVarResponse = getPlatformAppsByAppCdWithHttpInfo(app = app)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> Unit
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/apps/{app}/cd
     * One app&#39;s reconciliation
     * The Hanzo CD Application for one declaration, on its own — the poll a deploy view makes while it waits, without re-reading the whole inventory. 404 while the declaration exists only on a branch, because the generator reads main.
     * @param app 
     * @return ApiResponse<Unit?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformAppsByAppCdWithHttpInfo(app: kotlin.String) : ApiResponse<Unit?> {
        val localVariableConfig = getPlatformAppsByAppCdRequestConfig(app = app)

        return request<Unit, Unit>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformAppsByAppCd
     *
     * @param app 
     * @return RequestConfig
     */
    fun getPlatformAppsByAppCdRequestConfig(app: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        
        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/apps/{app}/cd".replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/builds
     * Returns real build records for your org.
     * Returns real build records for your org.  It lists the org&#39;s BuildKit build records — the git build step behind a deploy — each with the repo it built, the short commit, its status, when it started and how long it took. These are real records or an honest empty list; a build appears here because one ran, never because a page needed a row. Builds are created only by /deploy and the push-to-deploy hook. Requires a validated principal; 403 without one.
     * @return BuildBoard
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformBuilds() : BuildBoard {
        val localVarResponse = getPlatformBuildsWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as BuildBoard
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/builds
     * Returns real build records for your org.
     * Returns real build records for your org.  It lists the org&#39;s BuildKit build records — the git build step behind a deploy — each with the repo it built, the short commit, its status, when it started and how long it took. These are real records or an honest empty list; a build appears here because one ran, never because a page needed a row. Builds are created only by /deploy and the push-to-deploy hook. Requires a validated principal; 403 without one.
     * @return ApiResponse<BuildBoard?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformBuildsWithHttpInfo() : ApiResponse<BuildBoard?> {
        val localVariableConfig = getPlatformBuildsRequestConfig()

        return request<Unit, BuildBoard>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformBuilds
     *
     * @return RequestConfig
     */
    fun getPlatformBuildsRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/builds",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/cd
     * The delivery plane
     * Every Hanzo CD Application this caller may observe, with its sync verdict, health, the universe revision last applied, and whether automation and self-heal are on. A SuperAdmin sees the fleet; an org admin sees only Applications whose destination namespace IS its own organization, and never a reserved one.  A cluster with no CD installed answers an empty plane. A plane that cannot be READ answers 503 and says why — the two are opposite facts and never share a shape.
     * @return void
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformCd() : Unit {
        val localVarResponse = getPlatformCdWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> Unit
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/cd
     * The delivery plane
     * Every Hanzo CD Application this caller may observe, with its sync verdict, health, the universe revision last applied, and whether automation and self-heal are on. A SuperAdmin sees the fleet; an org admin sees only Applications whose destination namespace IS its own organization, and never a reserved one.  A cluster with no CD installed answers an empty plane. A plane that cannot be READ answers 503 and says why — the two are opposite facts and never share a shape.
     * @return ApiResponse<Unit?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformCdWithHttpInfo() : ApiResponse<Unit?> {
        val localVariableConfig = getPlatformCdRequestConfig()

        return request<Unit, Unit>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformCd
     *
     * @return RequestConfig
     */
    fun getPlatformCdRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        
        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/cd",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/ci
     * Continuous integration (not wired)
     * Answers 501. The forge&#39;s Actions runs need a Forgejo API client and this deployment has none; an empty run list would be indistinguishable from a forge with no runs.
     * @return void
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformCi() : Unit {
        val localVarResponse = getPlatformCiWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> Unit
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/ci
     * Continuous integration (not wired)
     * Answers 501. The forge&#39;s Actions runs need a Forgejo API client and this deployment has none; an empty run list would be indistinguishable from a forge with no runs.
     * @return ApiResponse<Unit?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformCiWithHttpInfo() : ApiResponse<Unit?> {
        val localVariableConfig = getPlatformCiRequestConfig()

        return request<Unit, Unit>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformCi
     *
     * @return RequestConfig
     */
    fun getPlatformCiRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        
        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/ci",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/environments
     * Returns your deploy targets, and what is running on each.
     * Returns your deploy targets, and what is running on each.  It returns the org&#39;s environments — the distinct deploy targets its applications name, &#x60;production&#x60; for anything that names none — each aggregating the apps that target it, a rolled-up status and when it last changed.  An environment is DERIVED, not stored: there is nothing to create or delete here, and an environment exists exactly as long as an app points at it. Requires a validated principal; 403 without one.
     * @return EnvironmentBoard
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformEnvironments() : EnvironmentBoard {
        val localVarResponse = getPlatformEnvironmentsWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as EnvironmentBoard
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/environments
     * Returns your deploy targets, and what is running on each.
     * Returns your deploy targets, and what is running on each.  It returns the org&#39;s environments — the distinct deploy targets its applications name, &#x60;production&#x60; for anything that names none — each aggregating the apps that target it, a rolled-up status and when it last changed.  An environment is DERIVED, not stored: there is nothing to create or delete here, and an environment exists exactly as long as an app points at it. Requires a validated principal; 403 without one.
     * @return ApiResponse<EnvironmentBoard?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformEnvironmentsWithHttpInfo() : ApiResponse<EnvironmentBoard?> {
        val localVariableConfig = getPlatformEnvironmentsRequestConfig()

        return request<Unit, EnvironmentBoard>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformEnvironments
     *
     * @return RequestConfig
     */
    fun getPlatformEnvironmentsRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/environments",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/fleet
     * Returns the platform&#39;s own service tier, and where it has drifted.
     * Returns the platform&#39;s own service tier, and where it has drifted.  It returns the board for the services the PLATFORM itself runs — iam, kms, gateway and the rest — as &#x60;{apps, summary}&#x60;: per service its environment, health, phase, the image tag its CR DECLARES, the tag actually running, and the drift between them, plus a summary counting the board green, yellow and red.  This is not a customer surface. &#x60;/v1/platform/projects/:project/apps&#x60; is a tenant&#39;s apps; this is the tier those tenants run ON, which is why the two are named differently rather than sharing a prefix.  Admission is scoped at the SCAN, before any CR is read: a platform SuperAdmin observes the whole fleet, an org admin observes only their own org&#39;s namespaces, and an org that owns none gets an empty board — a non-super caller never even lists another org&#39;s services. Narrow further with &#x60;env&#x60;, &#x60;health&#x60;, &#x60;org&#x60;, or &#x60;drift&#x3D;1&#x60; for only what has drifted.  It degrades honestly rather than failing whole: a namespace that does not exist is skipped, and a running-state read the caller cannot make leaves the running tag empty — an unknown, never a guess — while the declared, health and phase columns still render.
     * @param env Env narrows to one lifecycle env: main, test or dev. (optional)
     * @param health Health narrows to one health colour: green, yellow or red. (optional)
     * @param org Org narrows to one image namespace. (optional)
     * @param drift Drift is &#x60;1&#x60; or &#x60;true&#x60; to show only rows that have actually drifted. It is a STRING and not a bool because those two spellings are exactly what the board has always accepted, and a bool would silently widen that to &#x60;?drift&#x60; alone and to &#x60;TRUE&#x60; — a behaviour change wearing a type change&#39;s clothes. (optional)
     * @return DriftBoard
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformFleet(env: kotlin.String? = null, health: kotlin.String? = null, org: kotlin.String? = null, drift: kotlin.String? = null) : DriftBoard {
        val localVarResponse = getPlatformFleetWithHttpInfo(env = env, health = health, org = org, drift = drift)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as DriftBoard
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/fleet
     * Returns the platform&#39;s own service tier, and where it has drifted.
     * Returns the platform&#39;s own service tier, and where it has drifted.  It returns the board for the services the PLATFORM itself runs — iam, kms, gateway and the rest — as &#x60;{apps, summary}&#x60;: per service its environment, health, phase, the image tag its CR DECLARES, the tag actually running, and the drift between them, plus a summary counting the board green, yellow and red.  This is not a customer surface. &#x60;/v1/platform/projects/:project/apps&#x60; is a tenant&#39;s apps; this is the tier those tenants run ON, which is why the two are named differently rather than sharing a prefix.  Admission is scoped at the SCAN, before any CR is read: a platform SuperAdmin observes the whole fleet, an org admin observes only their own org&#39;s namespaces, and an org that owns none gets an empty board — a non-super caller never even lists another org&#39;s services. Narrow further with &#x60;env&#x60;, &#x60;health&#x60;, &#x60;org&#x60;, or &#x60;drift&#x3D;1&#x60; for only what has drifted.  It degrades honestly rather than failing whole: a namespace that does not exist is skipped, and a running-state read the caller cannot make leaves the running tag empty — an unknown, never a guess — while the declared, health and phase columns still render.
     * @param env Env narrows to one lifecycle env: main, test or dev. (optional)
     * @param health Health narrows to one health colour: green, yellow or red. (optional)
     * @param org Org narrows to one image namespace. (optional)
     * @param drift Drift is &#x60;1&#x60; or &#x60;true&#x60; to show only rows that have actually drifted. It is a STRING and not a bool because those two spellings are exactly what the board has always accepted, and a bool would silently widen that to &#x60;?drift&#x60; alone and to &#x60;TRUE&#x60; — a behaviour change wearing a type change&#39;s clothes. (optional)
     * @return ApiResponse<DriftBoard?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformFleetWithHttpInfo(env: kotlin.String?, health: kotlin.String?, org: kotlin.String?, drift: kotlin.String?) : ApiResponse<DriftBoard?> {
        val localVariableConfig = getPlatformFleetRequestConfig(env = env, health = health, org = org, drift = drift)

        return request<Unit, DriftBoard>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformFleet
     *
     * @param env Env narrows to one lifecycle env: main, test or dev. (optional)
     * @param health Health narrows to one health colour: green, yellow or red. (optional)
     * @param org Org narrows to one image namespace. (optional)
     * @param drift Drift is &#x60;1&#x60; or &#x60;true&#x60; to show only rows that have actually drifted. It is a STRING and not a bool because those two spellings are exactly what the board has always accepted, and a bool would silently widen that to &#x60;?drift&#x60; alone and to &#x60;TRUE&#x60; — a behaviour change wearing a type change&#39;s clothes. (optional)
     * @return RequestConfig
     */
    fun getPlatformFleetRequestConfig(env: kotlin.String?, health: kotlin.String?, org: kotlin.String?, drift: kotlin.String?) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf<kotlin.String, kotlin.collections.List<kotlin.String>>()
            .apply {
                if (env != null) {
                    put("env", listOf(env.toString()))
                }
                if (health != null) {
                    put("health", listOf(health.toString()))
                }
                if (org != null) {
                    put("org", listOf(org.toString()))
                }
                if (drift != null) {
                    put("drift", listOf(drift.toString()))
                }
            }
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/fleet",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/fleet/{app}
     * Returns one platform service, resolved to production by default.
     * Returns one platform service, resolved to production by default.  It returns a single platform service by its CR name, with the same declared-versus-running and drift facts the board carries. The name must be a DNS-1123 label; anything else is 400.  Namespaces are scanned in lifecycle order — main, then test, then dev — and the first match wins, so a bare name resolves to PRODUCTION. The scan covers only the namespaces the caller is authorized for, so an org admin can never read a service outside their own org, and a name found in none of them is 404 rather than a leak.
     * @param app App is the service&#39;s CR name, from the path. It must be a DNS-1123 label.
     * @param env Env narrows the scan to one lifecycle env: main, test or dev. Omitted, the namespaces are scanned in lifecycle order and the first match wins, so a bare name resolves to PRODUCTION. (optional)
     * @return AppView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformFleetByApp(app: kotlin.String, env: kotlin.String? = null) : AppView {
        val localVarResponse = getPlatformFleetByAppWithHttpInfo(app = app, env = env)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as AppView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/fleet/{app}
     * Returns one platform service, resolved to production by default.
     * Returns one platform service, resolved to production by default.  It returns a single platform service by its CR name, with the same declared-versus-running and drift facts the board carries. The name must be a DNS-1123 label; anything else is 400.  Namespaces are scanned in lifecycle order — main, then test, then dev — and the first match wins, so a bare name resolves to PRODUCTION. The scan covers only the namespaces the caller is authorized for, so an org admin can never read a service outside their own org, and a name found in none of them is 404 rather than a leak.
     * @param app App is the service&#39;s CR name, from the path. It must be a DNS-1123 label.
     * @param env Env narrows the scan to one lifecycle env: main, test or dev. Omitted, the namespaces are scanned in lifecycle order and the first match wins, so a bare name resolves to PRODUCTION. (optional)
     * @return ApiResponse<AppView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformFleetByAppWithHttpInfo(app: kotlin.String, env: kotlin.String?) : ApiResponse<AppView?> {
        val localVariableConfig = getPlatformFleetByAppRequestConfig(app = app, env = env)

        return request<Unit, AppView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformFleetByApp
     *
     * @param app App is the service&#39;s CR name, from the path. It must be a DNS-1123 label.
     * @param env Env narrows the scan to one lifecycle env: main, test or dev. Omitted, the namespaces are scanned in lifecycle order and the first match wins, so a bare name resolves to PRODUCTION. (optional)
     * @return RequestConfig
     */
    fun getPlatformFleetByAppRequestConfig(app: kotlin.String, env: kotlin.String?) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf<kotlin.String, kotlin.collections.List<kotlin.String>>()
            .apply {
                if (env != null) {
                    put("env", listOf(env.toString()))
                }
            }
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/fleet/{app}".replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/health
     * Reports whether this control plane can actually deploy anything.
     * Reports whether this control plane can actually deploy anything.  A real probe, not a status page. It answers 200 only when the metadata store is open AND the cluster is genuinely reachable — proved by LISTING the operator App CRD, which settles reachability and CRD presence in one bounded call, and which is the exact question every deploy depends on. Anything else is 503 carrying the real reason and whether the CRD was found.  A constructed cluster client proves nothing — it is built from a kubeconfig, not from a reachable apiserver — so this deliberately spends a round trip rather than reporting &#x60;ok&#x60; while every deploy fails. Not admin-gated: liveness has to be probe-able without a credential.
     * @return Readiness
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformHealth() : Readiness {
        val localVarResponse = getPlatformHealthWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as Readiness
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/health
     * Reports whether this control plane can actually deploy anything.
     * Reports whether this control plane can actually deploy anything.  A real probe, not a status page. It answers 200 only when the metadata store is open AND the cluster is genuinely reachable — proved by LISTING the operator App CRD, which settles reachability and CRD presence in one bounded call, and which is the exact question every deploy depends on. Anything else is 503 carrying the real reason and whether the CRD was found.  A constructed cluster client proves nothing — it is built from a kubeconfig, not from a reachable apiserver — so this deliberately spends a round trip rather than reporting &#x60;ok&#x60; while every deploy fails. Not admin-gated: liveness has to be probe-able without a credential.
     * @return ApiResponse<Readiness?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformHealthWithHttpInfo() : ApiResponse<Readiness?> {
        val localVariableConfig = getPlatformHealthRequestConfig()

        return request<Unit, Readiness>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformHealth
     *
     * @return RequestConfig
     */
    fun getPlatformHealthRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/health",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/pipelines
     * Returns one build-and-deploy pipeline per app, with its latest run.
     * Returns one build-and-deploy pipeline per app, with its latest run.  It returns one pipeline per application in the caller&#39;s org — its repo or image source, its current status, and when its most recent deployment ran and how long it took. A pipeline is a PROJECTION of an app plus its newest deployment, not a separate record: it comes into existence with the app and is triggered only through /deploy, never here. Requires a validated principal; 403 without one.
     * @return PipelineBoard
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformPipelines() : PipelineBoard {
        val localVarResponse = getPlatformPipelinesWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as PipelineBoard
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/pipelines
     * Returns one build-and-deploy pipeline per app, with its latest run.
     * Returns one build-and-deploy pipeline per app, with its latest run.  It returns one pipeline per application in the caller&#39;s org — its repo or image source, its current status, and when its most recent deployment ran and how long it took. A pipeline is a PROJECTION of an app plus its newest deployment, not a separate record: it comes into existence with the app and is triggered only through /deploy, never here. Requires a validated principal; 403 without one.
     * @return ApiResponse<PipelineBoard?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformPipelinesWithHttpInfo() : ApiResponse<PipelineBoard?> {
        val localVariableConfig = getPlatformPipelinesRequestConfig()

        return request<Unit, PipelineBoard>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformPipelines
     *
     * @return RequestConfig
     */
    fun getPlatformPipelinesRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/pipelines",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/projects
     * Returns your org&#39;s projects, each with how many apps live under it.
     * Returns your org&#39;s projects, each with how many apps live under it.  It lists the caller org&#39;s projects with the number of platform applications in each. A project is IAM&#39;s resource — it is created and deleted at /v1/iam/projects, never here — so this is the ONE projection IAM cannot serve: the project plus what the platform has put under it.  Requires a validated principal; 403 without one, and the org comes from that validated identity rather than a request header. This is the console&#39;s first authenticated read, so a project store that is not yet initialised degrades to an EMPTY list rather than a 500 — a new org genuinely has zero projects — and the real cause is surfaced to operators instead of to the caller.
     * @return kotlin.collections.List<ProjectView>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformProjects() : kotlin.collections.List<ProjectView> {
        val localVarResponse = getPlatformProjectsWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as kotlin.collections.List<ProjectView>
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/projects
     * Returns your org&#39;s projects, each with how many apps live under it.
     * Returns your org&#39;s projects, each with how many apps live under it.  It lists the caller org&#39;s projects with the number of platform applications in each. A project is IAM&#39;s resource — it is created and deleted at /v1/iam/projects, never here — so this is the ONE projection IAM cannot serve: the project plus what the platform has put under it.  Requires a validated principal; 403 without one, and the org comes from that validated identity rather than a request header. This is the console&#39;s first authenticated read, so a project store that is not yet initialised degrades to an EMPTY list rather than a 500 — a new org genuinely has zero projects — and the real cause is surfaced to operators instead of to the caller.
     * @return ApiResponse<kotlin.collections.List<ProjectView>?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformProjectsWithHttpInfo() : ApiResponse<kotlin.collections.List<ProjectView>?> {
        val localVariableConfig = getPlatformProjectsRequestConfig()

        return request<Unit, kotlin.collections.List<ProjectView>>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformProjects
     *
     * @return RequestConfig
     */
    fun getPlatformProjectsRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/projects",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/projects/{project}
     * Returns one project and its app count.
     * Returns one project and its app count.  It returns a single project of the caller&#39;s org with the number of platform applications under it. A project this org does not have is 404, which is also what another tenant&#39;s project looks like from here. Requires a validated principal; 403 without one.
     * @param project Project is the project&#39;s name, from the path.
     * @return ProjectView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformProjectsByProject(project: kotlin.String) : ProjectView {
        val localVarResponse = getPlatformProjectsByProjectWithHttpInfo(project = project)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as ProjectView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/projects/{project}
     * Returns one project and its app count.
     * Returns one project and its app count.  It returns a single project of the caller&#39;s org with the number of platform applications under it. A project this org does not have is 404, which is also what another tenant&#39;s project looks like from here. Requires a validated principal; 403 without one.
     * @param project Project is the project&#39;s name, from the path.
     * @return ApiResponse<ProjectView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformProjectsByProjectWithHttpInfo(project: kotlin.String) : ApiResponse<ProjectView?> {
        val localVariableConfig = getPlatformProjectsByProjectRequestConfig(project = project)

        return request<Unit, ProjectView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformProjectsByProject
     *
     * @param project Project is the project&#39;s name, from the path.
     * @return RequestConfig
     */
    fun getPlatformProjectsByProjectRequestConfig(project: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/projects/{project}".replace("{"+"project"+"}", encodeURIComponent(project.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/projects/{project}/apps
     * Returns the applications in one project, with what the cluster says about them.
     * Returns the applications in one project, with what the cluster says about them.  It lists the caller org&#39;s applications under one project. Each row carries the stored record and, for an app that is live or deploying, the LIVE phase and health read from its operator Service CR; an app with sealed env also carries its secret-sync state. Those cluster reads are best-effort — an unreachable cluster leaves those fields empty and never blocks the listing.  The project must exist in IAM for this org, or the answer is 404; the &#x60;default&#x60; project is implicit and always accepted, because it is part of what an org IS. Requires a validated principal; 403 without one.
     * @param project Project is the project&#39;s name, from the path.
     * @return kotlin.collections.List<AppView>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformProjectsByProjectApps(project: kotlin.String) : kotlin.collections.List<AppView> {
        val localVarResponse = getPlatformProjectsByProjectAppsWithHttpInfo(project = project)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as kotlin.collections.List<AppView>
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/projects/{project}/apps
     * Returns the applications in one project, with what the cluster says about them.
     * Returns the applications in one project, with what the cluster says about them.  It lists the caller org&#39;s applications under one project. Each row carries the stored record and, for an app that is live or deploying, the LIVE phase and health read from its operator Service CR; an app with sealed env also carries its secret-sync state. Those cluster reads are best-effort — an unreachable cluster leaves those fields empty and never blocks the listing.  The project must exist in IAM for this org, or the answer is 404; the &#x60;default&#x60; project is implicit and always accepted, because it is part of what an org IS. Requires a validated principal; 403 without one.
     * @param project Project is the project&#39;s name, from the path.
     * @return ApiResponse<kotlin.collections.List<AppView>?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformProjectsByProjectAppsWithHttpInfo(project: kotlin.String) : ApiResponse<kotlin.collections.List<AppView>?> {
        val localVariableConfig = getPlatformProjectsByProjectAppsRequestConfig(project = project)

        return request<Unit, kotlin.collections.List<AppView>>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformProjectsByProjectApps
     *
     * @param project Project is the project&#39;s name, from the path.
     * @return RequestConfig
     */
    fun getPlatformProjectsByProjectAppsRequestConfig(project: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/projects/{project}/apps".replace("{"+"project"+"}", encodeURIComponent(project.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}
     * Returns one application, with its live phase, health and secret sync.
     * Returns one application, with its live phase, health and secret sync.  It returns a single application of the caller&#39;s org together with what the cluster currently reports for it: the operator Service CR&#39;s phase and health, and whether its sealed env has synced. An app this org and project do not have is 404. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return AppView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformProjectsByProjectAppsByApp(project: kotlin.String, app: kotlin.String) : AppView {
        val localVarResponse = getPlatformProjectsByProjectAppsByAppWithHttpInfo(project = project, app = app)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as AppView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}
     * Returns one application, with its live phase, health and secret sync.
     * Returns one application, with its live phase, health and secret sync.  It returns a single application of the caller&#39;s org together with what the cluster currently reports for it: the operator Service CR&#39;s phase and health, and whether its sealed env has synced. An app this org and project do not have is 404. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return ApiResponse<AppView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformProjectsByProjectAppsByAppWithHttpInfo(project: kotlin.String, app: kotlin.String) : ApiResponse<AppView?> {
        val localVariableConfig = getPlatformProjectsByProjectAppsByAppRequestConfig(project = project, app = app)

        return request<Unit, AppView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformProjectsByProjectAppsByApp
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return RequestConfig
     */
    fun getPlatformProjectsByProjectAppsByAppRequestConfig(project: kotlin.String, app: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/projects/{project}/apps/{app}".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}/deployments
     * Returns an app&#39;s deployment history.
     * Returns an app&#39;s deployment history.  It lists every deployment recorded for one of the caller org&#39;s applications, newest version first, each with its version, status, source, commit and image. Failed and superseded attempts are included — that is the point of a history. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return kotlin.collections.List<DeploymentView>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformProjectsByProjectAppsByAppDeployments(project: kotlin.String, app: kotlin.String) : kotlin.collections.List<DeploymentView> {
        val localVarResponse = getPlatformProjectsByProjectAppsByAppDeploymentsWithHttpInfo(project = project, app = app)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as kotlin.collections.List<DeploymentView>
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}/deployments
     * Returns an app&#39;s deployment history.
     * Returns an app&#39;s deployment history.  It lists every deployment recorded for one of the caller org&#39;s applications, newest version first, each with its version, status, source, commit and image. Failed and superseded attempts are included — that is the point of a history. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return ApiResponse<kotlin.collections.List<DeploymentView>?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformProjectsByProjectAppsByAppDeploymentsWithHttpInfo(project: kotlin.String, app: kotlin.String) : ApiResponse<kotlin.collections.List<DeploymentView>?> {
        val localVariableConfig = getPlatformProjectsByProjectAppsByAppDeploymentsRequestConfig(project = project, app = app)

        return request<Unit, kotlin.collections.List<DeploymentView>>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformProjectsByProjectAppsByAppDeployments
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return RequestConfig
     */
    fun getPlatformProjectsByProjectAppsByAppDeploymentsRequestConfig(project: kotlin.String, app: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/projects/{project}/apps/{app}/deployments".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}/deployments/{id}
     * Returns one deployment of one app.
     * Returns one deployment of one app.  It returns a single deployment by id, scoped to the named application of the caller&#39;s org — so an id belonging to another app or another tenant is 404, not a read. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param id ID is the deployment&#39;s id, from the path.
     * @return DeploymentView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformProjectsByProjectAppsByAppDeploymentsById(project: kotlin.String, app: kotlin.String, id: kotlin.String) : DeploymentView {
        val localVarResponse = getPlatformProjectsByProjectAppsByAppDeploymentsByIdWithHttpInfo(project = project, app = app, id = id)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as DeploymentView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}/deployments/{id}
     * Returns one deployment of one app.
     * Returns one deployment of one app.  It returns a single deployment by id, scoped to the named application of the caller&#39;s org — so an id belonging to another app or another tenant is 404, not a read. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param id ID is the deployment&#39;s id, from the path.
     * @return ApiResponse<DeploymentView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformProjectsByProjectAppsByAppDeploymentsByIdWithHttpInfo(project: kotlin.String, app: kotlin.String, id: kotlin.String) : ApiResponse<DeploymentView?> {
        val localVariableConfig = getPlatformProjectsByProjectAppsByAppDeploymentsByIdRequestConfig(project = project, app = app, id = id)

        return request<Unit, DeploymentView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformProjectsByProjectAppsByAppDeploymentsById
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param id ID is the deployment&#39;s id, from the path.
     * @return RequestConfig
     */
    fun getPlatformProjectsByProjectAppsByAppDeploymentsByIdRequestConfig(project: kotlin.String, app: kotlin.String, id: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/projects/{project}/apps/{app}/deployments/{id}".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())).replace("{"+"id"+"}", encodeURIComponent(id.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}/deployments/{id}/logs
     * Returns real logs for a deployment — the build&#39;s, then the app&#39;s.
     * Returns real logs for a deployment — the build&#39;s, then the app&#39;s.  It returns the deployment&#39;s recorded status timeline together with LIVE pod logs pulled from the cluster: the build pod&#39;s output while a git build is running, and the running app&#39;s output once it is deployed. The &#x60;source&#x60; field says which of the two the body is — &#x60;build&#x60;, &#x60;app&#x60; or &#x60;none&#x60; — so a console can label the pane honestly.  It never fabricates log content. When no pod exists yet, or the cluster is unreachable, it degrades to the recorded timeline and says so. Every cluster read is confined to the caller org&#39;s own namespaces and time-boxed. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param id ID is the deployment&#39;s id, from the path.
     * @return DeployLogs
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogs(project: kotlin.String, app: kotlin.String, id: kotlin.String) : DeployLogs {
        val localVarResponse = getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsWithHttpInfo(project = project, app = app, id = id)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as DeployLogs
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}/deployments/{id}/logs
     * Returns real logs for a deployment — the build&#39;s, then the app&#39;s.
     * Returns real logs for a deployment — the build&#39;s, then the app&#39;s.  It returns the deployment&#39;s recorded status timeline together with LIVE pod logs pulled from the cluster: the build pod&#39;s output while a git build is running, and the running app&#39;s output once it is deployed. The &#x60;source&#x60; field says which of the two the body is — &#x60;build&#x60;, &#x60;app&#x60; or &#x60;none&#x60; — so a console can label the pane honestly.  It never fabricates log content. When no pod exists yet, or the cluster is unreachable, it degrades to the recorded timeline and says so. Every cluster read is confined to the caller org&#39;s own namespaces and time-boxed. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param id ID is the deployment&#39;s id, from the path.
     * @return ApiResponse<DeployLogs?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsWithHttpInfo(project: kotlin.String, app: kotlin.String, id: kotlin.String) : ApiResponse<DeployLogs?> {
        val localVariableConfig = getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsRequestConfig(project = project, app = app, id = id)

        return request<Unit, DeployLogs>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogs
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param id ID is the deployment&#39;s id, from the path.
     * @return RequestConfig
     */
    fun getPlatformProjectsByProjectAppsByAppDeploymentsByIdLogsRequestConfig(project: kotlin.String, app: kotlin.String, id: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/projects/{project}/apps/{app}/deployments/{id}/logs".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())).replace("{"+"id"+"}", encodeURIComponent(id.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}/domains
     * Returns every hostname this app answers on.
     * Returns every hostname this app answers on.  It lists the app&#39;s hosts: the permanent default host it was born with, any org-subtree hosts attached to it, and every custom host claimed for it with its verification state and, while pending, the DNS challenge records to publish. Live endpoint status for each host is observed from the cluster. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return kotlin.collections.List<DomainView>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformProjectsByProjectAppsByAppDomains(project: kotlin.String, app: kotlin.String) : kotlin.collections.List<DomainView> {
        val localVarResponse = getPlatformProjectsByProjectAppsByAppDomainsWithHttpInfo(project = project, app = app)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as kotlin.collections.List<DomainView>
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/projects/{project}/apps/{app}/domains
     * Returns every hostname this app answers on.
     * Returns every hostname this app answers on.  It lists the app&#39;s hosts: the permanent default host it was born with, any org-subtree hosts attached to it, and every custom host claimed for it with its verification state and, while pending, the DNS challenge records to publish. Live endpoint status for each host is observed from the cluster. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return ApiResponse<kotlin.collections.List<DomainView>?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformProjectsByProjectAppsByAppDomainsWithHttpInfo(project: kotlin.String, app: kotlin.String) : ApiResponse<kotlin.collections.List<DomainView>?> {
        val localVariableConfig = getPlatformProjectsByProjectAppsByAppDomainsRequestConfig(project = project, app = app)

        return request<Unit, kotlin.collections.List<DomainView>>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformProjectsByProjectAppsByAppDomains
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return RequestConfig
     */
    fun getPlatformProjectsByProjectAppsByAppDomainsRequestConfig(project: kotlin.String, app: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/projects/{project}/apps/{app}/domains".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * GET /v1/platform/releases
     * Returns the versions that actually reached the cluster.
     * Returns the versions that actually reached the cluster.  It lists the org&#39;s releases: the deployments that were genuinely applied to the cluster, with the app they belong to, their version, environment, status and when they were released. A deployment that failed or is still building is NOT a release and is excluded — reaching the cluster is what makes one. Requires a validated principal; 403 without one.
     * @return ReleaseBoard
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun getPlatformReleases() : ReleaseBoard {
        val localVarResponse = getPlatformReleasesWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as ReleaseBoard
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * GET /v1/platform/releases
     * Returns the versions that actually reached the cluster.
     * Returns the versions that actually reached the cluster.  It lists the org&#39;s releases: the deployments that were genuinely applied to the cluster, with the app they belong to, their version, environment, status and when they were released. A deployment that failed or is still building is NOT a release and is excluded — reaching the cluster is what makes one. Requires a validated principal; 403 without one.
     * @return ApiResponse<ReleaseBoard?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun getPlatformReleasesWithHttpInfo() : ApiResponse<ReleaseBoard?> {
        val localVariableConfig = getPlatformReleasesRequestConfig()

        return request<Unit, ReleaseBoard>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation getPlatformReleases
     *
     * @return RequestConfig
     */
    fun getPlatformReleasesRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.GET,
            path = "/v1/platform/releases",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/apps
     * Deploy an app through cd.hanzo.ai
     * Builds a git repository into an image and writes the declaration that names it — a values file in &#x60;hanzoai/universe&#x60; under &#x60;charts/app/values/&lt;namespace&gt;/&lt;name&gt;.yaml&#x60;, which the &#x60;fleet&#x60; ApplicationSet renders as one Application. That file IS the deployment: nothing else has to be applied.  &#x60;mode&#x60; decides whether anything can go live. The default, &#x60;branch&#x60;, pushes to &#x60;deploy/&lt;namespace&gt;/&lt;name&gt;/&lt;tag&gt;&#x60; and returns a review URL; the generator reads main, so a branch declaration deploys NOTHING and merging the review is the deliberate act. &#x60;commit&#x60; writes main, and proves the image is pullable first — a declaration naming an image the registry cannot serve is an ImagePullBackOff with no rollback path.  Omit &#x60;tag&#x60; to build; give it to declare an image an earlier call already built, which is how a green build is released without rebuilding it.  An org is its name: the values DIRECTORY, the destination NAMESPACE and the AppProject FENCE are all &#x60;&lt;org&gt;&#x60;, and the image is &#x60;&lt;registry&gt;/&lt;org&gt;/&lt;app&gt;&#x60;. None of them is a request field — the directory decides what CD admits the sync under and the repository decides what the cluster pulls, so a caller who could name either could reach outside its own org.  &#x60;org&#x60; is an ACT-AS, not a placement field: it defaults to the caller&#39;s own, and naming another requires SuperAdmin. So does naming a RESERVED org — the platform&#39;s own namespace family (the brands and their environments, the control and delivery planes, &#x60;admin&#x60;) — even when it is the caller&#39;s own, because an IAM org named &#x60;kube-system&#x60; does not own Kubernetes. Both refuse rather than downgrade, so an escape attempt is never indistinguishable from a normal request.  A host outside the caller&#39;s org subtree is refused: claim and verify a custom domain first.
     * @return void
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformApps() : Unit {
        val localVarResponse = postPlatformAppsWithHttpInfo()

        return when (localVarResponse.responseType) {
            ResponseType.Success -> Unit
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/apps
     * Deploy an app through cd.hanzo.ai
     * Builds a git repository into an image and writes the declaration that names it — a values file in &#x60;hanzoai/universe&#x60; under &#x60;charts/app/values/&lt;namespace&gt;/&lt;name&gt;.yaml&#x60;, which the &#x60;fleet&#x60; ApplicationSet renders as one Application. That file IS the deployment: nothing else has to be applied.  &#x60;mode&#x60; decides whether anything can go live. The default, &#x60;branch&#x60;, pushes to &#x60;deploy/&lt;namespace&gt;/&lt;name&gt;/&lt;tag&gt;&#x60; and returns a review URL; the generator reads main, so a branch declaration deploys NOTHING and merging the review is the deliberate act. &#x60;commit&#x60; writes main, and proves the image is pullable first — a declaration naming an image the registry cannot serve is an ImagePullBackOff with no rollback path.  Omit &#x60;tag&#x60; to build; give it to declare an image an earlier call already built, which is how a green build is released without rebuilding it.  An org is its name: the values DIRECTORY, the destination NAMESPACE and the AppProject FENCE are all &#x60;&lt;org&gt;&#x60;, and the image is &#x60;&lt;registry&gt;/&lt;org&gt;/&lt;app&gt;&#x60;. None of them is a request field — the directory decides what CD admits the sync under and the repository decides what the cluster pulls, so a caller who could name either could reach outside its own org.  &#x60;org&#x60; is an ACT-AS, not a placement field: it defaults to the caller&#39;s own, and naming another requires SuperAdmin. So does naming a RESERVED org — the platform&#39;s own namespace family (the brands and their environments, the control and delivery planes, &#x60;admin&#x60;) — even when it is the caller&#39;s own, because an IAM org named &#x60;kube-system&#x60; does not own Kubernetes. Both refuse rather than downgrade, so an escape attempt is never indistinguishable from a normal request.  A host outside the caller&#39;s org subtree is refused: claim and verify a custom domain first.
     * @return ApiResponse<Unit?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformAppsWithHttpInfo() : ApiResponse<Unit?> {
        val localVariableConfig = postPlatformAppsRequestConfig()

        return request<Unit, Unit>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformApps
     *
     * @return RequestConfig
     */
    fun postPlatformAppsRequestConfig() : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        
        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/apps",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/fleet/{app}/deploy
     * Rolls a platform service&#39;s pods, in a named environment.
     * Rolls a platform service&#39;s pods, in a named environment.  It triggers a rolling restart of one platform service&#39;s Deployment by stamping a fresh restart annotation, and answers 202 with the app, the namespace, the environment and the timestamp. It restarts pods; it does NOT change the image — a version change is the release path, not this.  SuperAdmin ONLY, and deliberately narrower than the read gate beside it. The only namespaces this board touches are the platform&#39;s own tier, so a restart here recycles a SHARED service every tenant depends on. A brand-org admin is a customer-org admin, not a platform operator: observing the board is bounded and audited, and restarting production identity is not.  &#x60;?env&#x3D;main|test|dev&#x60; is REQUIRED — a bare call does not default to production, which is what closes the fat-finger and confused-deputy hazard — and any other value is 400. A service with no Deployment to restart in that environment is 404.
     * @param app App is the service&#39;s CR name, from the path. It must be a DNS-1123 label.
     * @param restartRef 
     * @return Restarted
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformFleetByAppDeploy(app: kotlin.String, restartRef: RestartRef) : Restarted {
        val localVarResponse = postPlatformFleetByAppDeployWithHttpInfo(app = app, restartRef = restartRef)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as Restarted
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/fleet/{app}/deploy
     * Rolls a platform service&#39;s pods, in a named environment.
     * Rolls a platform service&#39;s pods, in a named environment.  It triggers a rolling restart of one platform service&#39;s Deployment by stamping a fresh restart annotation, and answers 202 with the app, the namespace, the environment and the timestamp. It restarts pods; it does NOT change the image — a version change is the release path, not this.  SuperAdmin ONLY, and deliberately narrower than the read gate beside it. The only namespaces this board touches are the platform&#39;s own tier, so a restart here recycles a SHARED service every tenant depends on. A brand-org admin is a customer-org admin, not a platform operator: observing the board is bounded and audited, and restarting production identity is not.  &#x60;?env&#x3D;main|test|dev&#x60; is REQUIRED — a bare call does not default to production, which is what closes the fat-finger and confused-deputy hazard — and any other value is 400. A service with no Deployment to restart in that environment is 404.
     * @param app App is the service&#39;s CR name, from the path. It must be a DNS-1123 label.
     * @param restartRef 
     * @return ApiResponse<Restarted?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformFleetByAppDeployWithHttpInfo(app: kotlin.String, restartRef: RestartRef) : ApiResponse<Restarted?> {
        val localVariableConfig = postPlatformFleetByAppDeployRequestConfig(app = app, restartRef = restartRef)

        return request<RestartRef, Restarted>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformFleetByAppDeploy
     *
     * @param app App is the service&#39;s CR name, from the path. It must be a DNS-1123 label.
     * @param restartRef 
     * @return RequestConfig
     */
    fun postPlatformFleetByAppDeployRequestConfig(app: kotlin.String, restartRef: RestartRef) : RequestConfig<RestartRef> {
        val localVariableBody = restartRef
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/fleet/{app}/deploy".replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/hook
     * Receive a push from the forge and trigger its build
     * The forge&#39;s push-to-deploy door. git.hanzo.ai runs as a separate server, so its pushes never reach this fleet&#39;s own receive-pack; without this a push to the host we call canonical builds nothing. A verified push is handed to the SAME two seams a native push travels — the single-registrant deploy trigger, and the many-subscriber lifecycle stream that notifies and indexes — and the build decision itself stays downstream in the one place that knows what a push means.  PUBLIC at the JWT layer, because the forge carries no Hanzo session: AUTHENTICATION IS THE SIGNATURE. The HMAC covers the raw bytes and is verified BEFORE the payload is parsed, so an unauthenticated body is never decoded. The secret is read from KMS; a deployment that cannot read it answers 503 and processes nothing, rather than trusting a delivery it could not check. The body is read UNCOMPRESSED — a request declaring a Content-Encoding is refused 415 before it is touched, because decoding one is unbounded work bought with a few bytes and no credential. A bad signature is 401, a payload over 8 MiB is 413, and a malformed one 400.  A verified push that reaches both seams answers 200 with fired true and the NUMBER OF BUILDS it launched — zero is ordinary, since most pushes track no application, and it is the answer &#39;fired&#39; cannot give. A push that could not be dispatched answers 500: the delivery page shows it red, and the Replay that prompts reaches a fresh attempt rather than being declined as already landed.  The deliveries deliberately ignored answer 200 with a reason and nothing else: a payload that is not a push, a ref DELETE (a zero &#x60;after&#x60; has no commit to build), a BOT-authored push (release automation pushes as the forge&#39;s own Actions user, and a release must never rebuild itself), a push from a forge namespace that maps to no org, and a redelivery of a push already fired. Branches and tags both reach the build trigger, because releases are cut by tag and filtering here would silently stop publishing.
     * @param push  (optional)
     * @return Verdict
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformHook(push: Push? = null) : Verdict {
        val localVarResponse = postPlatformHookWithHttpInfo(push = push)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as Verdict
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/hook
     * Receive a push from the forge and trigger its build
     * The forge&#39;s push-to-deploy door. git.hanzo.ai runs as a separate server, so its pushes never reach this fleet&#39;s own receive-pack; without this a push to the host we call canonical builds nothing. A verified push is handed to the SAME two seams a native push travels — the single-registrant deploy trigger, and the many-subscriber lifecycle stream that notifies and indexes — and the build decision itself stays downstream in the one place that knows what a push means.  PUBLIC at the JWT layer, because the forge carries no Hanzo session: AUTHENTICATION IS THE SIGNATURE. The HMAC covers the raw bytes and is verified BEFORE the payload is parsed, so an unauthenticated body is never decoded. The secret is read from KMS; a deployment that cannot read it answers 503 and processes nothing, rather than trusting a delivery it could not check. The body is read UNCOMPRESSED — a request declaring a Content-Encoding is refused 415 before it is touched, because decoding one is unbounded work bought with a few bytes and no credential. A bad signature is 401, a payload over 8 MiB is 413, and a malformed one 400.  A verified push that reaches both seams answers 200 with fired true and the NUMBER OF BUILDS it launched — zero is ordinary, since most pushes track no application, and it is the answer &#39;fired&#39; cannot give. A push that could not be dispatched answers 500: the delivery page shows it red, and the Replay that prompts reaches a fresh attempt rather than being declined as already landed.  The deliveries deliberately ignored answer 200 with a reason and nothing else: a payload that is not a push, a ref DELETE (a zero &#x60;after&#x60; has no commit to build), a BOT-authored push (release automation pushes as the forge&#39;s own Actions user, and a release must never rebuild itself), a push from a forge namespace that maps to no org, and a redelivery of a push already fired. Branches and tags both reach the build trigger, because releases are cut by tag and filtering here would silently stop publishing.
     * @param push  (optional)
     * @return ApiResponse<Verdict?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformHookWithHttpInfo(push: Push?) : ApiResponse<Verdict?> {
        val localVariableConfig = postPlatformHookRequestConfig(push = push)

        return request<Push, Verdict>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformHook
     *
     * @param push  (optional)
     * @return RequestConfig
     */
    fun postPlatformHookRequestConfig(push: Push?) : RequestConfig<Push> {
        val localVariableBody = push
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/hook",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps
     * Creates an application from a git repo or a container image.
     * Creates an application from a git repo or a container image.  It registers a new application under one of the caller org&#39;s projects and answers 201 with it. Creating does NOT deploy: the app lands in &#x60;draft&#x60; and nothing reaches the cluster until /deploy.  &#x60;source&#x60; is &#x60;git&#x60; — which requires &#x60;repo.url&#x60; — or &#x60;image&#x60;, which requires &#x60;image.repository&#x60;; anything else is 400. A git app builds with zero-config &#x60;pack&#x60; by default and may opt into &#x60;dockerfile&#x60;; an image app never builds. The repo URL and Dockerfile path are validated here against the SAME allowlist the privileged build enforces, so an unsafe source is refused before it is ever persisted.  The &#x60;slug&#x60; is the app&#39;s identity in the cluster: given or derived from &#x60;name&#x60;, it must match &#x60;^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$&#x60;, and a slug already used in this project is 409. &#x60;replicas&#x60; and &#x60;storageGb&#x60; are clamped to the deployment&#39;s limits rather than refused.  Env keys must match &#x60;^[A-Za-z_][A-Za-z0-9_]*$&#x60;. A variable marked &#x60;secret: true&#x60; is SEALED into KMS and its plaintext is never written to the database — and if KMS is unavailable the create fails 503 rather than falling back to storing a secret in the clear.  The app is seeded with its canonical default host, so it has a working HTTPS URL the moment it deploys. A bare custom domain cannot be attached here — it has to go through add-domain and DNS verification first. Requires a validated principal; 403 without one, and every cluster object it will later create lands in that org&#39;s own &#x60;tenant-&lt;org&gt;&#x60; namespace.
     * @param project Project is the project to create the application under, from the path.
     * @param createAppReq 
     * @return AppView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectApps(project: kotlin.String, createAppReq: CreateAppReq) : AppView {
        val localVarResponse = postPlatformProjectsByProjectAppsWithHttpInfo(project = project, createAppReq = createAppReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as AppView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps
     * Creates an application from a git repo or a container image.
     * Creates an application from a git repo or a container image.  It registers a new application under one of the caller org&#39;s projects and answers 201 with it. Creating does NOT deploy: the app lands in &#x60;draft&#x60; and nothing reaches the cluster until /deploy.  &#x60;source&#x60; is &#x60;git&#x60; — which requires &#x60;repo.url&#x60; — or &#x60;image&#x60;, which requires &#x60;image.repository&#x60;; anything else is 400. A git app builds with zero-config &#x60;pack&#x60; by default and may opt into &#x60;dockerfile&#x60;; an image app never builds. The repo URL and Dockerfile path are validated here against the SAME allowlist the privileged build enforces, so an unsafe source is refused before it is ever persisted.  The &#x60;slug&#x60; is the app&#39;s identity in the cluster: given or derived from &#x60;name&#x60;, it must match &#x60;^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$&#x60;, and a slug already used in this project is 409. &#x60;replicas&#x60; and &#x60;storageGb&#x60; are clamped to the deployment&#39;s limits rather than refused.  Env keys must match &#x60;^[A-Za-z_][A-Za-z0-9_]*$&#x60;. A variable marked &#x60;secret: true&#x60; is SEALED into KMS and its plaintext is never written to the database — and if KMS is unavailable the create fails 503 rather than falling back to storing a secret in the clear.  The app is seeded with its canonical default host, so it has a working HTTPS URL the moment it deploys. A bare custom domain cannot be attached here — it has to go through add-domain and DNS verification first. Requires a validated principal; 403 without one, and every cluster object it will later create lands in that org&#39;s own &#x60;tenant-&lt;org&gt;&#x60; namespace.
     * @param project Project is the project to create the application under, from the path.
     * @param createAppReq 
     * @return ApiResponse<AppView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsWithHttpInfo(project: kotlin.String, createAppReq: CreateAppReq) : ApiResponse<AppView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsRequestConfig(project = project, createAppReq = createAppReq)

        return request<CreateAppReq, AppView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectApps
     *
     * @param project Project is the project to create the application under, from the path.
     * @param createAppReq 
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsRequestConfig(project: kotlin.String, createAppReq: CreateAppReq) : RequestConfig<CreateAppReq> {
        val localVariableBody = createAppReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps".replace("{"+"project"+"}", encodeURIComponent(project.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/deploy
     * Deploys the app — building it first if it comes from git.
     * Deploys the app — building it first if it comes from git.  It starts a new, monotonically versioned deployment of the app and answers 202 with the deployment record. A 202 is an ACCEPTED deployment, not a live one.  An IMAGE app deploys the tag you name (falling back to the app&#39;s tag, then &#x60;latest&#x60;) by writing its operator Service CR; the operator reconciles it to running. A GIT app launches an in-cluster BuildKit Job at &#x60;commit&#x60; — or the app&#39;s branch — and comes back in &#x60;building&#x60;; the Service CR is applied later, by the reconciler, once the Job succeeds. The reconciler is restart-safe, so a build in flight survives a cloud restart.  Deploys are bounded per org: over the concurrent-deploy cap is 429 and NOTHING is recorded, so a rejected deploy leaves no phantom in the history. An unreachable cluster is 503 but still records an honest &#x60;error&#x60; deployment, because a deploy that was attempted and failed must not be indistinguishable from one never made. Every other failure is likewise recorded in its real terminal state.  This is metered work: a git build is billed to the org&#39;s ledger in wall-clock build minutes once the Job finishes, and the running deployment is billed for its compute per tick for as long as it stays live. Requires a validated principal; 403 without one, and everything is written into that org&#39;s own &#x60;tenant-&lt;org&gt;&#x60; namespace.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param deployReq 
     * @return DeploymentView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectAppsByAppDeploy(project: kotlin.String, app: kotlin.String, deployReq: DeployReq) : DeploymentView {
        val localVarResponse = postPlatformProjectsByProjectAppsByAppDeployWithHttpInfo(project = project, app = app, deployReq = deployReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as DeploymentView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/deploy
     * Deploys the app — building it first if it comes from git.
     * Deploys the app — building it first if it comes from git.  It starts a new, monotonically versioned deployment of the app and answers 202 with the deployment record. A 202 is an ACCEPTED deployment, not a live one.  An IMAGE app deploys the tag you name (falling back to the app&#39;s tag, then &#x60;latest&#x60;) by writing its operator Service CR; the operator reconciles it to running. A GIT app launches an in-cluster BuildKit Job at &#x60;commit&#x60; — or the app&#39;s branch — and comes back in &#x60;building&#x60;; the Service CR is applied later, by the reconciler, once the Job succeeds. The reconciler is restart-safe, so a build in flight survives a cloud restart.  Deploys are bounded per org: over the concurrent-deploy cap is 429 and NOTHING is recorded, so a rejected deploy leaves no phantom in the history. An unreachable cluster is 503 but still records an honest &#x60;error&#x60; deployment, because a deploy that was attempted and failed must not be indistinguishable from one never made. Every other failure is likewise recorded in its real terminal state.  This is metered work: a git build is billed to the org&#39;s ledger in wall-clock build minutes once the Job finishes, and the running deployment is billed for its compute per tick for as long as it stays live. Requires a validated principal; 403 without one, and everything is written into that org&#39;s own &#x60;tenant-&lt;org&gt;&#x60; namespace.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param deployReq 
     * @return ApiResponse<DeploymentView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsByAppDeployWithHttpInfo(project: kotlin.String, app: kotlin.String, deployReq: DeployReq) : ApiResponse<DeploymentView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsByAppDeployRequestConfig(project = project, app = app, deployReq = deployReq)

        return request<DeployReq, DeploymentView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectAppsByAppDeploy
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param deployReq 
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsByAppDeployRequestConfig(project: kotlin.String, app: kotlin.String, deployReq: DeployReq) : RequestConfig<DeployReq> {
        val localVariableBody = deployReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps/{app}/deploy".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/domains
     * Attaches a hostname — instantly if you already own it, otherwise with a DNS challenge.
     * Attaches a hostname — instantly if you already own it, otherwise with a DNS challenge.  It attaches &#x60;host&#x60; to the app, and which of two things happens depends on who owns the name. A host inside the caller org&#39;s own subtree is structurally owned, so it goes ACTIVE immediately and answers 201. A bring-your-own host is claimed as PENDING and answers the DNS challenge records to publish; it is NOT rendered into the app&#39;s ingress until /verify passes.  Claims are globally unique. A host already claimed by another organization is 409, and so is one claimed by a different app in your own; re-adding this app&#39;s OWN claim is idempotent and answers its current state at 200. The default host is always attached and re-adding it is 409. A host under the platform&#39;s shared apex that is not the caller&#39;s own subtree is 403 — it belongs to whoever owns that subtree and can never be grabbed through the custom path.  &#x60;host&#x60; must be a valid DNS hostname; anything else is 400. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param addDomainReq 
     * @return DomainView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectAppsByAppDomains(project: kotlin.String, app: kotlin.String, addDomainReq: AddDomainReq) : DomainView {
        val localVarResponse = postPlatformProjectsByProjectAppsByAppDomainsWithHttpInfo(project = project, app = app, addDomainReq = addDomainReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as DomainView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/domains
     * Attaches a hostname — instantly if you already own it, otherwise with a DNS challenge.
     * Attaches a hostname — instantly if you already own it, otherwise with a DNS challenge.  It attaches &#x60;host&#x60; to the app, and which of two things happens depends on who owns the name. A host inside the caller org&#39;s own subtree is structurally owned, so it goes ACTIVE immediately and answers 201. A bring-your-own host is claimed as PENDING and answers the DNS challenge records to publish; it is NOT rendered into the app&#39;s ingress until /verify passes.  Claims are globally unique. A host already claimed by another organization is 409, and so is one claimed by a different app in your own; re-adding this app&#39;s OWN claim is idempotent and answers its current state at 200. The default host is always attached and re-adding it is 409. A host under the platform&#39;s shared apex that is not the caller&#39;s own subtree is 403 — it belongs to whoever owns that subtree and can never be grabbed through the custom path.  &#x60;host&#x60; must be a valid DNS hostname; anything else is 400. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param addDomainReq 
     * @return ApiResponse<DomainView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsByAppDomainsWithHttpInfo(project: kotlin.String, app: kotlin.String, addDomainReq: AddDomainReq) : ApiResponse<DomainView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsByAppDomainsRequestConfig(project = project, app = app, addDomainReq = addDomainReq)

        return request<AddDomainReq, DomainView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectAppsByAppDomains
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param addDomainReq 
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsByAppDomainsRequestConfig(project: kotlin.String, app: kotlin.String, addDomainReq: AddDomainReq) : RequestConfig<AddDomainReq> {
        val localVariableBody = addDomainReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps/{app}/domains".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/domains/{host}/verify
     * Checks a custom domain&#39;s DNS and turns it on if it passes.
     * Checks a custom domain&#39;s DNS and turns it on if it passes.  It runs the DNS challenge check for a pending custom host and, when it passes, marks the host verified and renders it into the app&#39;s ingress so it starts serving.  A check that RAN and did not pass is not an error: it answers 200 with the host still pending and the reason in &#x60;detail&#x60;, so a console can show the operator what DNS is actually returning. An already-verified host answers as-is without re-checking. A host not claimed by this app is 404. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param host Host is the hostname, from the path.
     * @return DomainView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectAppsByAppDomainsByHostVerify(project: kotlin.String, app: kotlin.String, host: kotlin.String) : DomainView {
        val localVarResponse = postPlatformProjectsByProjectAppsByAppDomainsByHostVerifyWithHttpInfo(project = project, app = app, host = host)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as DomainView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/domains/{host}/verify
     * Checks a custom domain&#39;s DNS and turns it on if it passes.
     * Checks a custom domain&#39;s DNS and turns it on if it passes.  It runs the DNS challenge check for a pending custom host and, when it passes, marks the host verified and renders it into the app&#39;s ingress so it starts serving.  A check that RAN and did not pass is not an error: it answers 200 with the host still pending and the reason in &#x60;detail&#x60;, so a console can show the operator what DNS is actually returning. An already-verified host answers as-is without re-checking. A host not claimed by this app is 404. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param host Host is the hostname, from the path.
     * @return ApiResponse<DomainView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsByAppDomainsByHostVerifyWithHttpInfo(project: kotlin.String, app: kotlin.String, host: kotlin.String) : ApiResponse<DomainView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsByAppDomainsByHostVerifyRequestConfig(project = project, app = app, host = host)

        return request<Unit, DomainView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectAppsByAppDomainsByHostVerify
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param host Host is the hostname, from the path.
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsByAppDomainsByHostVerifyRequestConfig(project: kotlin.String, app: kotlin.String, host: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps/{app}/domains/{host}/verify".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())).replace("{"+"host"+"}", encodeURIComponent(host.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/preview
     * Puts a branch on its own URL.
     * Puts a branch on its own URL.  It deploys an already-built &#x60;image&#x60; to a per-branch preview and answers its URL, the branch, the preview&#39;s slug and the deployment. The preview is a FIRST-CLASS application named &#x60;&lt;app&gt;-&lt;branch&gt;&#x60; in the same project and tenant namespace, with its own default host — so it is completely isolated from production while reusing the same deploy mechanic. Re-previewing a branch converges that same target in place rather than stacking another one.  It carries NO environment variables, deliberately: a preview never inherits production&#39;s secrets. It also does not build — &#x60;image&#x60; is required and must already exist, and &#x60;branch&#x60; defaults to the parent app&#39;s. A branch that does not resolve to a valid slug distinct from the parent&#39;s is 400. Requires a validated principal; 403 without one.
     * @param project Project is the project the parent application lives under, from the path.
     * @param app App is the parent application&#39;s slug, from the path.
     * @param previewReq 
     * @return PreviewView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectAppsByAppPreview(project: kotlin.String, app: kotlin.String, previewReq: PreviewReq) : PreviewView {
        val localVarResponse = postPlatformProjectsByProjectAppsByAppPreviewWithHttpInfo(project = project, app = app, previewReq = previewReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as PreviewView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/preview
     * Puts a branch on its own URL.
     * Puts a branch on its own URL.  It deploys an already-built &#x60;image&#x60; to a per-branch preview and answers its URL, the branch, the preview&#39;s slug and the deployment. The preview is a FIRST-CLASS application named &#x60;&lt;app&gt;-&lt;branch&gt;&#x60; in the same project and tenant namespace, with its own default host — so it is completely isolated from production while reusing the same deploy mechanic. Re-previewing a branch converges that same target in place rather than stacking another one.  It carries NO environment variables, deliberately: a preview never inherits production&#39;s secrets. It also does not build — &#x60;image&#x60; is required and must already exist, and &#x60;branch&#x60; defaults to the parent app&#39;s. A branch that does not resolve to a valid slug distinct from the parent&#39;s is 400. Requires a validated principal; 403 without one.
     * @param project Project is the project the parent application lives under, from the path.
     * @param app App is the parent application&#39;s slug, from the path.
     * @param previewReq 
     * @return ApiResponse<PreviewView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsByAppPreviewWithHttpInfo(project: kotlin.String, app: kotlin.String, previewReq: PreviewReq) : ApiResponse<PreviewView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsByAppPreviewRequestConfig(project = project, app = app, previewReq = previewReq)

        return request<PreviewReq, PreviewView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectAppsByAppPreview
     *
     * @param project Project is the project the parent application lives under, from the path.
     * @param app App is the parent application&#39;s slug, from the path.
     * @param previewReq 
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsByAppPreviewRequestConfig(project: kotlin.String, app: kotlin.String, previewReq: PreviewReq) : RequestConfig<PreviewReq> {
        val localVariableBody = previewReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps/{app}/preview".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/promote
     * Promotes an already-built release to the app.
     * Promotes an already-built release to the app.  It redeploys an image that already exists — named either by &#x60;deploymentId&#x60;, which promotes that deployment&#39;s exact built image, or by &#x60;tag&#x60;, resolved the same way a deploy resolves one. One of the two is required; neither is 400.  Promotion never builds. A deployment that carries no built image cannot be promoted and is 400, and a deployment id outside this app is 404. It runs through the same deploy core as everything else, so it takes a NEW version number and is subject to the same per-org concurrency cap. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param promoteReq 
     * @return DeploymentView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectAppsByAppPromote(project: kotlin.String, app: kotlin.String, promoteReq: PromoteReq) : DeploymentView {
        val localVarResponse = postPlatformProjectsByProjectAppsByAppPromoteWithHttpInfo(project = project, app = app, promoteReq = promoteReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as DeploymentView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/promote
     * Promotes an already-built release to the app.
     * Promotes an already-built release to the app.  It redeploys an image that already exists — named either by &#x60;deploymentId&#x60;, which promotes that deployment&#39;s exact built image, or by &#x60;tag&#x60;, resolved the same way a deploy resolves one. One of the two is required; neither is 400.  Promotion never builds. A deployment that carries no built image cannot be promoted and is 400, and a deployment id outside this app is 404. It runs through the same deploy core as everything else, so it takes a NEW version number and is subject to the same per-org concurrency cap. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param promoteReq 
     * @return ApiResponse<DeploymentView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsByAppPromoteWithHttpInfo(project: kotlin.String, app: kotlin.String, promoteReq: PromoteReq) : ApiResponse<DeploymentView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsByAppPromoteRequestConfig(project = project, app = app, promoteReq = promoteReq)

        return request<PromoteReq, DeploymentView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectAppsByAppPromote
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param promoteReq 
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsByAppPromoteRequestConfig(project: kotlin.String, app: kotlin.String, promoteReq: PromoteReq) : RequestConfig<PromoteReq> {
        val localVariableBody = promoteReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps/{app}/promote".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/rollback
     * Goes back to the previous release.
     * Goes back to the previous release.  It redeploys a prior image: the one named by &#x60;deploymentId&#x60;, or — with no body — the newest earlier deployment that carries a real built image and did not error, skipping the release currently live. An app with nothing earlier to return to is 400.  A rollback is a deploy of an old image, not a rewind: it takes a NEW version number and appends to the history rather than erasing what came after. Both lookups are scoped to this app and org, so another tenant&#39;s image can never be rolled in. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param rollbackReq 
     * @return DeploymentView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectAppsByAppRollback(project: kotlin.String, app: kotlin.String, rollbackReq: RollbackReq) : DeploymentView {
        val localVarResponse = postPlatformProjectsByProjectAppsByAppRollbackWithHttpInfo(project = project, app = app, rollbackReq = rollbackReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as DeploymentView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/rollback
     * Goes back to the previous release.
     * Goes back to the previous release.  It redeploys a prior image: the one named by &#x60;deploymentId&#x60;, or — with no body — the newest earlier deployment that carries a real built image and did not error, skipping the release currently live. An app with nothing earlier to return to is 400.  A rollback is a deploy of an old image, not a rewind: it takes a NEW version number and appends to the history rather than erasing what came after. Both lookups are scoped to this app and org, so another tenant&#39;s image can never be rolled in. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param rollbackReq 
     * @return ApiResponse<DeploymentView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsByAppRollbackWithHttpInfo(project: kotlin.String, app: kotlin.String, rollbackReq: RollbackReq) : ApiResponse<DeploymentView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsByAppRollbackRequestConfig(project = project, app = app, rollbackReq = rollbackReq)

        return request<RollbackReq, DeploymentView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectAppsByAppRollback
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param rollbackReq 
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsByAppRollbackRequestConfig(project: kotlin.String, app: kotlin.String, rollbackReq: RollbackReq) : RequestConfig<RollbackReq> {
        val localVariableBody = rollbackReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps/{app}/rollback".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/start
     * Starts a stopped app back up.
     * Starts a stopped app back up.  It scales the app&#39;s Service back to its configured replica count and marks it live, answering the updated application. It does not redeploy: the image already on the Service CR is what comes back.  The billing watermark is reset to now as part of starting, so the org is charged for THIS live span and never for the gap the app spent stopped. An app with no Service CR is 404, an unreachable cluster is 503, and a cluster that refuses the scale is 502. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return AppView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectAppsByAppStart(project: kotlin.String, app: kotlin.String) : AppView {
        val localVarResponse = postPlatformProjectsByProjectAppsByAppStartWithHttpInfo(project = project, app = app)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as AppView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/start
     * Starts a stopped app back up.
     * Starts a stopped app back up.  It scales the app&#39;s Service back to its configured replica count and marks it live, answering the updated application. It does not redeploy: the image already on the Service CR is what comes back.  The billing watermark is reset to now as part of starting, so the org is charged for THIS live span and never for the gap the app spent stopped. An app with no Service CR is 404, an unreachable cluster is 503, and a cluster that refuses the scale is 502. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return ApiResponse<AppView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsByAppStartWithHttpInfo(project: kotlin.String, app: kotlin.String) : ApiResponse<AppView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsByAppStartRequestConfig(project = project, app = app)

        return request<Unit, AppView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectAppsByAppStart
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsByAppStartRequestConfig(project: kotlin.String, app: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps/{app}/start".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/stop
     * Stops an app without deleting it.
     * Stops an app without deleting it.  It scales the app&#39;s Service to zero replicas and marks it stopped, answering the updated application. Nothing else is removed — the record, its env, its domains and its deployment history all survive, and /start brings it back at the same replica count.  An app that is not deployed has no Service CR to scale and is 404. An unreachable cluster is 503 and a cluster that refuses the scale is 502. Because the pods stop, so does the compute metering. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return AppView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformProjectsByProjectAppsByAppStop(project: kotlin.String, app: kotlin.String) : AppView {
        val localVarResponse = postPlatformProjectsByProjectAppsByAppStopWithHttpInfo(project = project, app = app)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as AppView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/projects/{project}/apps/{app}/stop
     * Stops an app without deleting it.
     * Stops an app without deleting it.  It scales the app&#39;s Service to zero replicas and marks it stopped, answering the updated application. Nothing else is removed — the record, its env, its domains and its deployment history all survive, and /start brings it back at the same replica count.  An app that is not deployed has no Service CR to scale and is 404. An unreachable cluster is 503 and a cluster that refuses the scale is 502. Because the pods stop, so does the compute metering. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return ApiResponse<AppView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformProjectsByProjectAppsByAppStopWithHttpInfo(project: kotlin.String, app: kotlin.String) : ApiResponse<AppView?> {
        val localVariableConfig = postPlatformProjectsByProjectAppsByAppStopRequestConfig(project = project, app = app)

        return request<Unit, AppView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformProjectsByProjectAppsByAppStop
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @return RequestConfig
     */
    fun postPlatformProjectsByProjectAppsByAppStopRequestConfig(project: kotlin.String, app: kotlin.String) : RequestConfig<Unit> {
        val localVariableBody = null
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/projects/{project}/apps/{app}/stop".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/run
     * Runs a container image and gives back a URL.
     * Runs a container image and gives back a URL.  The one-call shortcut over project → app → deploy: give it a &#x60;name&#x60; and an &#x60;image&#x60; and it creates or updates an image-source application in your org&#39;s DEFAULT project, deploys it through the same operator Service-CR writer everything else uses, and answers its id, name, live URL, status and shape. Re-running the same name UPDATES it in place, so the call is idempotent by name.  What it produces is a first-class application, not a special object: it is listable, stoppable and redeployable through the /v1/platform routes like any other app.  &#x60;minScale&#x60; is the replica floor. &#x60;maxScale&#x60; above it declares an autoscaling ceiling; &#x60;maxScale: 0&#x60; means no autoscaler at all — a fixed run at the floor. Both are clamped to the deployment&#39;s limits. &#x60;runtime&#x60; and &#x60;shape&#x60; are accepted for the client contract and echoed back: the image is the runtime unit and sizing is the operator&#39;s default.  It is BILLING-GATED before it touches the cluster: a flat per-run fee is authorized against the org&#39;s own prepaid balance first, so an org that cannot pay is refused without anything being created. An unreachable cluster is 503 — a run never reports a URL it did not create. Secret env is sealed into KMS and fails closed without it.  Requires a validated principal; 403 without one. The org is resolved from that validated identity and is what both pays and owns the namespace — it is never read from the body.
     * @param runReq 
     * @return RunView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformRun(runReq: RunReq) : RunView {
        val localVarResponse = postPlatformRunWithHttpInfo(runReq = runReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as RunView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/run
     * Runs a container image and gives back a URL.
     * Runs a container image and gives back a URL.  The one-call shortcut over project → app → deploy: give it a &#x60;name&#x60; and an &#x60;image&#x60; and it creates or updates an image-source application in your org&#39;s DEFAULT project, deploys it through the same operator Service-CR writer everything else uses, and answers its id, name, live URL, status and shape. Re-running the same name UPDATES it in place, so the call is idempotent by name.  What it produces is a first-class application, not a special object: it is listable, stoppable and redeployable through the /v1/platform routes like any other app.  &#x60;minScale&#x60; is the replica floor. &#x60;maxScale&#x60; above it declares an autoscaling ceiling; &#x60;maxScale: 0&#x60; means no autoscaler at all — a fixed run at the floor. Both are clamped to the deployment&#39;s limits. &#x60;runtime&#x60; and &#x60;shape&#x60; are accepted for the client contract and echoed back: the image is the runtime unit and sizing is the operator&#39;s default.  It is BILLING-GATED before it touches the cluster: a flat per-run fee is authorized against the org&#39;s own prepaid balance first, so an org that cannot pay is refused without anything being created. An unreachable cluster is 503 — a run never reports a URL it did not create. Secret env is sealed into KMS and fails closed without it.  Requires a validated principal; 403 without one. The org is resolved from that validated identity and is what both pays and owns the namespace — it is never read from the body.
     * @param runReq 
     * @return ApiResponse<RunView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformRunWithHttpInfo(runReq: RunReq) : ApiResponse<RunView?> {
        val localVariableConfig = postPlatformRunRequestConfig(runReq = runReq)

        return request<RunReq, RunView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformRun
     *
     * @param runReq 
     * @return RequestConfig
     */
    fun postPlatformRunRequestConfig(runReq: RunReq) : RequestConfig<RunReq> {
        val localVariableBody = runReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/run",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * POST /v1/platform/runner
     * Triggers a native build — an image, or the binaries a repo declares.
     * Triggers a native build — an image, or the binaries a repo declares.  The fabric&#39;s own build trigger, and what &#x60;hanzo build&#x60; and git-push-to-deploy call. It answers 202 with the build job id: a queued build, not a pushed artifact.  Two lanes, and a build is exactly one of them. The IMAGE lane takes &#x60;repo&#x60; and the output &#x60;image&#x60; and launches a BuildKit Job that pushes it. The ARTIFACT lane takes &#x60;binaries&#x60; — the same recipe the repo&#39;s hanzo.yml declares — and publishes to object storage instead; it must carry no &#x60;image&#x60;, because a build produces binaries or an image, never both.  PRIVILEGED, and A BUILD BELONGS TO THE ORGANIZATION ITS CREDENTIAL NAMES. Two credentials, never a third:    - one that NAMES an organization — a person who administers it (the &#x60;hanzo     build&#x60; path, so one IAM login authorizes a build with no separate build     token), or that organization&#39;s own machine identity (the pipeline path). The     build is attributed to that org and confined to what it owns.   - the shared build-callback token, compared in constant time. It names NO     organization, which is both why the fabric&#39;s own release can publish across     brands with it and why anything that CAN name one is read first.  Both are bounded by the owned-registry allowlist. The org path is bounded again, by the org: the image&#39;s registry namespace must be one that organization owns, so it publishes into its own brand and can never overwrite another&#39;s through the shared push credential. The same confinement applies to the artifact lane&#39;s repo owner. There is no request field naming an organization — the attribution is read off the credential, so there is nothing for a caller to write it with.  The output image is parsed and validated as a single well-formed OCI ref before any authorization decision reads it, so a crafted ref cannot smuggle a build-exporter attribute past the check.
     * @param runnerBuildReq 
     * @return RunnerBuildResp
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun postPlatformRunner(runnerBuildReq: RunnerBuildReq) : RunnerBuildResp {
        val localVarResponse = postPlatformRunnerWithHttpInfo(runnerBuildReq = runnerBuildReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as RunnerBuildResp
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * POST /v1/platform/runner
     * Triggers a native build — an image, or the binaries a repo declares.
     * Triggers a native build — an image, or the binaries a repo declares.  The fabric&#39;s own build trigger, and what &#x60;hanzo build&#x60; and git-push-to-deploy call. It answers 202 with the build job id: a queued build, not a pushed artifact.  Two lanes, and a build is exactly one of them. The IMAGE lane takes &#x60;repo&#x60; and the output &#x60;image&#x60; and launches a BuildKit Job that pushes it. The ARTIFACT lane takes &#x60;binaries&#x60; — the same recipe the repo&#39;s hanzo.yml declares — and publishes to object storage instead; it must carry no &#x60;image&#x60;, because a build produces binaries or an image, never both.  PRIVILEGED, and A BUILD BELONGS TO THE ORGANIZATION ITS CREDENTIAL NAMES. Two credentials, never a third:    - one that NAMES an organization — a person who administers it (the &#x60;hanzo     build&#x60; path, so one IAM login authorizes a build with no separate build     token), or that organization&#39;s own machine identity (the pipeline path). The     build is attributed to that org and confined to what it owns.   - the shared build-callback token, compared in constant time. It names NO     organization, which is both why the fabric&#39;s own release can publish across     brands with it and why anything that CAN name one is read first.  Both are bounded by the owned-registry allowlist. The org path is bounded again, by the org: the image&#39;s registry namespace must be one that organization owns, so it publishes into its own brand and can never overwrite another&#39;s through the shared push credential. The same confinement applies to the artifact lane&#39;s repo owner. There is no request field naming an organization — the attribution is read off the credential, so there is nothing for a caller to write it with.  The output image is parsed and validated as a single well-formed OCI ref before any authorization decision reads it, so a crafted ref cannot smuggle a build-exporter attribute past the check.
     * @param runnerBuildReq 
     * @return ApiResponse<RunnerBuildResp?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun postPlatformRunnerWithHttpInfo(runnerBuildReq: RunnerBuildReq) : ApiResponse<RunnerBuildResp?> {
        val localVariableConfig = postPlatformRunnerRequestConfig(runnerBuildReq = runnerBuildReq)

        return request<RunnerBuildReq, RunnerBuildResp>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation postPlatformRunner
     *
     * @param runnerBuildReq 
     * @return RequestConfig
     */
    fun postPlatformRunnerRequestConfig(runnerBuildReq: RunnerBuildReq) : RequestConfig<RunnerBuildReq> {
        val localVariableBody = runnerBuildReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.POST,
            path = "/v1/platform/runner",
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }

    /**
     * PUT /v1/platform/projects/{project}/apps/{app}/env
     * Replaces an app&#39;s environment variables.
     * Replaces an app&#39;s environment variables.  It writes the app&#39;s whole environment set and answers the updated application. This is the one post-create write path for env, and it REPLACES rather than merges: a variable absent from the body is gone, and a secret dropped from the set leaves the app&#39;s Secret on its next deploy.  Keys must match &#x60;^[A-Za-z_][A-Za-z0-9_]*$&#x60;. A value marked &#x60;secret: true&#x60; is sealed into KMS and blanked in the database, so plaintext is never persisted — and the write fails 503 if KMS is unavailable rather than storing one in the clear.  The rule worth knowing: this does not restart anything. Once the app has been deployed the secret sync is re-declared immediately so the operator re-materialises the Secret, but RUNNING pods keep the environment they started with until their next deploy or restart. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param setEnvReq 
     * @return AppView
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     * @throws UnsupportedOperationException If the API returns an informational or redirection response
     * @throws ClientException If the API returns a client error response
     * @throws ServerException If the API returns a server error response
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class, UnsupportedOperationException::class, ClientException::class, ServerException::class)
    fun putPlatformProjectsByProjectAppsByAppEnv(project: kotlin.String, app: kotlin.String, setEnvReq: SetEnvReq) : AppView {
        val localVarResponse = putPlatformProjectsByProjectAppsByAppEnvWithHttpInfo(project = project, app = app, setEnvReq = setEnvReq)

        return when (localVarResponse.responseType) {
            ResponseType.Success -> (localVarResponse as Success<*>).data as AppView
            ResponseType.Informational -> throw UnsupportedOperationException("Client does not support Informational responses.")
            ResponseType.Redirection -> throw UnsupportedOperationException("Client does not support Redirection responses.")
            ResponseType.ClientError -> {
                val localVarError = localVarResponse as ClientError<*>
                throw ClientException("Client error : ${localVarError.statusCode} ${localVarError.message.orEmpty()}", localVarError.statusCode, localVarResponse)
            }
            ResponseType.ServerError -> {
                val localVarError = localVarResponse as ServerError<*>
                throw ServerException("Server error : ${localVarError.statusCode} ${localVarError.message.orEmpty()} ${localVarError.body}", localVarError.statusCode, localVarResponse)
            }
        }
    }

    /**
     * PUT /v1/platform/projects/{project}/apps/{app}/env
     * Replaces an app&#39;s environment variables.
     * Replaces an app&#39;s environment variables.  It writes the app&#39;s whole environment set and answers the updated application. This is the one post-create write path for env, and it REPLACES rather than merges: a variable absent from the body is gone, and a secret dropped from the set leaves the app&#39;s Secret on its next deploy.  Keys must match &#x60;^[A-Za-z_][A-Za-z0-9_]*$&#x60;. A value marked &#x60;secret: true&#x60; is sealed into KMS and blanked in the database, so plaintext is never persisted — and the write fails 503 if KMS is unavailable rather than storing one in the clear.  The rule worth knowing: this does not restart anything. Once the app has been deployed the secret sync is re-declared immediately so the operator re-materialises the Secret, but RUNNING pods keep the environment they started with until their next deploy or restart. Requires a validated principal; 403 without one.
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param setEnvReq 
     * @return ApiResponse<AppView?>
     * @throws IllegalStateException If the request is not correctly configured
     * @throws IOException Rethrows the OkHttp execute method exception
     */
    @Suppress("UNCHECKED_CAST")
    @Throws(IllegalStateException::class, IOException::class)
    fun putPlatformProjectsByProjectAppsByAppEnvWithHttpInfo(project: kotlin.String, app: kotlin.String, setEnvReq: SetEnvReq) : ApiResponse<AppView?> {
        val localVariableConfig = putPlatformProjectsByProjectAppsByAppEnvRequestConfig(project = project, app = app, setEnvReq = setEnvReq)

        return request<SetEnvReq, AppView>(
            localVariableConfig
        )
    }

    /**
     * To obtain the request config of the operation putPlatformProjectsByProjectAppsByAppEnv
     *
     * @param project Project is the project the application lives under, from the path.
     * @param app App is the application&#39;s slug, from the path.
     * @param setEnvReq 
     * @return RequestConfig
     */
    fun putPlatformProjectsByProjectAppsByAppEnvRequestConfig(project: kotlin.String, app: kotlin.String, setEnvReq: SetEnvReq) : RequestConfig<SetEnvReq> {
        val localVariableBody = setEnvReq
        val localVariableQuery: MultiValueMap = mutableMapOf()
        val localVariableHeaders: MutableMap<String, String> = mutableMapOf()
        localVariableHeaders["Content-Type"] = "application/json"
        localVariableHeaders["Accept"] = "application/json"

        return RequestConfig(
            method = RequestMethod.PUT,
            path = "/v1/platform/projects/{project}/apps/{app}/env".replace("{"+"project"+"}", encodeURIComponent(project.toString())).replace("{"+"app"+"}", encodeURIComponent(app.toString())),
            query = localVariableQuery,
            headers = localVariableHeaders,
            requiresAuthentication = true,
            body = localVariableBody
        )
    }


    private fun encodeURIComponent(uriComponent: kotlin.String): kotlin.String =
        HttpUrl.Builder().scheme("http").host("localhost").addPathSegment(uriComponent).build().encodedPathSegments[0]
}
