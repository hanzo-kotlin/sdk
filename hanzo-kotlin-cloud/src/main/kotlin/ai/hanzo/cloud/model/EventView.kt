/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param actor Actor is who produced the turn. A write that names nobody takes the calling principal, so this is rarely empty in practice.
 * @param createdAt CreatedAt is when the turn was recorded, RFC 3339 in UTC to the second. Seconds are coarse enough that two turns can share one, which is why Seq and not this is the order.
 * @param id ID is the event's own handle, minted as \"evt_\" + 32 hex characters. It identifies the turn; Seq is what ORDERS it.
 * @param kind Kind is what the turn IS, from a closed six: message (a model turn), tool-call, spawn (a subagent started), log, status, control (a steering command the running surface consumes). Anything else is refused at the write.
 * @param payload 
 * @param seq Seq is the turn's position in this session's log: monotonic from 1, assigned by the store inside the insert, and unique PER SESSION rather than globally. It is the cursor a reader resumes from after a reconnect — ask for everything after your last-seen seq.
 * @param sessionId SessionID is the session this turn belongs to. Carried on every event so a stream frame stands alone — a subscriber watching a whole tree gets turns from several sessions down one connection.
 */


data class EventView (

    /* Actor is who produced the turn. A write that names nobody takes the calling principal, so this is rarely empty in practice. */
    @SerializedName("actor")
    val actor: kotlin.String? = null,

    /* CreatedAt is when the turn was recorded, RFC 3339 in UTC to the second. Seconds are coarse enough that two turns can share one, which is why Seq and not this is the order. */
    @SerializedName("createdAt")
    val createdAt: kotlin.String? = null,

    /* ID is the event's own handle, minted as \"evt_\" + 32 hex characters. It identifies the turn; Seq is what ORDERS it. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Kind is what the turn IS, from a closed six: message (a model turn), tool-call, spawn (a subagent started), log, status, control (a steering command the running surface consumes). Anything else is refused at the write. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    @SerializedName("payload")
    val payload: kotlin.Any? = null,

    /* Seq is the turn's position in this session's log: monotonic from 1, assigned by the store inside the insert, and unique PER SESSION rather than globally. It is the cursor a reader resumes from after a reconnect — ask for everything after your last-seen seq. */
    @SerializedName("seq")
    val seq: kotlin.Int? = null,

    /* SessionID is the session this turn belongs to. Carried on every event so a stream frame stands alone — a subscriber watching a whole tree gets turns from several sessions down one connection. */
    @SerializedName("sessionId")
    val sessionId: kotlin.String? = null

) {


}

