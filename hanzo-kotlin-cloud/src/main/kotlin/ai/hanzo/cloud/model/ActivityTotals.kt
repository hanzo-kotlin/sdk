/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param activeDays ActiveDays counts the days with any usage at all — the streak/consistency number. Compare it against len(days) for the share of days the subject showed up.
 * @param costCents CostCents is the window's spend in whole US cents, the sum of Days[].CostCents.
 * @param maxRequests MaxRequests is the same ceiling for a request-based heatmap — the busiest single day's request count, 0 for an idle window.
 * @param maxTokens MaxTokens is the busiest single day's token count: the ceiling to normalize a token heatmap against, so the darkest cell is that day. 0 for an idle window, which a client must not divide by.
 * @param requests Requests is the sum of Days[].Requests over the whole window.
 * @param tokens Tokens is the sum of Days[].Tokens over the whole window.
 */


data class ActivityTotals (

    /* ActiveDays counts the days with any usage at all — the streak/consistency number. Compare it against len(days) for the share of days the subject showed up. */
    @SerializedName("activeDays")
    val activeDays: kotlin.Int? = null,

    /* CostCents is the window's spend in whole US cents, the sum of Days[].CostCents. */
    @SerializedName("costCents")
    val costCents: kotlin.Int? = null,

    /* MaxRequests is the same ceiling for a request-based heatmap — the busiest single day's request count, 0 for an idle window. */
    @SerializedName("maxRequests")
    val maxRequests: kotlin.Int? = null,

    /* MaxTokens is the busiest single day's token count: the ceiling to normalize a token heatmap against, so the darkest cell is that day. 0 for an idle window, which a client must not divide by. */
    @SerializedName("maxTokens")
    val maxTokens: kotlin.Int? = null,

    /* Requests is the sum of Days[].Requests over the whole window. */
    @SerializedName("requests")
    val requests: kotlin.Int? = null,

    /* Tokens is the sum of Days[].Tokens over the whole window. */
    @SerializedName("tokens")
    val tokens: kotlin.Int? = null

) {


}

