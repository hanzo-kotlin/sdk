/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.PluginPluginUsage

import com.google.gson.annotations.SerializedName

/**
 * One plugin's process view on one host — what `GET /v1/plugins` cannot answer, because that reports the manifest and this reports the kernel. 
 *
 * @param name 
 * @param prefix Prefix is the FIRST subtree this plugin answers — the one a log line names it by. Prefixes is every subtree, and a plugin may own several. Reporting only the first would understate the blast radius of taking this plugin down, which is the question a fleet view exists to answer. 
 * @param prefixes 
 * @param source Source is where the binary came from: \"embedded\", \"path\", \"url\", or \"remote\" for an instance this host did not start. 
 * @param version Version is the artifact's SHA-256 when it was installed from a URL — the only version identifier that cannot drift from the bits actually running, since it IS the bits. Empty for the other sources. 
 * @param addr Addr is the socket or address serving it.
 * @param pid PID is the child process, or 0 when this host did not start it.
 * @param running Running is false after Unload, or after a child exited and no Reload has replaced it. Its routes stay registered and answer 503, so a false here is the difference between \"not deployed\" and \"deployed but down\". 
 * @param disabled Disabled is true when Unload stopped it deliberately, as opposed to it having crashed. Both answer 503, so without this an operator cannot tell a maintenance window from an outage — and would page for the former. 
 * @param since Since is when the CURRENT instance started — it resets on Reload, so it reports the age of what is running, not of the mount. 
 * @param reloads Reloads counts successful swaps since Load. A climbing number on one host and not its peers is the signal that a rollout is uneven. 
 * @param restarts Restarts counts times the supervisor brought this plugin back after it died on its own. Distinct from Reloads, which are deliberate: a nonzero Restarts is a plugin crashing, and a climbing one is a crash loop. 
 * @param usage 
 */


data class PluginPluginStatus (

    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Prefix is the FIRST subtree this plugin answers — the one a log line names it by. Prefixes is every subtree, and a plugin may own several. Reporting only the first would understate the blast radius of taking this plugin down, which is the question a fleet view exists to answer.  */
    @SerializedName("prefix")
    val prefix: kotlin.String? = null,

    @SerializedName("prefixes")
    val prefixes: kotlin.collections.List<kotlin.String>? = null,

    /* Source is where the binary came from: \"embedded\", \"path\", \"url\", or \"remote\" for an instance this host did not start.  */
    @SerializedName("source")
    val source: PluginPluginStatus.Source? = null,

    /* Version is the artifact's SHA-256 when it was installed from a URL — the only version identifier that cannot drift from the bits actually running, since it IS the bits. Empty for the other sources.  */
    @SerializedName("version")
    val version: kotlin.String? = null,

    /* Addr is the socket or address serving it. */
    @SerializedName("addr")
    val addr: kotlin.String? = null,

    /* PID is the child process, or 0 when this host did not start it. */
    @SerializedName("pid")
    val pid: kotlin.Int? = null,

    /* Running is false after Unload, or after a child exited and no Reload has replaced it. Its routes stay registered and answer 503, so a false here is the difference between \"not deployed\" and \"deployed but down\".  */
    @SerializedName("running")
    val running: kotlin.Boolean? = null,

    /* Disabled is true when Unload stopped it deliberately, as opposed to it having crashed. Both answer 503, so without this an operator cannot tell a maintenance window from an outage — and would page for the former.  */
    @SerializedName("disabled")
    val disabled: kotlin.Boolean? = null,

    /* Since is when the CURRENT instance started — it resets on Reload, so it reports the age of what is running, not of the mount.  */
    @SerializedName("since")
    val since: java.time.OffsetDateTime? = null,

    /* Reloads counts successful swaps since Load. A climbing number on one host and not its peers is the signal that a rollout is uneven.  */
    @SerializedName("reloads")
    val reloads: kotlin.Int? = null,

    /* Restarts counts times the supervisor brought this plugin back after it died on its own. Distinct from Reloads, which are deliberate: a nonzero Restarts is a plugin crashing, and a climbing one is a crash loop.  */
    @SerializedName("restarts")
    val restarts: kotlin.Int? = null,

    @SerializedName("usage")
    val usage: PluginPluginUsage? = null

) {

    /**
     * Source is where the binary came from: \"embedded\", \"path\", \"url\", or \"remote\" for an instance this host did not start. 
     *
     * Values: embedded,path,url,remote
     */
    enum class Source(val value: kotlin.String) {
        @SerializedName(value = "embedded") embedded("embedded"),
        @SerializedName(value = "path") path("path"),
        @SerializedName(value = "url") url("url"),
        @SerializedName(value = "remote") remote("remote");
    }

}

