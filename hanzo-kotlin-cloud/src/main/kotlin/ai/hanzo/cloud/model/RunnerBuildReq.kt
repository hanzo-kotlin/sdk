/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.BinarySpec

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param arch Arch is the target architecture for the artifact lane.
 * @param args Args are --build-arg values. They are what lets several images off ONE Dockerfile mean different things — the sandbox classes are three entries differing only by STAGE. Validated at the k8s choke point, with VERSION and REVISION taking precedence: those are receipts the builder derives from the tag and the commit, and a caller that could overwrite them could make an image lie about which commit it is.
 * @param binaries Binaries selects the ARTIFACT lane (artifact.go): build what the repo's hanzo.yml `binaries:` block declares — a Go binary, an npm tarball, a Rust binary — and publish it to hanzoai/s3 instead of pushing an image. It is the same recipe hanzoai/ci reads, sent verbatim, so `image` is meaningless here and must be absent.
 * @param branch Branch is the branch to build when no SHA or Ref is given.
 * @param bucket Bucket mirrors hanzo.yml's `bucket:` — where the artifact lane publishes.
 * @param context Context is the build context path within the repo.
 * @param dockerTarget DockerTarget is the multi-stage build target to stop at.
 * @param dockerfile Dockerfile is the path to build from; empty uses the zero-config frontend.
 * @param image Image is the output image ref to push. Required on the image lane, and it must target a registry namespace the caller's org owns.
 * @param os OS is the target operating system for the artifact lane.
 * @param ref Ref is the git ref to build when no SHA is given.
 * @param repo Repo is the repository clone URL to build. Required on the image lane.
 * @param sha SHA is the commit to pin; it wins over Ref and Branch.
 * @param tag Tag is the publish path segment, so both front doors write ONE index at ONE URL. It defaults to the pinned ref, and must be named explicitly for a branch.
 */


data class RunnerBuildReq (

    /* Arch is the target architecture for the artifact lane. */
    @SerializedName("arch")
    val arch: kotlin.String? = null,

    /* Args are --build-arg values. They are what lets several images off ONE Dockerfile mean different things — the sandbox classes are three entries differing only by STAGE. Validated at the k8s choke point, with VERSION and REVISION taking precedence: those are receipts the builder derives from the tag and the commit, and a caller that could overwrite them could make an image lie about which commit it is. */
    @SerializedName("args")
    val args: kotlin.collections.Map<kotlin.String, kotlin.String>? = null,

    /* Binaries selects the ARTIFACT lane (artifact.go): build what the repo's hanzo.yml `binaries:` block declares — a Go binary, an npm tarball, a Rust binary — and publish it to hanzoai/s3 instead of pushing an image. It is the same recipe hanzoai/ci reads, sent verbatim, so `image` is meaningless here and must be absent. */
    @SerializedName("binaries")
    val binaries: kotlin.collections.List<BinarySpec>? = null,

    /* Branch is the branch to build when no SHA or Ref is given. */
    @SerializedName("branch")
    val branch: kotlin.String? = null,

    /* Bucket mirrors hanzo.yml's `bucket:` — where the artifact lane publishes. */
    @SerializedName("bucket")
    val bucket: kotlin.String? = null,

    /* Context is the build context path within the repo. */
    @SerializedName("context")
    val context: kotlin.String? = null,

    /* DockerTarget is the multi-stage build target to stop at. */
    @SerializedName("dockerTarget")
    val dockerTarget: kotlin.String? = null,

    /* Dockerfile is the path to build from; empty uses the zero-config frontend. */
    @SerializedName("dockerfile")
    val dockerfile: kotlin.String? = null,

    /* Image is the output image ref to push. Required on the image lane, and it must target a registry namespace the caller's org owns. */
    @SerializedName("image")
    val image: kotlin.String? = null,

    /* OS is the target operating system for the artifact lane. */
    @SerializedName("os")
    val os: kotlin.String? = null,

    /* Ref is the git ref to build when no SHA is given. */
    @SerializedName("ref")
    val ref: kotlin.String? = null,

    /* Repo is the repository clone URL to build. Required on the image lane. */
    @SerializedName("repo")
    val repo: kotlin.String? = null,

    /* SHA is the commit to pin; it wins over Ref and Branch. */
    @SerializedName("sha")
    val sha: kotlin.String? = null,

    /* Tag is the publish path segment, so both front doors write ONE index at ONE URL. It defaults to the pinned ref, and must be named explicitly for a branch. */
    @SerializedName("tag")
    val tag: kotlin.String? = null

) {


}

