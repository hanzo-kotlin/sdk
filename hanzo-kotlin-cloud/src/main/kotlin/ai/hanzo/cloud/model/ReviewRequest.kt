/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param glossary Glossary is the terminology the entry was translated under. Its VERSION — the digest of the sorted terms — is part of the entry's identity, so editing a term yields a new entry rather than overwriting the old rendering.
 * @param source Source is the ORIGINAL string this entry translates. Required; part of the entry's identity, so a different source is a different entry.
 * @param state State is the entry's new position on the review ladder: suggested, approved or published. `machine` is engine-only and is refused here — a human may not demote a string back into the churn.
 * @param target Target is the target language tag (BCP-47, e.g. \"es\" or \"pt-BR\"). Required; part of the entry's identity.
 * @param text Text is the reviewed translation to store. A human write always wins over the stored value.
 * @param tier Tier is the engine tier the entry belongs to, quality (the default) or bulk. Part of the entry's identity: the two tiers keep separate renderings.
 */


data class ReviewRequest (

    /* Glossary is the terminology the entry was translated under. Its VERSION — the digest of the sorted terms — is part of the entry's identity, so editing a term yields a new entry rather than overwriting the old rendering. */
    @SerializedName("glossary")
    val glossary: kotlin.collections.Map<kotlin.String, kotlin.String>? = null,

    /* Source is the ORIGINAL string this entry translates. Required; part of the entry's identity, so a different source is a different entry. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* State is the entry's new position on the review ladder: suggested, approved or published. `machine` is engine-only and is refused here — a human may not demote a string back into the churn. */
    @SerializedName("state")
    val state: kotlin.String? = null,

    /* Target is the target language tag (BCP-47, e.g. \"es\" or \"pt-BR\"). Required; part of the entry's identity. */
    @SerializedName("target")
    val target: kotlin.String? = null,

    /* Text is the reviewed translation to store. A human write always wins over the stored value. */
    @SerializedName("text")
    val text: kotlin.String? = null,

    /* Tier is the engine tier the entry belongs to, quality (the default) or bulk. Part of the entry's identity: the two tiers keep separate renderings. */
    @SerializedName("tier")
    val tier: kotlin.String? = null

) {


}

