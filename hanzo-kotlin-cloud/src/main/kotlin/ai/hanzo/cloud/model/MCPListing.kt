/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.MCPPackage
import ai.hanzo.cloud.model.MCPRemote

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param description Description is the publisher's one-line summary.
 * @param featured Featured puts the listing on the front of the shelf. Curation.
 * @param hidden Hidden keeps the listing out of the org-visible catalog. Curation: a sync never changes it. Only a SuperAdmin sets it, and only a SuperAdmin sees a hidden entry listed.
 * @param id ID addresses the listing in a URL. It is the reverse-DNS NAME with its one slash written as an underscore — reversible, because a namespace never contains an underscore — so the id is readable and stable rather than a hash that means nothing to whoever reads a link.
 * @param logo Logo is the brand mark to render for the listing — the publisher's icon when the entry carries one, or the one an admin set. Curation.
 * @param name Name is the publisher's reverse-DNS name, e.g. \"com.stripe/mcp\".
 * @param official Official is whether this is the vendor's OWN server rather than someone else's copy of it. Derived on every sync (see isOfficial) until a SuperAdmin sets it explicitly, after which the admin's answer stands.
 * @param packages Packages are the runnable package forms — npm, pypi, oci — each with the runtime that launches it and the transport it then speaks.
 * @param registry Registry is the upstream this row was synced from.
 * @param remotes Remotes are the hosted endpoints the publisher serves the server at.
 * @param repo Repo is the source repository URL, when the entry names one.
 * @param site Site is the project's homepage, when the entry names one.
 * @param synced Synced is when this row was last confirmed against upstream, Unix seconds.
 * @param title Title is the human-readable display name, when the entry carries one.
 * @param transports Transports are the distinct transports this server can be reached over, sorted: some of \"stdio\", \"streamable-http\", \"sse\". A listing with \"streamable-http\" is one an org can enable here and now; a listing that is only \"stdio\" needs a process to run it.
 * @param vendor Vendor is the namespace half of Name — the publisher, e.g. \"com.stripe\".
 * @param version Version is the published version of this listing.
 */


data class MCPListing (

    /* Description is the publisher's one-line summary. */
    @SerializedName("description")
    val description: kotlin.String? = null,

    /* Featured puts the listing on the front of the shelf. Curation. */
    @SerializedName("featured")
    val featured: kotlin.Boolean? = null,

    /* Hidden keeps the listing out of the org-visible catalog. Curation: a sync never changes it. Only a SuperAdmin sets it, and only a SuperAdmin sees a hidden entry listed. */
    @SerializedName("hidden")
    val hidden: kotlin.Boolean? = null,

    /* ID addresses the listing in a URL. It is the reverse-DNS NAME with its one slash written as an underscore — reversible, because a namespace never contains an underscore — so the id is readable and stable rather than a hash that means nothing to whoever reads a link. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Logo is the brand mark to render for the listing — the publisher's icon when the entry carries one, or the one an admin set. Curation. */
    @SerializedName("logo")
    val logo: kotlin.String? = null,

    /* Name is the publisher's reverse-DNS name, e.g. \"com.stripe/mcp\". */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Official is whether this is the vendor's OWN server rather than someone else's copy of it. Derived on every sync (see isOfficial) until a SuperAdmin sets it explicitly, after which the admin's answer stands. */
    @SerializedName("official")
    val official: kotlin.Boolean? = null,

    /* Packages are the runnable package forms — npm, pypi, oci — each with the runtime that launches it and the transport it then speaks. */
    @SerializedName("packages")
    val packages: kotlin.collections.List<MCPPackage>? = null,

    /* Registry is the upstream this row was synced from. */
    @SerializedName("registry")
    val registry: kotlin.String? = null,

    /* Remotes are the hosted endpoints the publisher serves the server at. */
    @SerializedName("remotes")
    val remotes: kotlin.collections.List<MCPRemote>? = null,

    /* Repo is the source repository URL, when the entry names one. */
    @SerializedName("repo")
    val repo: kotlin.String? = null,

    /* Site is the project's homepage, when the entry names one. */
    @SerializedName("site")
    val site: kotlin.String? = null,

    /* Synced is when this row was last confirmed against upstream, Unix seconds. */
    @SerializedName("synced")
    val synced: kotlin.Int? = null,

    /* Title is the human-readable display name, when the entry carries one. */
    @SerializedName("title")
    val title: kotlin.String? = null,

    /* Transports are the distinct transports this server can be reached over, sorted: some of \"stdio\", \"streamable-http\", \"sse\". A listing with \"streamable-http\" is one an org can enable here and now; a listing that is only \"stdio\" needs a process to run it. */
    @SerializedName("transports")
    val transports: kotlin.collections.List<kotlin.String>? = null,

    /* Vendor is the namespace half of Name — the publisher, e.g. \"com.stripe\". */
    @SerializedName("vendor")
    val vendor: kotlin.String? = null,

    /* Version is the published version of this listing. */
    @SerializedName("version")
    val version: kotlin.String? = null

) {


}

