/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param balanceCents BalanceCents is the org's balance AFTER this payment, read back from the same key just credited so it matches what the balance endpoint reports.
 * @param id ID is the ledger transaction id for the credit. It is what getPayment reads back, and the customer-visible receipt for the money.
 * @param processorRef ProcessorRef is the payment processor's own reference for the charge (Square's payment id). It is the field that proves money actually moved at the gateway rather than only in our ledger — the thing to quote when reconciling against a processor dashboard.
 * @param status Status is \"ok\" on a settled charge. A charge that did not settle is an error with the processor's reason, never a status field to inspect.
 * @param test Test reports which bucket this credited: true is a SANDBOX charge crediting the test balance, false is live money. It is always stated so a receipt can never be mistaken for the other kind.
 */


data class PaymentOut (

    /* BalanceCents is the org's balance AFTER this payment, read back from the same key just credited so it matches what the balance endpoint reports. */
    @SerializedName("balanceCents")
    val balanceCents: kotlin.Int? = null,

    /* ID is the ledger transaction id for the credit. It is what getPayment reads back, and the customer-visible receipt for the money. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* ProcessorRef is the payment processor's own reference for the charge (Square's payment id). It is the field that proves money actually moved at the gateway rather than only in our ledger — the thing to quote when reconciling against a processor dashboard. */
    @SerializedName("processorRef")
    val processorRef: kotlin.String? = null,

    /* Status is \"ok\" on a settled charge. A charge that did not settle is an error with the processor's reason, never a status field to inspect. */
    @SerializedName("status")
    val status: kotlin.String? = null,

    /* Test reports which bucket this credited: true is a SANDBOX charge crediting the test balance, false is live money. It is always stated so a receipt can never be mistaken for the other kind. */
    @SerializedName("test")
    val test: kotlin.Boolean? = null

) {


}

