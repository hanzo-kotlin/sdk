/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.EnvVarJSON

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param env Env is the run's environment. Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`; a variable marked `secret: true` is sealed into KMS.
 * @param gpu GPU is how many GPUs the run asks for; a negative value is 400.
 * @param image Image is the container image to run. Required.
 * @param maxScale MaxScale above the floor declares an autoscaling ceiling; 0 means no autoscaler at all — a fixed run at the floor.
 * @param minScale MinScale is the replica floor, clamped to the deployment's limit.
 * @param name Name is the run's name, and the slug is derived from it. Required, and it must resolve to `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`. Re-running the same name updates that run in place.
 * @param port Port is the container port the run listens on.
 * @param runtime Runtime is accepted for the client contract and echoed nowhere: the image IS the runtime unit.
 * @param shape Shape is a compute size label, echoed back; sizing is the operator's default. Defaults to \"auto\".
 */


data class RunReq (

    /* Env is the run's environment. Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`; a variable marked `secret: true` is sealed into KMS. */
    @SerializedName("env")
    val env: kotlin.collections.List<EnvVarJSON>? = null,

    /* GPU is how many GPUs the run asks for; a negative value is 400. */
    @SerializedName("gpu")
    val gpu: kotlin.Int? = null,

    /* Image is the container image to run. Required. */
    @SerializedName("image")
    val image: kotlin.String? = null,

    /* MaxScale above the floor declares an autoscaling ceiling; 0 means no autoscaler at all — a fixed run at the floor. */
    @SerializedName("maxScale")
    val maxScale: kotlin.Int? = null,

    /* MinScale is the replica floor, clamped to the deployment's limit. */
    @SerializedName("minScale")
    val minScale: kotlin.Int? = null,

    /* Name is the run's name, and the slug is derived from it. Required, and it must resolve to `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`. Re-running the same name updates that run in place. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Port is the container port the run listens on. */
    @SerializedName("port")
    val port: kotlin.Int? = null,

    /* Runtime is accepted for the client contract and echoed nowhere: the image IS the runtime unit. */
    @SerializedName("runtime")
    val runtime: kotlin.String? = null,

    /* Shape is a compute size label, echoed back; sizing is the operator's default. Defaults to \"auto\". */
    @SerializedName("shape")
    val shape: kotlin.String? = null

) {


}

