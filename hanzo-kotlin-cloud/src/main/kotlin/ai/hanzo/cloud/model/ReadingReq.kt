/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param account Account is the provider-side account the sample belongs to.
 * @param cachedInputTokens CachedInputTokens is the window's cached-prompt-token count.
 * @param confidence Confidence says how real the counters are, as the meter graded itself.
 * @param costCents CostCents is the window's spend in cents, as the provider's meter states it.
 * @param costLimitCents CostLimitCents is the window's spend cap in cents, when the meter knows one.
 * @param currency Currency is the ISO currency the cost fields are stated in.
 * @param inputTokens InputTokens is the window's prompt-token count.
 * @param kind Kind is subscription or apikey; anything else is refused.
 * @param lane Lane names the meter's own lane label for this measurement.
 * @param machine Machine is the machine the collector observed the account on. Required.
 * @param outputTokens OutputTokens is the window's completion-token count.
 * @param plan Plan is the provider plan label the account is on.
 * @param provider Provider is the AI provider whose meter reported this sample. Required.
 * @param requests Requests is the window's request count.
 * @param resetsAt ResetsAt is when the window resets, RFC 3339, bounded.
 * @param synthetic Synthetic marks a sample the collector derived rather than observed.
 * @param totalTokens TotalTokens is the window's total token count.
 * @param usedPct UsedPct is how much of the window's allowance is consumed, clamped 0..100.
 * @param window Window is the window class, one of 6h, day, week, month; anything else is refused rather than silently reclassified.
 * @param windowMinutes WindowMinutes is the window's length as the meter reported it.
 * @param windowStart WindowStart is when the measured window opened, RFC 3339, bounded to a sane interval around now.
 */


data class ReadingReq (

    /* Account is the provider-side account the sample belongs to. */
    @SerializedName("account")
    val account: kotlin.String? = null,

    /* CachedInputTokens is the window's cached-prompt-token count. */
    @SerializedName("cachedInputTokens")
    val cachedInputTokens: kotlin.Int? = null,

    /* Confidence says how real the counters are, as the meter graded itself. */
    @SerializedName("confidence")
    val confidence: kotlin.String? = null,

    /* CostCents is the window's spend in cents, as the provider's meter states it. */
    @SerializedName("costCents")
    val costCents: kotlin.Int? = null,

    /* CostLimitCents is the window's spend cap in cents, when the meter knows one. */
    @SerializedName("costLimitCents")
    val costLimitCents: kotlin.Int? = null,

    /* Currency is the ISO currency the cost fields are stated in. */
    @SerializedName("currency")
    val currency: kotlin.String? = null,

    /* InputTokens is the window's prompt-token count. */
    @SerializedName("inputTokens")
    val inputTokens: kotlin.Int? = null,

    /* Kind is subscription or apikey; anything else is refused. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Lane names the meter's own lane label for this measurement. */
    @SerializedName("lane")
    val lane: kotlin.String? = null,

    /* Machine is the machine the collector observed the account on. Required. */
    @SerializedName("machine")
    val machine: kotlin.String? = null,

    /* OutputTokens is the window's completion-token count. */
    @SerializedName("outputTokens")
    val outputTokens: kotlin.Int? = null,

    /* Plan is the provider plan label the account is on. */
    @SerializedName("plan")
    val plan: kotlin.String? = null,

    /* Provider is the AI provider whose meter reported this sample. Required. */
    @SerializedName("provider")
    val provider: kotlin.String? = null,

    /* Requests is the window's request count. */
    @SerializedName("requests")
    val requests: kotlin.Int? = null,

    /* ResetsAt is when the window resets, RFC 3339, bounded. */
    @SerializedName("resetsAt")
    val resetsAt: kotlin.String? = null,

    /* Synthetic marks a sample the collector derived rather than observed. */
    @SerializedName("synthetic")
    val synthetic: kotlin.Boolean? = null,

    /* TotalTokens is the window's total token count. */
    @SerializedName("totalTokens")
    val totalTokens: kotlin.Int? = null,

    /* UsedPct is how much of the window's allowance is consumed, clamped 0..100. */
    @SerializedName("usedPct")
    val usedPct: java.math.BigDecimal? = null,

    /* Window is the window class, one of 6h, day, week, month; anything else is refused rather than silently reclassified. */
    @SerializedName("window")
    val window: kotlin.String? = null,

    /* WindowMinutes is the window's length as the meter reported it. */
    @SerializedName("windowMinutes")
    val windowMinutes: kotlin.Int? = null,

    /* WindowStart is when the measured window opened, RFC 3339, bounded to a sane interval around now. */
    @SerializedName("windowStart")
    val windowStart: kotlin.String? = null

) {


}

