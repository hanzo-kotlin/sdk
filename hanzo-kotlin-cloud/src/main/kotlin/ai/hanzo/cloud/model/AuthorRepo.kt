/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param badgeMarkdown BadgeMarkdown is the ready-to-paste README snippet, DERIVED for each response from this deployment's badge host and never stored: a \"Deploy on Hanzo\" image linking to the one-click import of this repository. Re-hosting the builder changes every badge without touching a row.
 * @param createdAt CreatedAt is unix seconds when the claim was first recorded. It equals verifiedAt on the first proof and then stays put while verifiedAt moves, so the pair reads as \"claimed since / last proven\".
 * @param method Method is HOW ownership was proven: \"oauth\" — an IAM-linked forge token showed admin or push on the repository; \"file\" — a hanzo.json on the default branch carried this author's verify code; or \"maintainer\" — the repository sits in a first-party namespace, where ownership is intrinsic and the treasury author holds it with no proof step. Omitted on a row written before the method was recorded.
 * @param repoUrl RepoURL is the claim key in canonical form — lowercased \"host/owner/name\", no scheme, no .git, host ∈ {github.com, gitlab.com}. A deploy's source repo is normalized through the same function before attribution, so the two sides can never miss on a cosmetic difference. UNIQUE across every author: first proven claim wins.
 * @param verified Verified reports that ownership was proven. Only a proven claim is ever written, so it is true on every row this surface returns; the deploy path re-reads it regardless, because an unverified claim attributes nothing.
 * @param verifiedAt VerifiedAt is unix seconds of the most recent successful proof. Re-verifying refreshes it, and the method beside it, in place.
 */


data class AuthorRepo (

    /* BadgeMarkdown is the ready-to-paste README snippet, DERIVED for each response from this deployment's badge host and never stored: a \"Deploy on Hanzo\" image linking to the one-click import of this repository. Re-hosting the builder changes every badge without touching a row. */
    @SerializedName("badgeMarkdown")
    val badgeMarkdown: kotlin.String? = null,

    /* CreatedAt is unix seconds when the claim was first recorded. It equals verifiedAt on the first proof and then stays put while verifiedAt moves, so the pair reads as \"claimed since / last proven\". */
    @SerializedName("createdAt")
    val createdAt: kotlin.Int? = null,

    /* Method is HOW ownership was proven: \"oauth\" — an IAM-linked forge token showed admin or push on the repository; \"file\" — a hanzo.json on the default branch carried this author's verify code; or \"maintainer\" — the repository sits in a first-party namespace, where ownership is intrinsic and the treasury author holds it with no proof step. Omitted on a row written before the method was recorded. */
    @SerializedName("method")
    val method: kotlin.String? = null,

    /* RepoURL is the claim key in canonical form — lowercased \"host/owner/name\", no scheme, no .git, host ∈ {github.com, gitlab.com}. A deploy's source repo is normalized through the same function before attribution, so the two sides can never miss on a cosmetic difference. UNIQUE across every author: first proven claim wins. */
    @SerializedName("repoUrl")
    val repoUrl: kotlin.String? = null,

    /* Verified reports that ownership was proven. Only a proven claim is ever written, so it is true on every row this surface returns; the deploy path re-reads it regardless, because an unverified claim attributes nothing. */
    @SerializedName("verified")
    val verified: kotlin.Boolean? = null,

    /* VerifiedAt is unix seconds of the most recent successful proof. Re-verifying refreshes it, and the method beside it, in place. */
    @SerializedName("verifiedAt")
    val verifiedAt: kotlin.Int? = null

) {


}

