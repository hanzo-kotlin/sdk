/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param id ID is the cart to amend, from the path.
 * @param product Product names the catalog product to set, by its id or its URL slug. Give this or Variant, never both; a request naming neither is refused.
 * @param quantity Quantity is how many of that item the cart should hold AFTER this call — it is the resulting count, not a delta, so sending 3 twice leaves 3 and not 6. ZERO REMOVES the line, which is the only way to take an item out.
 * @param variant Variant names the specific sellable variant to set, by its id or its SKU. Prefer it over Product for anything sold in sizes, colours or tiers — the price and the stock are the variant's, not the product's.
 */


data class CartItemSet (

    /* ID is the cart to amend, from the path. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Product names the catalog product to set, by its id or its URL slug. Give this or Variant, never both; a request naming neither is refused. */
    @SerializedName("product")
    val product: kotlin.String? = null,

    /* Quantity is how many of that item the cart should hold AFTER this call — it is the resulting count, not a delta, so sending 3 twice leaves 3 and not 6. ZERO REMOVES the line, which is the only way to take an item out. */
    @SerializedName("quantity")
    val quantity: kotlin.Int? = null,

    /* Variant names the specific sellable variant to set, by its id or its SKU. Prefer it over Product for anything sold in sizes, colours or tiers — the price and the stock are the variant's, not the product's. */
    @SerializedName("variant")
    val variant: kotlin.String? = null

) {


}

