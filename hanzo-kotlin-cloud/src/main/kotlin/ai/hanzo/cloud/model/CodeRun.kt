/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.CodeFile

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param code Code is the WHOLE program, not a fragment: it is written to a single file and that file is what runs, so a compiled language needs its entry point and an interpreted one runs top to bottom.
 * @param lang Lang selects the toolchain, and with it the filename the code is written to and the line that runs it: py, js, ts, bash, r, php, go, rs, c, cpp, java, d, f90. Anything else is refused rather than guessed at — a run in the wrong language fails somewhere deep in a compiler, which reads as an outage.
 * @param args Args become the PROGRAM's argv, never the compiler's. For the compiled languages the toolchain builds first and these are passed to the binary it produced.
 * @param files Files are inputs the host already put in some session. Each names the session its bytes live in, which is usually — and ideally — the session this run wants.
 * @param runtimeSessionHint RuntimeSessionHint is the stateful-session hint. It is carried so a client that sends it is not silently misread, and it selects nothing here: every session in this implementation is already a warm sandbox, so there is no second kind of runtime for a hint to choose between.
 * @param sessionId SessionID continues an EXISTING sandbox, which is what makes runs stateful: the same filesystem, so one run's output file is the next run's input. Empty leases a fresh sandbox and the id it got comes back on the result.
 * @param userId UserID attributes the run inside the caller's org. It is a label, never a tenant: the org is resolved from the validated principal and a value here cannot widen what the run may reach.
 */


data class CodeRun (

    /* Code is the WHOLE program, not a fragment: it is written to a single file and that file is what runs, so a compiled language needs its entry point and an interpreted one runs top to bottom. */
    @SerializedName("code")
    val code: kotlin.String,

    /* Lang selects the toolchain, and with it the filename the code is written to and the line that runs it: py, js, ts, bash, r, php, go, rs, c, cpp, java, d, f90. Anything else is refused rather than guessed at — a run in the wrong language fails somewhere deep in a compiler, which reads as an outage. */
    @SerializedName("lang")
    val lang: kotlin.String,

    /* Args become the PROGRAM's argv, never the compiler's. For the compiled languages the toolchain builds first and these are passed to the binary it produced. */
    @SerializedName("args")
    val args: kotlin.collections.List<kotlin.String>? = null,

    /* Files are inputs the host already put in some session. Each names the session its bytes live in, which is usually — and ideally — the session this run wants. */
    @SerializedName("files")
    val files: kotlin.collections.List<CodeFile>? = null,

    /* RuntimeSessionHint is the stateful-session hint. It is carried so a client that sends it is not silently misread, and it selects nothing here: every session in this implementation is already a warm sandbox, so there is no second kind of runtime for a hint to choose between. */
    @SerializedName("runtime_session_hint")
    val runtimeSessionHint: kotlin.String? = null,

    /* SessionID continues an EXISTING sandbox, which is what makes runs stateful: the same filesystem, so one run's output file is the next run's input. Empty leases a fresh sandbox and the id it got comes back on the result. */
    @SerializedName("session_id")
    val sessionId: kotlin.String? = null,

    /* UserID attributes the run inside the caller's org. It is a label, never a tenant: the org is resolved from the validated principal and a value here cannot widen what the run may reach. */
    @SerializedName("user_id")
    val userId: kotlin.String? = null

) {


}

