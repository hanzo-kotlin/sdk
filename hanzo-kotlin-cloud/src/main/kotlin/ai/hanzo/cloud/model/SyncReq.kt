/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.EndpointReq

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param actor Actor is the identity the sync writes as, used as the loop guard so its own writes do not re-trigger it. Defaults to the deployment's GIT_SYNC_ACTOR.
 * @param direction Direction is both (the default), pull, push or off.
 * @param kind Kind is what is being synced. Only \"git\" today, which is also the default.
 * @param run Run reconciles once immediately after the upsert, in the background.
 * @param source Source is the upstream end. Required.
 * @param target Target is the downstream end. Optional for git: a native repository named after the source is derived when it is omitted.
 * @param trigger Trigger is what starts a reconcile: webhook (the default), poll or manual.
 */


data class SyncReq (

    /* Actor is the identity the sync writes as, used as the loop guard so its own writes do not re-trigger it. Defaults to the deployment's GIT_SYNC_ACTOR. */
    @SerializedName("actor")
    val actor: kotlin.String? = null,

    /* Direction is both (the default), pull, push or off. */
    @SerializedName("direction")
    val direction: kotlin.String? = null,

    /* Kind is what is being synced. Only \"git\" today, which is also the default. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Run reconciles once immediately after the upsert, in the background. */
    @SerializedName("run")
    val run: kotlin.Boolean? = null,

    /* Source is the upstream end. Required. */
    @SerializedName("source")
    val source: EndpointReq? = null,

    /* Target is the downstream end. Optional for git: a native repository named after the source is derived when it is omitted. */
    @SerializedName("target")
    val target: EndpointReq? = null,

    /* Trigger is what starts a reconcile: webhook (the default), poll or manual. */
    @SerializedName("trigger")
    val trigger: kotlin.String? = null

) {


}

