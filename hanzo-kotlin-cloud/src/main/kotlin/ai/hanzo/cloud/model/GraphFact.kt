/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param at At is when the thing was so, RFC 3339. Required, and refused when it sits more than five minutes ahead of the server clock — an assertion dated further out would never mature and would skew every read until it did.
 * @param confidence Confidence in [0,1]. A tie-breaker within the order, never a substitute for it. Absent is 0, the weakest an assertion can be.
 * @param entity Entity is the thing being described, in the organization's own namespace. It is not created: an entity exists because something was asserted about it. Required, 512 bytes at most.
 * @param evidence Evidence points at the record this claim came from, 512 bytes at most. An assertion without one is admitted and carries no defence.
 * @param names Names says the value is an entity. A walk reads only the edges, so this is a declaration and never a guess about the value's shape.
 * @param relation Relation is what is being asserted — `depends`, `owner`, `same`, `title`. It is open: this plane holds no vocabulary of its own. Required, 128 bytes at most.
 * @param seen Seen is when this assertion became knowable, RFC 3339. Defaults to At and may not precede it. It is provenance and it decides nothing: the instant every read uses is derived as the later of Seen and the server's own clock.
 * @param source Source names who asserted. Required, because an assertion nobody is named for cannot be weighed against one that is. Open text: this plane ranks no source above another.
 * @param `value` Value is what the relation points at. When Names is true it is another entity's key and the assertion is an EDGE; otherwise it is a scalar and the assertion is a property. 2048 bytes at most, or 512 when it names an entity.
 */


data class GraphFact (

    /* At is when the thing was so, RFC 3339. Required, and refused when it sits more than five minutes ahead of the server clock — an assertion dated further out would never mature and would skew every read until it did. */
    @SerializedName("at")
    val at: kotlin.String? = null,

    /* Confidence in [0,1]. A tie-breaker within the order, never a substitute for it. Absent is 0, the weakest an assertion can be. */
    @SerializedName("confidence")
    val confidence: java.math.BigDecimal? = null,

    /* Entity is the thing being described, in the organization's own namespace. It is not created: an entity exists because something was asserted about it. Required, 512 bytes at most. */
    @SerializedName("entity")
    val entity: kotlin.String? = null,

    /* Evidence points at the record this claim came from, 512 bytes at most. An assertion without one is admitted and carries no defence. */
    @SerializedName("evidence")
    val evidence: kotlin.String? = null,

    /* Names says the value is an entity. A walk reads only the edges, so this is a declaration and never a guess about the value's shape. */
    @SerializedName("names")
    val names: kotlin.Boolean? = null,

    /* Relation is what is being asserted — `depends`, `owner`, `same`, `title`. It is open: this plane holds no vocabulary of its own. Required, 128 bytes at most. */
    @SerializedName("relation")
    val relation: kotlin.String? = null,

    /* Seen is when this assertion became knowable, RFC 3339. Defaults to At and may not precede it. It is provenance and it decides nothing: the instant every read uses is derived as the later of Seen and the server's own clock. */
    @SerializedName("seen")
    val seen: kotlin.String? = null,

    /* Source names who asserted. Required, because an assertion nobody is named for cannot be weighed against one that is. Open text: this plane ranks no source above another. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Value is what the relation points at. When Names is true it is another entity's key and the assertion is an EDGE; otherwise it is a scalar and the assertion is a property. 2048 bytes at most, or 512 when it names an entity. */
    @SerializedName("value")
    val `value`: kotlin.String? = null

) {


}

