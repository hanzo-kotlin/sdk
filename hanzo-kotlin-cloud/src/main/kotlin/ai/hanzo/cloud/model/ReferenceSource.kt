/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param asOf AsOf is when this publisher was current, RFC 3339.
 * @param basis Basis is the KIND of permission this publisher's data reaches you under: licence (an explicit grant), registry (the registry of record publishing for anyone to consult), operator (an operator's own machine-readable statement about its own network, published for third parties to filter by — not a licence, and not claimed as one), own (computed here), or none (nothing reaches you: the membership is held by the component that screens against it). It is on the wire so the licence position is an audit you can run.
 * @param propertyKeys Keys is how many members this publisher contributed.
 * @param origin Origin is exactly where it was taken from, so it can be taken again.
 * @param refusal Refusal is why this publisher's last take failed, if it did. The set keeps its previous version of this source and ages out visibly rather than silently shrinking.
 * @param source Source is the publisher.
 * @param terms Terms is the CITATION that basis points at — the licence identifier, the registry, or the operator publication. A source with no stated terms is not in the catalog.
 * @param version Version is the content digest of what this publisher last supplied. Two refreshes that agree on it took the same data.
 */


data class ReferenceSource (

    /* AsOf is when this publisher was current, RFC 3339. */
    @SerializedName("asOf")
    val asOf: kotlin.String? = null,

    /* Basis is the KIND of permission this publisher's data reaches you under: licence (an explicit grant), registry (the registry of record publishing for anyone to consult), operator (an operator's own machine-readable statement about its own network, published for third parties to filter by — not a licence, and not claimed as one), own (computed here), or none (nothing reaches you: the membership is held by the component that screens against it). It is on the wire so the licence position is an audit you can run. */
    @SerializedName("basis")
    val basis: kotlin.String? = null,

    /* Keys is how many members this publisher contributed. */
    @SerializedName("keys")
    val propertyKeys: kotlin.Int? = null,

    /* Origin is exactly where it was taken from, so it can be taken again. */
    @SerializedName("origin")
    val origin: kotlin.String? = null,

    /* Refusal is why this publisher's last take failed, if it did. The set keeps its previous version of this source and ages out visibly rather than silently shrinking. */
    @SerializedName("refusal")
    val refusal: kotlin.String? = null,

    /* Source is the publisher. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Terms is the CITATION that basis points at — the licence identifier, the registry, or the operator publication. A source with no stated terms is not in the catalog. */
    @SerializedName("terms")
    val terms: kotlin.String? = null,

    /* Version is the content digest of what this publisher last supplied. Two refreshes that agree on it took the same data. */
    @SerializedName("version")
    val version: kotlin.String? = null

) {


}

