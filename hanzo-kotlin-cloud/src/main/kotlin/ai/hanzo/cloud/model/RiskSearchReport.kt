/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.RiskModelValue
import ai.hanzo.cloud.model.RiskTrial

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param done Done is false while the run is still going; the trials below are then the ones finished so far.
 * @param ended Ended is when it finished, RFC 3339. Absent while it is still going.
 * @param events Events is how much of this organisation's history was replayed.
 * @param fitted Fitted is the winning shape FITTED over your own history and published as one of your organisation's own model values. Name its address on PUT /v1/risk/state/model and the winning shape becomes the model you are running.  It is why this op answers something you can act on. A trial keeps counts and not the model that produced them, so a report without this named a shape nobody could install — and the adoption path refused a shape change besides. Fitting the winner once is a sixty-fifth pass over the same history; keeping all sixty-four fitted models resident instead would cost a measured 21 MiB per run for sixty-three shapes nobody adopts.  Two things about it are worth knowing before you adopt it. Its realised rate can differ from the winner's above, because the ranking measures every candidate under one fixed reference geometry so the comparison is a comparison, while this is fitted under YOUR geometry — the one an outsider cannot predict. And it has learned the window this search replayed and nothing older, so adopting it trades history for fit.
 * @param gap Gap says why the winning shape could not be fitted into an adoptable value, when it could not. It is separate from Refusal because they are different facts: a refusal means the ranking below proves nothing, a gap means the ranking stands and only the value is missing.
 * @param id ID is the run.
 * @param refusal Refusal says why the run proves nothing, when it does. An empty history is REFUSED rather than reported as zero alerts: \"no alerts\" is exactly what a quiet model looks like, and choosing a shape on the strength of an empty replay is the failure a sandbox exists to prevent.
 * @param started Started is when the run was accepted, RFC 3339.
 * @param trials Trials is every shape tried, best first.
 * @param winner Winner is the best-fitting shape, absent when nothing fit.
 */


data class RiskSearchReport (

    /* Done is false while the run is still going; the trials below are then the ones finished so far. */
    @SerializedName("done")
    val done: kotlin.Boolean? = null,

    /* Ended is when it finished, RFC 3339. Absent while it is still going. */
    @SerializedName("ended")
    val ended: kotlin.String? = null,

    /* Events is how much of this organisation's history was replayed. */
    @SerializedName("events")
    val events: kotlin.Int? = null,

    /* Fitted is the winning shape FITTED over your own history and published as one of your organisation's own model values. Name its address on PUT /v1/risk/state/model and the winning shape becomes the model you are running.  It is why this op answers something you can act on. A trial keeps counts and not the model that produced them, so a report without this named a shape nobody could install — and the adoption path refused a shape change besides. Fitting the winner once is a sixty-fifth pass over the same history; keeping all sixty-four fitted models resident instead would cost a measured 21 MiB per run for sixty-three shapes nobody adopts.  Two things about it are worth knowing before you adopt it. Its realised rate can differ from the winner's above, because the ranking measures every candidate under one fixed reference geometry so the comparison is a comparison, while this is fitted under YOUR geometry — the one an outsider cannot predict. And it has learned the window this search replayed and nothing older, so adopting it trades history for fit. */
    @SerializedName("fitted")
    val fitted: RiskModelValue? = null,

    /* Gap says why the winning shape could not be fitted into an adoptable value, when it could not. It is separate from Refusal because they are different facts: a refusal means the ranking below proves nothing, a gap means the ranking stands and only the value is missing. */
    @SerializedName("gap")
    val gap: kotlin.String? = null,

    /* ID is the run. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Refusal says why the run proves nothing, when it does. An empty history is REFUSED rather than reported as zero alerts: \"no alerts\" is exactly what a quiet model looks like, and choosing a shape on the strength of an empty replay is the failure a sandbox exists to prevent. */
    @SerializedName("refusal")
    val refusal: kotlin.String? = null,

    /* Started is when the run was accepted, RFC 3339. */
    @SerializedName("started")
    val started: kotlin.String? = null,

    /* Trials is every shape tried, best first. */
    @SerializedName("trials")
    val trials: kotlin.collections.List<RiskTrial>? = null,

    /* Winner is the best-fitting shape, absent when nothing fit. */
    @SerializedName("winner")
    val winner: RiskTrial? = null

) {


}

