/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.RiskBand
import ai.hanzo.cloud.model.RiskModelFeature
import ai.hanzo.cloud.model.RiskOrgFeature

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param gap Gap says why a lens could not be measured, when that is the case. Each reason names its own lens, because \"the surface is unreadable\" and \"the network baseline is unreadable\" are different facts.
 * @param model Model is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves and the published standard that asks for it. It is the same for every organisation, because it is the model's shape.
 * @param network Network is the published cross-organisation baseline over the same window, so the surface above has something to be read AGAINST. It is the same for every caller and it names nobody.  It carries no tenant and cannot be made to: the table it reads has no org column, every figure is a quantile over at least kAnonOrgs organisations weighted one vote each, and a band that does not meet that floor is dropped on the way out.
 * @param surface Surface is what this organisation's own event surface carries, per dimension, measured over the window. A dimension present in no bucket is blind here — the model reads its neutral value and a reviewer has to be able to see that.
 * @param tenant Tenant is whose surface was measured.
 */


data class RiskCatalog (

    /* Gap says why a lens could not be measured, when that is the case. Each reason names its own lens, because \"the surface is unreadable\" and \"the network baseline is unreadable\" are different facts. */
    @SerializedName("gap")
    val gap: kotlin.String? = null,

    /* Model is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves and the published standard that asks for it. It is the same for every organisation, because it is the model's shape. */
    @SerializedName("model")
    val model: kotlin.collections.List<RiskModelFeature>? = null,

    /* Network is the published cross-organisation baseline over the same window, so the surface above has something to be read AGAINST. It is the same for every caller and it names nobody.  It carries no tenant and cannot be made to: the table it reads has no org column, every figure is a quantile over at least kAnonOrgs organisations weighted one vote each, and a band that does not meet that floor is dropped on the way out. */
    @SerializedName("network")
    val network: kotlin.collections.List<RiskBand>? = null,

    /* Surface is what this organisation's own event surface carries, per dimension, measured over the window. A dimension present in no bucket is blind here — the model reads its neutral value and a reviewer has to be able to see that. */
    @SerializedName("surface")
    val surface: kotlin.collections.List<RiskOrgFeature>? = null,

    /* Tenant is whose surface was measured. */
    @SerializedName("tenant")
    val tenant: kotlin.String? = null

) {


}

