/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param authHeader AuthHeader is the request header the credential is injected into, e.g. \"Authorization\". Empty means the server needs no credential.
 * @param listing Listing enables a CATALOG entry instead — the id from GET /v1/tools/catalog. The endpoint is the listing's own streamable-http remote, so a listing that only ships a stdio package is refused: there is nothing to reach yet.
 * @param name Name labels the server for the org. Required with URL; with Listing it defaults to the listing's own title.
 * @param secret Secret is the credential VALUE. It is sealed into KMS under a per-org ref and never stored in SQLite, never listed, and never returned.
 * @param url URL is the server's JSON-RPC endpoint. It must be an http(s) URL naming a PUBLIC host: loopback, link-local, private and cloud-metadata addresses are refused here and again when the dialer connects.
 */


data class CloudCreateServerReq (

    /* AuthHeader is the request header the credential is injected into, e.g. \"Authorization\". Empty means the server needs no credential. */
    @SerializedName("authHeader")
    val authHeader: kotlin.String? = null,

    /* Listing enables a CATALOG entry instead — the id from GET /v1/tools/catalog. The endpoint is the listing's own streamable-http remote, so a listing that only ships a stdio package is refused: there is nothing to reach yet. */
    @SerializedName("listing")
    val listing: kotlin.String? = null,

    /* Name labels the server for the org. Required with URL; with Listing it defaults to the listing's own title. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Secret is the credential VALUE. It is sealed into KMS under a per-org ref and never stored in SQLite, never listed, and never returned. */
    @SerializedName("secret")
    val secret: kotlin.String? = null,

    /* URL is the server's JSON-RPC endpoint. It must be an http(s) URL naming a PUBLIC host: loopback, link-local, private and cloud-metadata addresses are refused here and again when the dialer connects. */
    @SerializedName("url")
    val url: kotlin.String? = null

) {


}

