/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param account Account is the provider account this channel runs under: an ad-account, a page or a mailing-list id. An executor may replace it at launch with the account it actually used.
 * @param detail Detail is the last outcome in one secret-free line — the failure reason, or what the executor reported. Absent when there is nothing to explain.
 * @param externalId ExternalID is the provider-side id of the running execution, recorded by the orchestrator at launch and handed back verbatim to read spend or to pause. Server-owned and absent until this channel has launched; anything a caller sends for it is dropped.
 * @param kind Kind is the channel and the identity a campaign holds at most one of: paid, organic or email. It picks the executor the launch fans out to.
 * @param platform Platform is the provider within the kind — meta, google, x, instagram, or the email provider.
 * @param status Status is this channel's own launch outcome, not the campaign's: pending (added, never launched), live, paused, failed (Detail says why) or unavailable (no executor wired on this deployment). Server-owned — a caller can never assert it.
 */


data class ChannelSpec (

    /* Account is the provider account this channel runs under: an ad-account, a page or a mailing-list id. An executor may replace it at launch with the account it actually used. */
    @SerializedName("account")
    val account: kotlin.String? = null,

    /* Detail is the last outcome in one secret-free line — the failure reason, or what the executor reported. Absent when there is nothing to explain. */
    @SerializedName("detail")
    val detail: kotlin.String? = null,

    /* ExternalID is the provider-side id of the running execution, recorded by the orchestrator at launch and handed back verbatim to read spend or to pause. Server-owned and absent until this channel has launched; anything a caller sends for it is dropped. */
    @SerializedName("externalId")
    val externalId: kotlin.String? = null,

    /* Kind is the channel and the identity a campaign holds at most one of: paid, organic or email. It picks the executor the launch fans out to. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Platform is the provider within the kind — meta, google, x, instagram, or the email provider. */
    @SerializedName("platform")
    val platform: kotlin.String? = null,

    /* Status is this channel's own launch outcome, not the campaign's: pending (added, never launched), live, paused, failed (Detail says why) or unavailable (no executor wired on this deployment). Server-owned — a caller can never assert it. */
    @SerializedName("status")
    val status: kotlin.String? = null

) {


}

