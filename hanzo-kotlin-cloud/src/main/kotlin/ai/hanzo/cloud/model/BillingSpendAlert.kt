/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * A tenant-configured budget on cumulative spend for a SCOPE (project, service). It is a soft alert by default; with `enforce: true` it is a HARD per-scope spend cap — once `period` spend for the scope would exceed `threshold`, the metering gate denies with reason `spend_cap` (402). Period spend is DERIVED on demand from the append-only usage ledger for the current UTC month; the row carries NO running total, so a cap change takes effect on the next request and can never drift from the debited spend. Scope axes use \"\" as a wildcard: (\"\",\"\") is the org-wide default row, (P,\"\") every service in project P, (\"\",S) service S in any project. 
 *
 * @param id 
 * @param userId The org the cap is scoped to — server-pinned to the validated X-Org-Id, never a client field.
 * @param threshold The scope's spend cap / alert ceiling in USD cents (Money). 0 = no cap (a rate-limit-only row).
 * @param currency 
 * @param project Scope project axis; \"\" = wildcard (the org-wide default row).
 * @param service Scope service axis (server-derived route/provider); \"\" = wildcard (all services).
 * @param enforce false (default) = SOFT (warn only, never blocks); true = HARD cap (402 on breach).
 * @param softPct Soft-warn threshold as a percent of `threshold` (unset/0 defaults to 80).
 * @param rateLimitRpm Requests-per-minute ceiling for the scope (429 when exceeded). 0 = none.
 * @param title 
 * @param period The UTC calendar month the cap is measured over, e.g. \"2026-07\" (Schedule window; resets on the 1st).
 * @param resetsAt Midnight UTC on the first of the next month — when period spend resets to 0.
 * @param periodSpentCents DERIVED spend for the scope in `period`, in USD cents, summed from the deduped usage ledger. Absent if the aggregation errored.
 * @param over DERIVED — true iff a further billable request would breach the cap (the SAME boundary `enforce` uses, so it never drifts from the verdict).
 * @param warn DERIVED — true iff period spend has reached the soft-warn threshold (`softPct` of `threshold`).
 * @param triggeredAt ISO timestamp the alert last fired; empty if never.
 * @param createdAt 
 * @param updatedAt 
 */


data class BillingSpendAlert (

    @SerializedName("id")
    val id: kotlin.String,

    /* The org the cap is scoped to — server-pinned to the validated X-Org-Id, never a client field. */
    @SerializedName("userId")
    val userId: kotlin.String,

    /* The scope's spend cap / alert ceiling in USD cents (Money). 0 = no cap (a rate-limit-only row). */
    @SerializedName("threshold")
    val threshold: kotlin.Long,

    @SerializedName("currency")
    val currency: kotlin.String = "usd",

    /* Scope project axis; \"\" = wildcard (the org-wide default row). */
    @SerializedName("project")
    val project: kotlin.String,

    /* Scope service axis (server-derived route/provider); \"\" = wildcard (all services). */
    @SerializedName("service")
    val service: kotlin.String,

    /* false (default) = SOFT (warn only, never blocks); true = HARD cap (402 on breach). */
    @SerializedName("enforce")
    val enforce: kotlin.Boolean,

    /* Soft-warn threshold as a percent of `threshold` (unset/0 defaults to 80). */
    @SerializedName("softPct")
    val softPct: kotlin.Int,

    /* Requests-per-minute ceiling for the scope (429 when exceeded). 0 = none. */
    @SerializedName("rateLimitRpm")
    val rateLimitRpm: kotlin.Int,

    @SerializedName("title")
    val title: kotlin.String? = null,

    /* The UTC calendar month the cap is measured over, e.g. \"2026-07\" (Schedule window; resets on the 1st). */
    @SerializedName("period")
    val period: kotlin.String? = null,

    /* Midnight UTC on the first of the next month — when period spend resets to 0. */
    @SerializedName("resetsAt")
    val resetsAt: java.time.OffsetDateTime? = null,

    /* DERIVED spend for the scope in `period`, in USD cents, summed from the deduped usage ledger. Absent if the aggregation errored. */
    @SerializedName("periodSpentCents")
    val periodSpentCents: kotlin.Long? = null,

    /* DERIVED — true iff a further billable request would breach the cap (the SAME boundary `enforce` uses, so it never drifts from the verdict). */
    @SerializedName("over")
    val over: kotlin.Boolean? = null,

    /* DERIVED — true iff period spend has reached the soft-warn threshold (`softPct` of `threshold`). */
    @SerializedName("warn")
    val warn: kotlin.Boolean? = null,

    /* ISO timestamp the alert last fired; empty if never. */
    @SerializedName("triggeredAt")
    val triggeredAt: kotlin.String? = null,

    @SerializedName("createdAt")
    val createdAt: java.time.OffsetDateTime? = null,

    @SerializedName("updatedAt")
    val updatedAt: java.time.OffsetDateTime? = null

) : kotlin.collections.HashMap<String, kotlin.Any>() {


}

