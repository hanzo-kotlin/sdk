/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param error Error is why the probe is degraded, in plain words. Absent when it is not.
 * @param presign Presign is whether presigned upload and download URLs can be minted, which needs a PUBLIC endpoint on top of the credentials. False does not make the surface degraded — listing and creating still work.  NOT omitempty. On the ready path the untyped probe wrote this key unconditionally, and omitting a false one would turn what a healthy-but-presignless deployment reports from \"presign: false\" into silence — two different facts. The degraded body now carries it too, which is the one delta of typing this: the probe answers ONE shape under both of its statuses, which is what makes a single declared Out honest.
 * @param ready Ready is whether this deployment can serve object operations at all: true only when admin credentials are configured.
 * @param service Service names the subsystem this probe is for. Always \"s3\".
 * @param status Status is \"ok\" when the store is reachable in principle, \"degraded\" when it is not. It is the field to read; the HTTP status carries the same fact for a caller that only looks at the code.
 */


data class S3Health (

    /* Error is why the probe is degraded, in plain words. Absent when it is not. */
    @SerializedName("error")
    val error: kotlin.String? = null,

    /* Presign is whether presigned upload and download URLs can be minted, which needs a PUBLIC endpoint on top of the credentials. False does not make the surface degraded — listing and creating still work.  NOT omitempty. On the ready path the untyped probe wrote this key unconditionally, and omitting a false one would turn what a healthy-but-presignless deployment reports from \"presign: false\" into silence — two different facts. The degraded body now carries it too, which is the one delta of typing this: the probe answers ONE shape under both of its statuses, which is what makes a single declared Out honest. */
    @SerializedName("presign")
    val presign: kotlin.Boolean? = null,

    /* Ready is whether this deployment can serve object operations at all: true only when admin credentials are configured. */
    @SerializedName("ready")
    val ready: kotlin.Boolean? = null,

    /* Service names the subsystem this probe is for. Always \"s3\". */
    @SerializedName("service")
    val service: kotlin.String? = null,

    /* Status is \"ok\" when the store is reachable in principle, \"degraded\" when it is not. It is the field to read; the HTTP status carries the same fact for a caller that only looks at the code. */
    @SerializedName("status")
    val status: kotlin.String? = null

) {


}

