/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.DeclareEnv

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param application Application is the CD Application name the generator mints: <org>-<name>. It is the join key against /v1/platform/cd.
 * @param automated Automated is cd.automated: false means the Application reports drift and NOTHING moves. It is off by default for a new file on purpose.
 * @param digest image.digest — wins over tag
 * @param env Env is the declared container environment, as the chart's list of {name,value}. It is read back so a re-declare of an identical body is a no-op rather than a refusal — idempotency is what makes a retry safe.
 * @param hosts ingress.hosts, both shapes flattened
 * @param name the Helm release name — the file's basename
 * @param org Org is the owner. It is ALSO the values directory and the destination namespace, because those are one value under one name — see the header.
 * @param path Path is the file, relative to the repository root.
 * @param project Project is the AppProject the sync is admitted under, derived from the directory exactly as the ApplicationSet derives it. It differs from Org for a reserved directory, which syncs under the platform fence.
 * @param replicas 
 * @param repository image.repository
 * @param tag image.tag
 */


data class Declaration (

    /* Application is the CD Application name the generator mints: <org>-<name>. It is the join key against /v1/platform/cd. */
    @SerializedName("application")
    val application: kotlin.String? = null,

    /* Automated is cd.automated: false means the Application reports drift and NOTHING moves. It is off by default for a new file on purpose. */
    @SerializedName("automated")
    val automated: kotlin.Boolean? = null,

    /* image.digest — wins over tag */
    @SerializedName("digest")
    val digest: kotlin.String? = null,

    /* Env is the declared container environment, as the chart's list of {name,value}. It is read back so a re-declare of an identical body is a no-op rather than a refusal — idempotency is what makes a retry safe. */
    @SerializedName("env")
    val env: kotlin.collections.List<DeclareEnv>? = null,

    /* ingress.hosts, both shapes flattened */
    @SerializedName("hosts")
    val hosts: kotlin.collections.List<kotlin.String>? = null,

    /* the Helm release name — the file's basename */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Org is the owner. It is ALSO the values directory and the destination namespace, because those are one value under one name — see the header. */
    @SerializedName("org")
    val org: kotlin.String? = null,

    /* Path is the file, relative to the repository root. */
    @SerializedName("path")
    val path: kotlin.String? = null,

    /* Project is the AppProject the sync is admitted under, derived from the directory exactly as the ApplicationSet derives it. It differs from Org for a reserved directory, which syncs under the platform fence. */
    @SerializedName("project")
    val project: kotlin.String? = null,

    @SerializedName("replicas")
    val replicas: kotlin.Int? = null,

    /* image.repository */
    @SerializedName("repository")
    val repository: kotlin.String? = null,

    /* image.tag */
    @SerializedName("tag")
    val tag: kotlin.String? = null

) {


}

