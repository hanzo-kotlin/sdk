/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.TrafficCaller

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param blind Blind is how many requests in the window carried no identity to attribute them to — no validated credential and no client address. Non-zero on a public plane means the client address is not reaching this process (a TCP load balancer with no PROXY protocol in front of it, typically), so this scope's callers cannot be told apart and nothing can be held against them.
 * @param callers Callers is the scope's busiest callers this window. A credentialed caller appears as a FINGERPRINT — a per-process one-way digest: enough to recognise the same caller across requests, never enough to reconstruct the credential.
 * @param ceiling Ceiling is the most callers this scope may hold at once.
 * @param denied Denied is how many of them the gate refused.
 * @param lanes Lanes is the request count per lane — agent, human, bot, unknown. This is the split that separates a customer's automation from a scraper.
 * @param mode Mode is the abuse gate's posture for this scope: \"shadow\" records the scorer's action without enforcing it, \"live\" enforces it.
 * @param org Org is the scope this view was taken for — the validated principal's own, never a value the caller supplied. Empty names the anonymous lane, the one scope that has no tenant.
 * @param refused Refused is how many callers this scope's ceilings turned away in the window.
 * @param requests Requests is how many requests this scope made in the window.
 * @param screens Screens is how many of them were put to the scorer — the billable unit of the risk product. Counted from the first request, whatever the SKU costs.
 * @param strain Strain is what this scope's ceilings are doing: \"clear\" below them, \"full\" at them, \"refuse\" once a caller has been turned away inside this window — which means that caller is UNMEASURED and the numbers here are a sample rather than a census. It is reported rather than logged because the alternative — a bound that degrades a scope silently — is the failure this design exists to rule out. No other scope can move it.
 * @param tracked Tracked is how many callers this scope holds state for right now, and Ceiling is the most it may hold. Tracked == Ceiling is the fact a bound that binds cannot hide.
 * @param unscored Unscored is how many of those screens got NO answer — the scorer was absent, stuck, slow, erroring or silent. An unanswered screen allows ordinary traffic, so this is the number that separates \"a quiet day\" from \"the judge stopped answering and nothing said so\".
 * @param windowSec WindowSec is the span the counts cover, in seconds.
 */


data class TrafficView (

    /* Blind is how many requests in the window carried no identity to attribute them to — no validated credential and no client address. Non-zero on a public plane means the client address is not reaching this process (a TCP load balancer with no PROXY protocol in front of it, typically), so this scope's callers cannot be told apart and nothing can be held against them. */
    @SerializedName("blind")
    val blind: kotlin.Int? = null,

    /* Callers is the scope's busiest callers this window. A credentialed caller appears as a FINGERPRINT — a per-process one-way digest: enough to recognise the same caller across requests, never enough to reconstruct the credential. */
    @SerializedName("callers")
    val callers: kotlin.collections.List<TrafficCaller>? = null,

    /* Ceiling is the most callers this scope may hold at once. */
    @SerializedName("ceiling")
    val ceiling: kotlin.Int? = null,

    /* Denied is how many of them the gate refused. */
    @SerializedName("denied")
    val denied: kotlin.Int? = null,

    /* Lanes is the request count per lane — agent, human, bot, unknown. This is the split that separates a customer's automation from a scraper. */
    @SerializedName("lanes")
    val lanes: kotlin.collections.Map<kotlin.String, kotlin.Int>? = null,

    /* Mode is the abuse gate's posture for this scope: \"shadow\" records the scorer's action without enforcing it, \"live\" enforces it. */
    @SerializedName("mode")
    val mode: kotlin.String? = null,

    /* Org is the scope this view was taken for — the validated principal's own, never a value the caller supplied. Empty names the anonymous lane, the one scope that has no tenant. */
    @SerializedName("org")
    val org: kotlin.String? = null,

    /* Refused is how many callers this scope's ceilings turned away in the window. */
    @SerializedName("refused")
    val refused: kotlin.Int? = null,

    /* Requests is how many requests this scope made in the window. */
    @SerializedName("requests")
    val requests: kotlin.Int? = null,

    /* Screens is how many of them were put to the scorer — the billable unit of the risk product. Counted from the first request, whatever the SKU costs. */
    @SerializedName("screens")
    val screens: kotlin.Int? = null,

    /* Strain is what this scope's ceilings are doing: \"clear\" below them, \"full\" at them, \"refuse\" once a caller has been turned away inside this window — which means that caller is UNMEASURED and the numbers here are a sample rather than a census. It is reported rather than logged because the alternative — a bound that degrades a scope silently — is the failure this design exists to rule out. No other scope can move it. */
    @SerializedName("strain")
    val strain: kotlin.String? = null,

    /* Tracked is how many callers this scope holds state for right now, and Ceiling is the most it may hold. Tracked == Ceiling is the fact a bound that binds cannot hide. */
    @SerializedName("tracked")
    val tracked: kotlin.Int? = null,

    /* Unscored is how many of those screens got NO answer — the scorer was absent, stuck, slow, erroring or silent. An unanswered screen allows ordinary traffic, so this is the number that separates \"a quiet day\" from \"the judge stopped answering and nothing said so\". */
    @SerializedName("unscored")
    val unscored: kotlin.Int? = null,

    /* WindowSec is the span the counts cover, in seconds. */
    @SerializedName("window_sec")
    val windowSec: kotlin.Int? = null

) {


}

