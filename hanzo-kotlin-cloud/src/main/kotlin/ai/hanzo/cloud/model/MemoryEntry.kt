/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param actor Actor is the validated user id that last wrote this entry by hand. Empty on an entry an engine produced, and on one written before attribution existed.
 * @param glossaryVersion Glossary is the glossary VERSION the entry was translated under — the digest version() derives from the terms, so changing a term changes the key and the stale rendering can never be served.
 * @param source Source is the ORIGINAL string this entry translates. Part of the identity.
 * @param state State is the entry's position on the review ladder: machine, suggested, approved or published.
 * @param target Target is the target language tag (BCP-47, e.g. \"es\" or \"pt-BR\"). Part of the identity.
 * @param text Text is the stored translation. A memory hit returns it verbatim, which is the idempotence contract.
 * @param tier Tier is the engine tier the entry belongs to, quality or bulk. Part of the identity: the two tiers keep separate renderings of the same source.
 * @param updatedAt UpdatedAt is the unix second the entry last changed.
 */


data class MemoryEntry (

    /* Actor is the validated user id that last wrote this entry by hand. Empty on an entry an engine produced, and on one written before attribution existed. */
    @SerializedName("actor")
    val actor: kotlin.String? = null,

    /* Glossary is the glossary VERSION the entry was translated under — the digest version() derives from the terms, so changing a term changes the key and the stale rendering can never be served. */
    @SerializedName("glossary_version")
    val glossaryVersion: kotlin.String? = null,

    /* Source is the ORIGINAL string this entry translates. Part of the identity. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* State is the entry's position on the review ladder: machine, suggested, approved or published. */
    @SerializedName("state")
    val state: kotlin.String? = null,

    /* Target is the target language tag (BCP-47, e.g. \"es\" or \"pt-BR\"). Part of the identity. */
    @SerializedName("target")
    val target: kotlin.String? = null,

    /* Text is the stored translation. A memory hit returns it verbatim, which is the idempotence contract. */
    @SerializedName("text")
    val text: kotlin.String? = null,

    /* Tier is the engine tier the entry belongs to, quality or bulk. Part of the identity: the two tiers keep separate renderings of the same source. */
    @SerializedName("tier")
    val tier: kotlin.String? = null,

    /* UpdatedAt is the unix second the entry last changed. */
    @SerializedName("updated_at")
    val updatedAt: kotlin.Int? = null

) {


}

