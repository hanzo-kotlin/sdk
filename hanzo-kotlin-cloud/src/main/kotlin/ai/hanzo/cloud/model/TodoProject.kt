/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param createdAt CreatedAt is when the board was created, in unix seconds. 0 on a forge board for the same reason Description is absent.
 * @param description Description is whatever an index board was created with. Absent on a forge board: this projection takes the repository's name and nothing else about the repository.
 * @param id ID is the board's opaque handle, and it is NOT how you address it — Key is. Its shape says which source answered: a forge board's is the repository's full name (\"hanzoai/cloud\"), an index board's a minted \"prj_\" id.
 * @param key Key addresses the board everywhere else — /v1/todo/projects/<key>/issues — and prefixes every issue identifier on it. An index board's key is 2-8 uppercase alphanumerics starting with a letter (\"ENG\", \"OPS2\") and is matched case-insensitively; a forge board's is the repository name as the forge spells it.
 * @param name Name is the board's display name. For a forge board it is the repository name, so it equals Key; an index board carries its own.
 * @param org Org is the IAM org the board belongs to, taken from the validated principal and never from the request. Every board a caller can see is in it.
 * @param updatedAt UpdatedAt is when the board record last changed, in unix seconds — the BOARD, not the work on it, so filing an issue does not move it. 0 on a forge board.
 */


data class TodoProject (

    /* CreatedAt is when the board was created, in unix seconds. 0 on a forge board for the same reason Description is absent. */
    @SerializedName("createdAt")
    val createdAt: kotlin.Int? = null,

    /* Description is whatever an index board was created with. Absent on a forge board: this projection takes the repository's name and nothing else about the repository. */
    @SerializedName("description")
    val description: kotlin.String? = null,

    /* ID is the board's opaque handle, and it is NOT how you address it — Key is. Its shape says which source answered: a forge board's is the repository's full name (\"hanzoai/cloud\"), an index board's a minted \"prj_\" id. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Key addresses the board everywhere else — /v1/todo/projects/<key>/issues — and prefixes every issue identifier on it. An index board's key is 2-8 uppercase alphanumerics starting with a letter (\"ENG\", \"OPS2\") and is matched case-insensitively; a forge board's is the repository name as the forge spells it. */
    @SerializedName("key")
    val key: kotlin.String? = null,

    /* Name is the board's display name. For a forge board it is the repository name, so it equals Key; an index board carries its own. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Org is the IAM org the board belongs to, taken from the validated principal and never from the request. Every board a caller can see is in it. */
    @SerializedName("org")
    val org: kotlin.String? = null,

    /* UpdatedAt is when the board record last changed, in unix seconds — the BOARD, not the work on it, so filing an issue does not move it. 0 on a forge board. */
    @SerializedName("updatedAt")
    val updatedAt: kotlin.Int? = null

) {


}

