/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param email Email is where this recipient's signing link is meant to go, lower-cased.
 * @param id ID is the recipient id, which is what a field is placed against.
 * @param name Name is the recipient's display name, empty when none was given.
 * @param readStatus ReadStatus is NOT_OPENED until they first open their link, then OPENED.
 * @param rejectionReason RejectionReason is why they declined, null unless they did.
 * @param role Role is SIGNER, CC, VIEWER, APPROVER or ASSISTANT. A document waits only for its SIGNERs and APPROVERs before it can seal.
 * @param sendStatus SendStatus is NOT_SENT until the document goes out, then SENT. A CC recipient is SENT from the moment they are added.
 * @param signedAt SignedAt is when they finished or declined, in unix milliseconds; null while neither has happened.
 * @param signingOrder SigningOrder is their position in a SEQUENTIAL document, null when they were added without one. A PARALLEL document ignores it.
 * @param signingStatus SigningStatus is NOT_SIGNED, SIGNED or REJECTED. A CC recipient is SIGNED from the moment they are added, because they are never asked.
 */


data class EsignRecipient (

    /* Email is where this recipient's signing link is meant to go, lower-cased. */
    @SerializedName("email")
    val email: kotlin.String? = null,

    /* ID is the recipient id, which is what a field is placed against. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Name is the recipient's display name, empty when none was given. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* ReadStatus is NOT_OPENED until they first open their link, then OPENED. */
    @SerializedName("readStatus")
    val readStatus: kotlin.String? = null,

    /* RejectionReason is why they declined, null unless they did. */
    @SerializedName("rejectionReason")
    val rejectionReason: kotlin.String? = null,

    /* Role is SIGNER, CC, VIEWER, APPROVER or ASSISTANT. A document waits only for its SIGNERs and APPROVERs before it can seal. */
    @SerializedName("role")
    val role: kotlin.String? = null,

    /* SendStatus is NOT_SENT until the document goes out, then SENT. A CC recipient is SENT from the moment they are added. */
    @SerializedName("sendStatus")
    val sendStatus: kotlin.String? = null,

    /* SignedAt is when they finished or declined, in unix milliseconds; null while neither has happened. */
    @SerializedName("signedAt")
    val signedAt: kotlin.Int? = null,

    /* SigningOrder is their position in a SEQUENTIAL document, null when they were added without one. A PARALLEL document ignores it. */
    @SerializedName("signingOrder")
    val signingOrder: java.math.BigDecimal? = null,

    /* SigningStatus is NOT_SIGNED, SIGNED or REJECTED. A CC recipient is SIGNED from the moment they are added, because they are never asked. */
    @SerializedName("signingStatus")
    val signingStatus: kotlin.String? = null

) {


}

