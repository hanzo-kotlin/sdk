/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param attester Attester is who vouched for it: \"self\" for anything the org states itself, or \"auditor\" for anything an independent auditor put their name to. REQUIRED, and anything other than \"self\" is read as \"auditor\" — the safe direction, since an auditor-signed item can only ever be released on request.
 * @param body Body is the item's content for the kinds that are text rather than a file: an article, a subprocessor entry, a dated note.
 * @param document Document is a data-room document holding the item's bytes, uploaded first through POST /v1/dataroom/documents. Optional: an item can be content with no file. The document must already exist in the caller org's own store.
 * @param framework Framework is the standard it speaks to. Optional and free text — the value is the org's own, not a list this API keeps.
 * @param kind Kind is what the item is: report, letter, policy, questionnaire, subprocessor, article or update. Required.
 * @param name Name is the item's title. Required.
 * @param summary Summary is a line about it. Optional.
 * @param tier Tier is who may read it: \"public\" or \"gated\". It DEFAULTS TO GATED and anything that is not exactly \"public\" is gated, so an item published by a caller that says nothing is private and someone has to release it on purpose. \"public\" is refused for an auditor-signed item.
 */


data class TrustPublish (

    /* Attester is who vouched for it: \"self\" for anything the org states itself, or \"auditor\" for anything an independent auditor put their name to. REQUIRED, and anything other than \"self\" is read as \"auditor\" — the safe direction, since an auditor-signed item can only ever be released on request. */
    @SerializedName("attester")
    val attester: kotlin.String? = null,

    /* Body is the item's content for the kinds that are text rather than a file: an article, a subprocessor entry, a dated note. */
    @SerializedName("body")
    val body: kotlin.String? = null,

    /* Document is a data-room document holding the item's bytes, uploaded first through POST /v1/dataroom/documents. Optional: an item can be content with no file. The document must already exist in the caller org's own store. */
    @SerializedName("document")
    val document: kotlin.String? = null,

    /* Framework is the standard it speaks to. Optional and free text — the value is the org's own, not a list this API keeps. */
    @SerializedName("framework")
    val framework: kotlin.String? = null,

    /* Kind is what the item is: report, letter, policy, questionnaire, subprocessor, article or update. Required. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Name is the item's title. Required. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Summary is a line about it. Optional. */
    @SerializedName("summary")
    val summary: kotlin.String? = null,

    /* Tier is who may read it: \"public\" or \"gated\". It DEFAULTS TO GATED and anything that is not exactly \"public\" is gated, so an item published by a caller that says nothing is private and someone has to release it on purpose. \"public\" is refused for an auditor-signed item. */
    @SerializedName("tier")
    val tier: kotlin.String? = null

) {


}

