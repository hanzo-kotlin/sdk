/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param author Author is the team account uuid that wrote it. It is an ACCOUNT and not a display name: what to call somebody is the roster's answer, and copying it onto every message is how the two come to disagree. An agent's messages carry the account derived from its id, so the same field answers for both.
 * @param createdOn CreatedOn is unix MILLIseconds, which is what the platform stamps.
 * @param id ID is the message document's own id.
 * @param room Room is the room it was said in — the same id the room listing answers with, so a caller holding a message can name its room without a second read.
 * @param text Text is the message as PLAIN TEXT. The document stores markup; this is the same `plainText` reduction the agent responder reads a prompt with, so a caller never has to parse the client's markup to know what was said.
 */


data class TeamMessage (

    /* Author is the team account uuid that wrote it. It is an ACCOUNT and not a display name: what to call somebody is the roster's answer, and copying it onto every message is how the two come to disagree. An agent's messages carry the account derived from its id, so the same field answers for both. */
    @SerializedName("author")
    val author: kotlin.String? = null,

    /* CreatedOn is unix MILLIseconds, which is what the platform stamps. */
    @SerializedName("createdOn")
    val createdOn: kotlin.Int? = null,

    /* ID is the message document's own id. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Room is the room it was said in — the same id the room listing answers with, so a caller holding a message can name its room without a second read. */
    @SerializedName("room")
    val room: kotlin.String? = null,

    /* Text is the message as PLAIN TEXT. The document stores markup; this is the same `plainText` reduction the agent responder reads a prompt with, so a caller never has to parse the client's markup to know what was said. */
    @SerializedName("text")
    val text: kotlin.String? = null

) {


}

