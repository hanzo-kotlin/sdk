/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param groups Groups is the caller's group list, always empty here: this console authorizes on the platform SuperAdmin fact alone, not on argocd RBAC groups. Absent for an anonymous caller.
 * @param iss Iss is the token issuer as the SPA expects to see it — the literal \"argocd\", so the UI never triggers an SSO redirect of its own. Absent for an anonymous caller.
 * @param loggedIn LoggedIn reports whether this browser holds a session this console accepts.
 * @param loginUrl LoginURL is where an anonymous caller signs in. Absent once signed in.
 * @param logoutUrl LogoutURL is where a signed-in caller ends the session. Absent when anonymous.
 * @param username Username is the validated principal's user ID — the opaque gateway id, which is what argocd's UI renders as the signed-in user here — or \"admin\" when the principal carries none. Absent when anonymous.
 */


data class SessionUser (

    /* Groups is the caller's group list, always empty here: this console authorizes on the platform SuperAdmin fact alone, not on argocd RBAC groups. Absent for an anonymous caller. */
    @SerializedName("groups")
    val groups: kotlin.collections.List<kotlin.String>? = null,

    /* Iss is the token issuer as the SPA expects to see it — the literal \"argocd\", so the UI never triggers an SSO redirect of its own. Absent for an anonymous caller. */
    @SerializedName("iss")
    val iss: kotlin.String? = null,

    /* LoggedIn reports whether this browser holds a session this console accepts. */
    @SerializedName("loggedIn")
    val loggedIn: kotlin.Boolean? = null,

    /* LoginURL is where an anonymous caller signs in. Absent once signed in. */
    @SerializedName("loginUrl")
    val loginUrl: kotlin.String? = null,

    /* LogoutURL is where a signed-in caller ends the session. Absent when anonymous. */
    @SerializedName("logoutUrl")
    val logoutUrl: kotlin.String? = null,

    /* Username is the validated principal's user ID — the opaque gateway id, which is what argocd's UI renders as the signed-in user here — or \"admin\" when the principal carries none. Absent when anonymous. */
    @SerializedName("username")
    val username: kotlin.String? = null

) {


}

