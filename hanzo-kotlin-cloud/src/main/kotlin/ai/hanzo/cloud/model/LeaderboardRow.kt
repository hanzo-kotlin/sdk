/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accruedCents AccruedCents is that affiliate's lifetime commission accrued, in cents, and what the board is ordered by. An aggregate: no per-customer figure is exposed.
 * @param handle Handle is the affiliate's self-chosen display name — the only identity the board ever carries. The org behind it is never disclosed.
 * @param isYou IsYou marks the caller's own row, so a client can highlight it without matching on a handle. Absent on every other row.
 * @param rank Rank is the position in the GLOBAL approved set ordered by lifetime accrued commission, 1-based. Affiliates that set no handle still occupy their rank and are simply not listed, so the visible ranks have gaps and the board is not a complete roster. On the caller's own row the rank is computed over the whole set, so it is exact well outside the top page.
 * @param referredCount ReferredCount is how many orgs that affiliate directly referred — a count only, never which orgs.
 */


data class LeaderboardRow (

    /* AccruedCents is that affiliate's lifetime commission accrued, in cents, and what the board is ordered by. An aggregate: no per-customer figure is exposed. */
    @SerializedName("accruedCents")
    val accruedCents: kotlin.Int? = null,

    /* Handle is the affiliate's self-chosen display name — the only identity the board ever carries. The org behind it is never disclosed. */
    @SerializedName("handle")
    val handle: kotlin.String? = null,

    /* IsYou marks the caller's own row, so a client can highlight it without matching on a handle. Absent on every other row. */
    @SerializedName("isYou")
    val isYou: kotlin.Boolean? = null,

    /* Rank is the position in the GLOBAL approved set ordered by lifetime accrued commission, 1-based. Affiliates that set no handle still occupy their rank and are simply not listed, so the visible ranks have gaps and the board is not a complete roster. On the caller's own row the rank is computed over the whole set, so it is exact well outside the top page. */
    @SerializedName("rank")
    val rank: kotlin.Int? = null,

    /* ReferredCount is how many orgs that affiliate directly referred — a count only, never which orgs. */
    @SerializedName("referredCount")
    val referredCount: kotlin.Int? = null

) {


}

