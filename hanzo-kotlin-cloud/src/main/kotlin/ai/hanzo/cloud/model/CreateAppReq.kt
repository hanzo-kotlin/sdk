/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.EnvVarJSON
import ai.hanzo.cloud.model.GitOrigin
import ai.hanzo.cloud.model.ImageOrigin

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param buildType BuildType is `pack` — the zero-config default that detects any project — or `dockerfile`, the explicit escape hatch. An image app never builds.
 * @param description Description is free text about what the application is.
 * @param dockerfile Dockerfile is the path to build from, for buildType `dockerfile`.
 * @param domains Domains are extra ingress hosts. The canonical default host is always attached; a bare custom host is refused here and must go through add-domain → verify first.
 * @param env Env is the application's environment. Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`; a variable marked `secret: true` is sealed into KMS and its plaintext is never written to the database.
 * @param environment Environment is the deploy target this app names (\"production\" by default).
 * @param image Image is the container image to run, for source `image`.
 * @param name Name is the application's display name. Required; the slug is derived from it when none is given.
 * @param port Port is the container port the app listens on.
 * @param project Project is the project to create the application under, from the path.
 * @param replicas Replicas is how many copies to run; clamped to the deployment's limit rather than refused.
 * @param repo Repo is the git source to build from, for source `git`.
 * @param slug Slug is the app's identity in the cluster — its CR name and part of its host. Given or derived from Name, it must match `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`, and one already used in this project is 409.
 * @param source Source is `git`, which requires repo.url, or `image`, which requires image.repository. Anything else is 400.
 * @param storageGb StorageGB is the persistent volume size in GiB; absent means stateless. Clamped to the deployment's limit rather than refused.
 */


data class CreateAppReq (

    /* BuildType is `pack` — the zero-config default that detects any project — or `dockerfile`, the explicit escape hatch. An image app never builds. */
    @SerializedName("buildType")
    val buildType: kotlin.String? = null,

    /* Description is free text about what the application is. */
    @SerializedName("description")
    val description: kotlin.String? = null,

    /* Dockerfile is the path to build from, for buildType `dockerfile`. */
    @SerializedName("dockerfile")
    val dockerfile: kotlin.String? = null,

    /* Domains are extra ingress hosts. The canonical default host is always attached; a bare custom host is refused here and must go through add-domain → verify first. */
    @SerializedName("domains")
    val domains: kotlin.collections.List<kotlin.String>? = null,

    /* Env is the application's environment. Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`; a variable marked `secret: true` is sealed into KMS and its plaintext is never written to the database. */
    @SerializedName("env")
    val env: kotlin.collections.List<EnvVarJSON>? = null,

    /* Environment is the deploy target this app names (\"production\" by default). */
    @SerializedName("environment")
    val environment: kotlin.String? = null,

    /* Image is the container image to run, for source `image`. */
    @SerializedName("image")
    val image: ImageOrigin? = null,

    /* Name is the application's display name. Required; the slug is derived from it when none is given. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Port is the container port the app listens on. */
    @SerializedName("port")
    val port: kotlin.Int? = null,

    /* Project is the project to create the application under, from the path. */
    @SerializedName("project")
    val project: kotlin.String? = null,

    /* Replicas is how many copies to run; clamped to the deployment's limit rather than refused. */
    @SerializedName("replicas")
    val replicas: kotlin.Int? = null,

    /* Repo is the git source to build from, for source `git`. */
    @SerializedName("repo")
    val repo: GitOrigin? = null,

    /* Slug is the app's identity in the cluster — its CR name and part of its host. Given or derived from Name, it must match `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`, and one already used in this project is 409. */
    @SerializedName("slug")
    val slug: kotlin.String? = null,

    /* Source is `git`, which requires repo.url, or `image`, which requires image.repository. Anything else is 400. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* StorageGB is the persistent volume size in GiB; absent means stateless. Clamped to the deployment's limit rather than refused. */
    @SerializedName("storageGb")
    val storageGb: kotlin.Int? = null

) {


}

