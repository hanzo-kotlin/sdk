/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.CloudDestinationField

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param account Account is the operator's own label for the connected account, as supplied on connect. Absent when unset.
 * @param category groups the card: Analytics | Advertising
 * @param config Config is the org's stored NON-SECRET configuration — the measurement/pixel ids keyed by DestinationField.Key. A secret is never in here; secrets live in KMS and only their names are published, in Secrets.
 * @param connected Connected is true when this org has a stored row for the platform — it has been configured here at least once. It says nothing about whether a credential still resolves; that is Live.
 * @param enabled Enabled is whether the fan-out forwards to this destination. False on a destination that is connected but paused, and on one never connected.
 * @param fields Fields are the non-secret inputs this platform needs, which the console card renders and the connect body fills.
 * @param live Live is whether a credential resolves RIGHT NOW: a KMS-sealed secret for this org, else the integrations connection named by the platform's Fallback, else no credential needed at all (a public-ingest sink like Umami). False on a connected destination whose secret has gone missing — Connected && !Live is exactly the \"reconnect me\" state.
 * @param name the platform's display name (\"Google Analytics 4\")
 * @param platform the platform slug, and the path segment every route addresses it by
 * @param secrets Secrets are the KMS secret NAMES this platform custodies for the org — names only, never values. The connect body accepts each under its camelCase form.
 */


data class CloudDestinationStatus (

    /* Account is the operator's own label for the connected account, as supplied on connect. Absent when unset. */
    @SerializedName("account")
    val account: kotlin.String? = null,

    /* groups the card: Analytics | Advertising */
    @SerializedName("category")
    val category: kotlin.String? = null,

    /* Config is the org's stored NON-SECRET configuration — the measurement/pixel ids keyed by DestinationField.Key. A secret is never in here; secrets live in KMS and only their names are published, in Secrets. */
    @SerializedName("config")
    val config: kotlin.collections.Map<kotlin.String, kotlin.String>? = null,

    /* Connected is true when this org has a stored row for the platform — it has been configured here at least once. It says nothing about whether a credential still resolves; that is Live. */
    @SerializedName("connected")
    val connected: kotlin.Boolean? = null,

    /* Enabled is whether the fan-out forwards to this destination. False on a destination that is connected but paused, and on one never connected. */
    @SerializedName("enabled")
    val enabled: kotlin.Boolean? = null,

    /* Fields are the non-secret inputs this platform needs, which the console card renders and the connect body fills. */
    @SerializedName("fields")
    val fields: kotlin.collections.List<CloudDestinationField>? = null,

    /* Live is whether a credential resolves RIGHT NOW: a KMS-sealed secret for this org, else the integrations connection named by the platform's Fallback, else no credential needed at all (a public-ingest sink like Umami). False on a connected destination whose secret has gone missing — Connected && !Live is exactly the \"reconnect me\" state. */
    @SerializedName("live")
    val live: kotlin.Boolean? = null,

    /* the platform's display name (\"Google Analytics 4\") */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* the platform slug, and the path segment every route addresses it by */
    @SerializedName("platform")
    val platform: kotlin.String? = null,

    /* Secrets are the KMS secret NAMES this platform custodies for the org — names only, never values. The connect body accepts each under its camelCase form. */
    @SerializedName("secrets")
    val secrets: kotlin.collections.List<kotlin.String>? = null

) {


}

