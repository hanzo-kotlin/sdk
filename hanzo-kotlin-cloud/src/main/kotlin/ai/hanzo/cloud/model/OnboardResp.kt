/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accessKey AccessKey is the identifier of the org-scoped credential provisioning minted with the organization. Present on a first run that actually minted one.
 * @param accessSecret AccessSecret is that credential's confidential half, returned ONCE — on the response that mints it and never again. IAM keeps only its argon2id digest and blanks the plaintext, so this is the single moment it exists in a form its owner can read; a replay of the same provision re-reveals nothing.
 * @param additional Additional is true when the caller already had an organization and this one was created WITHOUT moving them into it — they reach it via the org switcher.
 * @param displayName DisplayName is the organization's human name.
 * @param org Org is the created organization's slug, which is what X-Org-Id carries.
 */


data class OnboardResp (

    /* AccessKey is the identifier of the org-scoped credential provisioning minted with the organization. Present on a first run that actually minted one. */
    @SerializedName("accessKey")
    val accessKey: kotlin.String? = null,

    /* AccessSecret is that credential's confidential half, returned ONCE — on the response that mints it and never again. IAM keeps only its argon2id digest and blanks the plaintext, so this is the single moment it exists in a form its owner can read; a replay of the same provision re-reveals nothing. */
    @SerializedName("accessSecret")
    val accessSecret: kotlin.String? = null,

    /* Additional is true when the caller already had an organization and this one was created WITHOUT moving them into it — they reach it via the org switcher. */
    @SerializedName("additional")
    val additional: kotlin.Boolean? = null,

    /* DisplayName is the organization's human name. */
    @SerializedName("displayName")
    val displayName: kotlin.String? = null,

    /* Org is the created organization's slug, which is what X-Org-Id carries. */
    @SerializedName("org")
    val org: kotlin.String? = null

) {


}

