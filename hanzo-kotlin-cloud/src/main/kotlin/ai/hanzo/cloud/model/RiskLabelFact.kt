/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param at At is when the judged event happened, RFC 3339.
 * @param confidence Confidence in [0,1]. A processor chargeback is 1; an analyst's hunch is not. It breaks a tie WITHIN a precedence rank and can never lift a weak source above a strong one — otherwise every caller would send 1.  A litigation hold is NOT a field here. It is a fact about the record and not about the world, so it is not part of what was asserted, it is not in the content digest, and it has its own op — which is also the only way one can be released. Carried here it was silently a no-op on any record that already existed: the digest was the same, the insert was ignored, and the caller was told `duplicate` while the hold it asked for was never placed.
 * @param disposition Disposition is productive, unproductive, or empty for an explicit unjudged — the AML engine's own vocabulary, verbatim.
 * @param evidence Evidence points at the record this conclusion came from: a dispute id, a case id, a decision id. Required, because a label with no evidence cannot be defended when the adverse action it fed is challenged.
 * @param kind Kind is what the subject is: account, agent, merchant, payout, person, session or transaction. Closed, because a typo in an open field would shard a tenant's labels into a partition nothing reads and nothing would say so.
 * @param seen Seen is when this assertion became KNOWABLE, RFC 3339. It is required and it is not At: a chargeback lands 30 to 120 days after the transaction it judges, and a training set joined on At alone knows the future. Everything this plane does to prevent leakage is computed from Seen.
 * @param source Source is who asserted: chargeoff, dispute, case, refund, review or sample. It is the primary term of the precedence rule, so it is closed — an unknown source has no rank and a conflict with it could not be resolved.
 * @param subject Subject identifies the thing being judged, in the tenant's own namespace.
 */


data class RiskLabelFact (

    /* At is when the judged event happened, RFC 3339. */
    @SerializedName("at")
    val at: kotlin.String? = null,

    /* Confidence in [0,1]. A processor chargeback is 1; an analyst's hunch is not. It breaks a tie WITHIN a precedence rank and can never lift a weak source above a strong one — otherwise every caller would send 1.  A litigation hold is NOT a field here. It is a fact about the record and not about the world, so it is not part of what was asserted, it is not in the content digest, and it has its own op — which is also the only way one can be released. Carried here it was silently a no-op on any record that already existed: the digest was the same, the insert was ignored, and the caller was told `duplicate` while the hold it asked for was never placed. */
    @SerializedName("confidence")
    val confidence: java.math.BigDecimal? = null,

    /* Disposition is productive, unproductive, or empty for an explicit unjudged — the AML engine's own vocabulary, verbatim. */
    @SerializedName("disposition")
    val disposition: kotlin.String? = null,

    /* Evidence points at the record this conclusion came from: a dispute id, a case id, a decision id. Required, because a label with no evidence cannot be defended when the adverse action it fed is challenged. */
    @SerializedName("evidence")
    val evidence: kotlin.String? = null,

    /* Kind is what the subject is: account, agent, merchant, payout, person, session or transaction. Closed, because a typo in an open field would shard a tenant's labels into a partition nothing reads and nothing would say so. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Seen is when this assertion became KNOWABLE, RFC 3339. It is required and it is not At: a chargeback lands 30 to 120 days after the transaction it judges, and a training set joined on At alone knows the future. Everything this plane does to prevent leakage is computed from Seen. */
    @SerializedName("seen")
    val seen: kotlin.String? = null,

    /* Source is who asserted: chargeoff, dispute, case, refund, review or sample. It is the primary term of the precedence rule, so it is closed — an unknown source has no rank and a conflict with it could not be resolved. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Subject identifies the thing being judged, in the tenant's own namespace. */
    @SerializedName("subject")
    val subject: kotlin.String? = null

) {


}

