/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.CartItem

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param createdAt CreatedAt is when the cart was opened, RFC3339.
 * @param currency Currency is the ISO 4217 code every amount below is denominated in.
 * @param discountCents DiscountCents is what coupons and promotions took off, in whole cents.
 * @param email Email is the shopper's address, when the cart carries one.
 * @param id ID is the cart's id — what every other cart op addresses it by, and what a storefront persists against the browser session.
 * @param items Items are the cart's lines, in the order they were added.
 * @param lineTotalCents LineTotalCents is the sum of the lines before any discount, in whole cents.
 * @param order Order is the order this cart became, once checkout completed it. Empty until then, and its presence is what makes a cart final.
 * @param shippingCents ShippingCents is the shipping charge, in whole cents. It stays zero until a shipping option is priced at checkout.
 * @param status Status is \"active\" for a cart still being filled, \"ordered\" once checkout turned it into an order, and \"discarded\" when the shopper abandoned it.
 * @param store Store is the storefront the cart is being filled on.
 * @param subtotalCents SubtotalCents is LineTotalCents less DiscountCents, in whole cents.
 * @param taxCents TaxCents is the sales tax, in whole cents. It stays zero until checkout resolves the shopper's tax region.
 * @param totalCents TotalCents is what the shopper pays: subtotal plus shipping plus tax, in whole cents.
 * @param updatedAt UpdatedAt is when the cart was last amended, RFC3339.
 * @param user User is the signed-in shopper this cart belongs to, empty for a guest cart.
 */


data class Cart (

    /* CreatedAt is when the cart was opened, RFC3339. */
    @SerializedName("createdAt")
    val createdAt: kotlin.String? = null,

    /* Currency is the ISO 4217 code every amount below is denominated in. */
    @SerializedName("currency")
    val currency: kotlin.String? = null,

    /* DiscountCents is what coupons and promotions took off, in whole cents. */
    @SerializedName("discountCents")
    val discountCents: kotlin.Int? = null,

    /* Email is the shopper's address, when the cart carries one. */
    @SerializedName("email")
    val email: kotlin.String? = null,

    /* ID is the cart's id — what every other cart op addresses it by, and what a storefront persists against the browser session. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Items are the cart's lines, in the order they were added. */
    @SerializedName("items")
    val items: kotlin.collections.List<CartItem>? = null,

    /* LineTotalCents is the sum of the lines before any discount, in whole cents. */
    @SerializedName("lineTotalCents")
    val lineTotalCents: kotlin.Int? = null,

    /* Order is the order this cart became, once checkout completed it. Empty until then, and its presence is what makes a cart final. */
    @SerializedName("order")
    val order: kotlin.String? = null,

    /* ShippingCents is the shipping charge, in whole cents. It stays zero until a shipping option is priced at checkout. */
    @SerializedName("shippingCents")
    val shippingCents: kotlin.Int? = null,

    /* Status is \"active\" for a cart still being filled, \"ordered\" once checkout turned it into an order, and \"discarded\" when the shopper abandoned it. */
    @SerializedName("status")
    val status: kotlin.String? = null,

    /* Store is the storefront the cart is being filled on. */
    @SerializedName("store")
    val store: kotlin.String? = null,

    /* SubtotalCents is LineTotalCents less DiscountCents, in whole cents. */
    @SerializedName("subtotalCents")
    val subtotalCents: kotlin.Int? = null,

    /* TaxCents is the sales tax, in whole cents. It stays zero until checkout resolves the shopper's tax region. */
    @SerializedName("taxCents")
    val taxCents: kotlin.Int? = null,

    /* TotalCents is what the shopper pays: subtotal plus shipping plus tax, in whole cents. */
    @SerializedName("totalCents")
    val totalCents: kotlin.Int? = null,

    /* UpdatedAt is when the cart was last amended, RFC3339. */
    @SerializedName("updatedAt")
    val updatedAt: kotlin.String? = null,

    /* User is the signed-in shopper this cart belongs to, empty for a guest cart. */
    @SerializedName("user")
    val user: kotlin.String? = null

) {


}

