/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.ChannelMetric

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param abTest 
 * @param available Available is false when the analytics warehouse is not connected or the query failed: the funnel below is then zero because nothing could be read, not because nothing happened. Spend and Channels are still real — they come from the connectors, not the warehouse.
 * @param cac CAC is customer acquisition cost: spend DOLLARS per conversion, rounded to cents. 0 when nothing converted — that is \"not yet computable\", not \"free\".
 * @param campaignId CampaignID is the campaign these results are for, echoed from the request.
 * @param channels Channels is the per-channel spend breakdown that SpendCents sums, one row per channel on the campaign including the ones that never launched.
 * @param clicks Clicks is the campaign's click events over the window.
 * @param conversions Conversions is the terminal funnel events attributed to the campaign — orders completed, signups completed, explicit conversion events.
 * @param ctr CTR is clicks per impression, a fraction rounded to 4 places (0.0123 = 1.23%), not a percentage. 0 when there were no impressions to divide by.
 * @param cvr CVR is conversions per click, a fraction rounded to 4 places. 0 when there were no clicks.
 * @param end End is the window's end, RFC3339 UTC — the read's own clock unless an explicit pair was given. The window is a LOOKBACK, not the campaign's own lifetime.
 * @param impressions Impressions is how many times the campaign's creatives were shown, counted from its utm_campaign-tagged impression events.
 * @param name Name is the campaign's display name at read time, so a result can be labelled without a second fetch.
 * @param range Range is the window actually used: 24h, 7d, 30d, 90d, or \"custom\" when an explicit start/end pair was honored. An unparseable or absent range reads 30d, so this is the value to trust, not the one that was sent.
 * @param revenue Revenue is the summed revenue attribute of the campaign's events, in whole CURRENCY UNITS (dollars) — the one money value here that is not in cents.
 * @param roas ROAS is return on ad spend: revenue per spend DOLLAR, rounded to 2 places (2.5 = $2.50 back per $1). 0 when nothing was spent.
 * @param source Source names the analytics table the funnel was read from, so an operator can see exactly what was counted. Set even when Available is false.
 * @param spendCents SpendCents is the campaign's total spend in CENTS: the sum of what each live channel's provider reports. A channel whose spend could not be read contributes 0 and says so on its own row.
 * @param start Start is the window's inclusive start, RFC3339 UTC.
 * @param status Status is the campaign's lifecycle state at read time — draft, live, paused, completed or failed. A draft has never run, so its funnel is legitimately zero.
 * @param visitors Visitors is how many distinct people the campaign reached, counted by event identity across ALL its events in the window — not a subset of Impressions, so it can exceed them for a campaign whose provider reports clicks but not views.
 */


data class CampaignResults (

    @SerializedName("abTest")
    val abTest: kotlin.Any? = null,

    /* Available is false when the analytics warehouse is not connected or the query failed: the funnel below is then zero because nothing could be read, not because nothing happened. Spend and Channels are still real — they come from the connectors, not the warehouse. */
    @SerializedName("available")
    val available: kotlin.Boolean? = null,

    /* CAC is customer acquisition cost: spend DOLLARS per conversion, rounded to cents. 0 when nothing converted — that is \"not yet computable\", not \"free\". */
    @SerializedName("cac")
    val cac: java.math.BigDecimal? = null,

    /* CampaignID is the campaign these results are for, echoed from the request. */
    @SerializedName("campaignId")
    val campaignId: kotlin.String? = null,

    /* Channels is the per-channel spend breakdown that SpendCents sums, one row per channel on the campaign including the ones that never launched. */
    @SerializedName("channels")
    val channels: kotlin.collections.List<ChannelMetric>? = null,

    /* Clicks is the campaign's click events over the window. */
    @SerializedName("clicks")
    val clicks: kotlin.Int? = null,

    /* Conversions is the terminal funnel events attributed to the campaign — orders completed, signups completed, explicit conversion events. */
    @SerializedName("conversions")
    val conversions: kotlin.Int? = null,

    /* CTR is clicks per impression, a fraction rounded to 4 places (0.0123 = 1.23%), not a percentage. 0 when there were no impressions to divide by. */
    @SerializedName("ctr")
    val ctr: java.math.BigDecimal? = null,

    /* CVR is conversions per click, a fraction rounded to 4 places. 0 when there were no clicks. */
    @SerializedName("cvr")
    val cvr: java.math.BigDecimal? = null,

    /* End is the window's end, RFC3339 UTC — the read's own clock unless an explicit pair was given. The window is a LOOKBACK, not the campaign's own lifetime. */
    @SerializedName("end")
    val end: kotlin.String? = null,

    /* Impressions is how many times the campaign's creatives were shown, counted from its utm_campaign-tagged impression events. */
    @SerializedName("impressions")
    val impressions: kotlin.Int? = null,

    /* Name is the campaign's display name at read time, so a result can be labelled without a second fetch. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Range is the window actually used: 24h, 7d, 30d, 90d, or \"custom\" when an explicit start/end pair was honored. An unparseable or absent range reads 30d, so this is the value to trust, not the one that was sent. */
    @SerializedName("range")
    val range: kotlin.String? = null,

    /* Revenue is the summed revenue attribute of the campaign's events, in whole CURRENCY UNITS (dollars) — the one money value here that is not in cents. */
    @SerializedName("revenue")
    val revenue: java.math.BigDecimal? = null,

    /* ROAS is return on ad spend: revenue per spend DOLLAR, rounded to 2 places (2.5 = $2.50 back per $1). 0 when nothing was spent. */
    @SerializedName("roas")
    val roas: java.math.BigDecimal? = null,

    /* Source names the analytics table the funnel was read from, so an operator can see exactly what was counted. Set even when Available is false. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* SpendCents is the campaign's total spend in CENTS: the sum of what each live channel's provider reports. A channel whose spend could not be read contributes 0 and says so on its own row. */
    @SerializedName("spendCents")
    val spendCents: kotlin.Int? = null,

    /* Start is the window's inclusive start, RFC3339 UTC. */
    @SerializedName("start")
    val start: kotlin.String? = null,

    /* Status is the campaign's lifecycle state at read time — draft, live, paused, completed or failed. A draft has never run, so its funnel is legitimately zero. */
    @SerializedName("status")
    val status: kotlin.String? = null,

    /* Visitors is how many distinct people the campaign reached, counted by event identity across ALL its events in the window — not a subset of Impressions, so it can exceed them for a campaign whose provider reports clicks but not views. */
    @SerializedName("visitors")
    val visitors: kotlin.Int? = null

) {


}

