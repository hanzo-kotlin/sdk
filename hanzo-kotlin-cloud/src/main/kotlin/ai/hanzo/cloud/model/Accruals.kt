/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accrued Accrued is how many NEW commission accruals this run created, counted across every upline level. The accrual is latched at most once per (affiliate, source org, period), so a re-run inside the same month reports 0 having changed nothing — 0 means \"already accrued\", not \"failed\".
 * @param royaltiesAccrued RoyaltiesAccrued is how many OSS-author royalty accruals the SAME spend read produced in the sibling authors program. One read drives both.
 * @param royaltyFailures RoyaltyFailures is reported, not swallowed: a sweep that could not reach the royalty store must not read as one that found nothing owed. The count was already computed and then dropped on the floor, which is the same silence the typed leg was added to end.
 * @param swept Swept is how many source (referred) orgs the run visited, bounded at 500 per run. A source with no spend this period, or one whose spend could not be read, still counts as swept.
 */


data class Accruals (

    /* Accrued is how many NEW commission accruals this run created, counted across every upline level. The accrual is latched at most once per (affiliate, source org, period), so a re-run inside the same month reports 0 having changed nothing — 0 means \"already accrued\", not \"failed\". */
    @SerializedName("accrued")
    val accrued: kotlin.Int? = null,

    /* RoyaltiesAccrued is how many OSS-author royalty accruals the SAME spend read produced in the sibling authors program. One read drives both. */
    @SerializedName("royaltiesAccrued")
    val royaltiesAccrued: kotlin.Int? = null,

    /* RoyaltyFailures is reported, not swallowed: a sweep that could not reach the royalty store must not read as one that found nothing owed. The count was already computed and then dropped on the floor, which is the same silence the typed leg was added to end. */
    @SerializedName("royaltyFailures")
    val royaltyFailures: kotlin.Int? = null,

    /* Swept is how many source (referred) orgs the run visited, bounded at 500 per run. A source with no spend this period, or one whose spend could not be read, still counts as swept. */
    @SerializedName("swept")
    val swept: kotlin.Int? = null

) {


}

