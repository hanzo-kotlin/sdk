/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.AppliedResource
import ai.hanzo.cloud.model.ReconcileSource

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param declared Declared is how many objects the rendered source declares — the denominator the three outcome counts below are read against. Zero means the render produced nothing, which trips the prune fuse rather than sweeping the fleet.
 * @param failed Failed is how many objects the apply could not reconcile. Non-zero is a PARTIAL run reported at 200: the engine applied what it could and each failure names itself in Results, so a caller reads this number rather than the status code to learn whether the fleet matches the source.
 * @param instance Instance is the tracking id this run stamps on everything it manages, so a later run can tell the objects it owns from objects another instance declares. DEPLOY_ENGINE_INSTANCE names it; the default is `universe`.
 * @param prune Prune reports whether DELETION was enabled for this run. False means an object the source no longer declares was left alone rather than removed, so a zero Pruned below means \"nothing to delete\" only when this is true.
 * @param pruned Pruned is how many live objects this run DELETED because the source no longer declares them. Always 0 when Prune is false.
 * @param results Results is one entry per object the run acted on, in the order the engine applied them. Empty (never null) when the run reconciled nothing.
 * @param revision Revision is the source commit this run applied, as the source resolved it — a git commit SHA, not an image tag. It is what an operator cites when asking what the cluster was last made to match.
 * @param source Source is the git coordinate the run rendered. It is this deployment's own configuration echoed back, never a request parameter, and it is reported so a reader of the answer knows WHICH tree the revision names.
 * @param synced Synced is how many objects the run applied successfully.
 */


data class ReconcileReport (

    /* Declared is how many objects the rendered source declares — the denominator the three outcome counts below are read against. Zero means the render produced nothing, which trips the prune fuse rather than sweeping the fleet. */
    @SerializedName("declared")
    val declared: kotlin.Int? = null,

    /* Failed is how many objects the apply could not reconcile. Non-zero is a PARTIAL run reported at 200: the engine applied what it could and each failure names itself in Results, so a caller reads this number rather than the status code to learn whether the fleet matches the source. */
    @SerializedName("failed")
    val failed: kotlin.Int? = null,

    /* Instance is the tracking id this run stamps on everything it manages, so a later run can tell the objects it owns from objects another instance declares. DEPLOY_ENGINE_INSTANCE names it; the default is `universe`. */
    @SerializedName("instance")
    val instance: kotlin.String? = null,

    /* Prune reports whether DELETION was enabled for this run. False means an object the source no longer declares was left alone rather than removed, so a zero Pruned below means \"nothing to delete\" only when this is true. */
    @SerializedName("prune")
    val prune: kotlin.Boolean? = null,

    /* Pruned is how many live objects this run DELETED because the source no longer declares them. Always 0 when Prune is false. */
    @SerializedName("pruned")
    val pruned: kotlin.Int? = null,

    /* Results is one entry per object the run acted on, in the order the engine applied them. Empty (never null) when the run reconciled nothing. */
    @SerializedName("results")
    val results: kotlin.collections.List<AppliedResource>? = null,

    /* Revision is the source commit this run applied, as the source resolved it — a git commit SHA, not an image tag. It is what an operator cites when asking what the cluster was last made to match. */
    @SerializedName("revision")
    val revision: kotlin.String? = null,

    /* Source is the git coordinate the run rendered. It is this deployment's own configuration echoed back, never a request parameter, and it is reported so a reader of the answer knows WHICH tree the revision names. */
    @SerializedName("source")
    val source: ReconcileSource? = null,

    /* Synced is how many objects the run applied successfully. */
    @SerializedName("synced")
    val synced: kotlin.Int? = null

) {


}

