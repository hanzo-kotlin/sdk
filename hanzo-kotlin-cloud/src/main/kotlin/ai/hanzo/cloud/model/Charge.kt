/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param amountCents AmountCents is what this line costs.
 * @param asOf AsOf is when a pass-through amount was last checked against its source.
 * @param code Code names the line so a caller can branch on it without reading prose.
 * @param label Label is what the payer sees on the invoice.
 * @param passThrough PassThrough marks money we collect and remit rather than keep — the state's fee is not our revenue, and a quote that hides that is a quote that reads as a bigger margin than it is.
 * @param recurring Recurring marks a line that repeats. An agent of record is billed every year for as long as the entity stands, and a payer agreeing to a one-time total is not agreeing to that.
 * @param source Source names who publishes this amount, for a line we merely pass through. Empty for a price of ours, which needs no external authority.
 * @param stale Stale reports that AsOf is older than the review window — the figure may have moved and nobody has looked. It does not block; it tells.
 */


data class Charge (

    /* AmountCents is what this line costs. */
    @SerializedName("amountCents")
    val amountCents: kotlin.Int? = null,

    /* AsOf is when a pass-through amount was last checked against its source. */
    @SerializedName("asOf")
    val asOf: kotlin.String? = null,

    /* Code names the line so a caller can branch on it without reading prose. */
    @SerializedName("code")
    val code: kotlin.String? = null,

    /* Label is what the payer sees on the invoice. */
    @SerializedName("label")
    val label: kotlin.String? = null,

    /* PassThrough marks money we collect and remit rather than keep — the state's fee is not our revenue, and a quote that hides that is a quote that reads as a bigger margin than it is. */
    @SerializedName("passThrough")
    val passThrough: kotlin.Boolean? = null,

    /* Recurring marks a line that repeats. An agent of record is billed every year for as long as the entity stands, and a payer agreeing to a one-time total is not agreeing to that. */
    @SerializedName("recurring")
    val recurring: kotlin.String? = null,

    /* Source names who publishes this amount, for a line we merely pass through. Empty for a price of ours, which needs no external authority. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Stale reports that AsOf is older than the review window — the figure may have moved and nobody has looked. It does not block; it tells. */
    @SerializedName("stale")
    val stale: kotlin.Boolean? = null

) {


}

