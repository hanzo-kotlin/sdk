/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param configured Configured is whether the edge holds credentials to act at all. False means every purge is a no-op.
 * @param error Error is the blocker, so an operator reads it instead of guessing at it.
 * @param freshness Freshness says, in one phrase, how long after a publish a reader sees it. It is the sentence an operator actually wants; the booleans above are how a machine reads the same fact.
 * @param policy Policy is the Cache-Control this edge serves each class of object with. It is DERIVED from the one canonical function, never a second copy: half the confusion when a publish looks stale is not knowing what the TTLs are, and reading them out of the source is not something an operator should have to do to answer \"how long until this is live\".
 * @param provider Provider is the CDN behind this edge, or \"none\". It is the first thing an operator wants and the only vendor name this API returns.
 * @param reach Reach is the apexes a publish is invalidated on. A site is served on more than one — the site plane's own and the first-party apex — and a purge that covers one of them looks identical from here to a purge that covers both.
 * @param status Status is \"ok\" when a publish reaches readers immediately, else \"degraded\".
 */


data class EdgeState (

    /* Configured is whether the edge holds credentials to act at all. False means every purge is a no-op. */
    @SerializedName("configured")
    val configured: kotlin.Boolean? = null,

    /* Error is the blocker, so an operator reads it instead of guessing at it. */
    @SerializedName("error")
    val error: kotlin.String? = null,

    /* Freshness says, in one phrase, how long after a publish a reader sees it. It is the sentence an operator actually wants; the booleans above are how a machine reads the same fact. */
    @SerializedName("freshness")
    val freshness: kotlin.String? = null,

    /* Policy is the Cache-Control this edge serves each class of object with. It is DERIVED from the one canonical function, never a second copy: half the confusion when a publish looks stale is not knowing what the TTLs are, and reading them out of the source is not something an operator should have to do to answer \"how long until this is live\". */
    @SerializedName("policy")
    val policy: kotlin.collections.Map<kotlin.String, kotlin.String>? = null,

    /* Provider is the CDN behind this edge, or \"none\". It is the first thing an operator wants and the only vendor name this API returns. */
    @SerializedName("provider")
    val provider: kotlin.String? = null,

    /* Reach is the apexes a publish is invalidated on. A site is served on more than one — the site plane's own and the first-party apex — and a purge that covers one of them looks identical from here to a purge that covers both. */
    @SerializedName("reach")
    val reach: kotlin.collections.List<kotlin.String>? = null,

    /* Status is \"ok\" when a publish reaches readers immediately, else \"degraded\". */
    @SerializedName("status")
    val status: kotlin.String? = null

) {


}

