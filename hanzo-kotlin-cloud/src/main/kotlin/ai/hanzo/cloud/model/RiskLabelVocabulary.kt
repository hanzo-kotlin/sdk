/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param dispositions Dispositions is the closed set a write's `disposition` must be drawn from, published in full so a caller can validate a batch before filing it instead of discovering a refusal per member: \"productive\", \"unproductive\", and \"\" — the EMPTY STRING is a member and means an explicit unjudged, so a client that filters empties out of this list drops a third of the vocabulary and can never file \"we looked and could not say\". They are the AML engine's own spelling, verbatim, which is what lets a replay there report against these values.
 * @param kinds Kinds, Dispositions and Sources are the closed vocabularies. A value outside them is refused at the door.
 * @param precedence Precedence is the sources in the order that resolves a conflict, strongest first. It is DERIVED from the same declaration the resolver reads, so the published order is the enforced order and cannot drift from it.
 * @param retention Retention is the platform floor in days: no tenant may dispose of a label younger than this, because a label can be the input to an adverse action.
 * @param rule Rule states the tie-breaks below rank, in order, so a caller reading a contested resolution can reproduce it.
 */


data class RiskLabelVocabulary (

    /* Dispositions is the closed set a write's `disposition` must be drawn from, published in full so a caller can validate a batch before filing it instead of discovering a refusal per member: \"productive\", \"unproductive\", and \"\" — the EMPTY STRING is a member and means an explicit unjudged, so a client that filters empties out of this list drops a third of the vocabulary and can never file \"we looked and could not say\". They are the AML engine's own spelling, verbatim, which is what lets a replay there report against these values. */
    @SerializedName("dispositions")
    val dispositions: kotlin.collections.List<kotlin.String>? = null,

    /* Kinds, Dispositions and Sources are the closed vocabularies. A value outside them is refused at the door. */
    @SerializedName("kinds")
    val kinds: kotlin.collections.List<kotlin.String>? = null,

    /* Precedence is the sources in the order that resolves a conflict, strongest first. It is DERIVED from the same declaration the resolver reads, so the published order is the enforced order and cannot drift from it. */
    @SerializedName("precedence")
    val precedence: kotlin.collections.List<kotlin.String>? = null,

    /* Retention is the platform floor in days: no tenant may dispose of a label younger than this, because a label can be the input to an adverse action. */
    @SerializedName("retention")
    val retention: kotlin.Int? = null,

    /* Rule states the tie-breaks below rank, in order, so a caller reading a contested resolution can reproduce it. */
    @SerializedName("rule")
    val rule: kotlin.collections.List<kotlin.String>? = null

) {


}

