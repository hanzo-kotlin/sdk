/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.WireFact

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param asOf AsOf is the instant this answer was taken at, RFC 3339: the one asked for, or the server's clock when none was.
 * @param conflicts Conflicts is every OTHER assertion knowable at AsOf, strongest first. They are not all disagreements: one that repeats the winner's value ranks below it and is listed here too.
 * @param contested Contested is true when at least one conflict claims a value different from the winner's. Any number of conflicts that all agree leaves it false.
 * @param entity Entity is the entity the question named, echoed so a stored answer still says what it is about.
 * @param known Known is false when this plane held nothing knowable at AsOf. That is an answer, not an error.
 * @param relation Relation is the relation the question named, echoed for the same reason.
 * @param truncated Truncated says this pair holds more assertions than one read returns, so the winner was decided from the most recent ceiling-full of them. It is reported because a provenance plane that trims silently is a plane that answers confidently and wrongly; narrow the question with as_of to see what it dropped.
 * @param winner Winner is the assertion in force — the strongest of those knowable at AsOf under the order `rule` names. Absent exactly when Known is false.
 */


data class GraphResolveOut (

    /* AsOf is the instant this answer was taken at, RFC 3339: the one asked for, or the server's clock when none was. */
    @SerializedName("as_of")
    val asOf: kotlin.String? = null,

    /* Conflicts is every OTHER assertion knowable at AsOf, strongest first. They are not all disagreements: one that repeats the winner's value ranks below it and is listed here too. */
    @SerializedName("conflicts")
    val conflicts: kotlin.collections.List<WireFact>? = null,

    /* Contested is true when at least one conflict claims a value different from the winner's. Any number of conflicts that all agree leaves it false. */
    @SerializedName("contested")
    val contested: kotlin.Boolean? = null,

    /* Entity is the entity the question named, echoed so a stored answer still says what it is about. */
    @SerializedName("entity")
    val entity: kotlin.String? = null,

    /* Known is false when this plane held nothing knowable at AsOf. That is an answer, not an error. */
    @SerializedName("known")
    val known: kotlin.Boolean? = null,

    /* Relation is the relation the question named, echoed for the same reason. */
    @SerializedName("relation")
    val relation: kotlin.String? = null,

    /* Truncated says this pair holds more assertions than one read returns, so the winner was decided from the most recent ceiling-full of them. It is reported because a provenance plane that trims silently is a plane that answers confidently and wrongly; narrow the question with as_of to see what it dropped. */
    @SerializedName("truncated")
    val truncated: kotlin.Boolean? = null,

    /* Winner is the assertion in force — the strongest of those knowable at AsOf under the order `rule` names. Absent exactly when Known is false. */
    @SerializedName("winner")
    val winner: WireFact? = null

) {


}

