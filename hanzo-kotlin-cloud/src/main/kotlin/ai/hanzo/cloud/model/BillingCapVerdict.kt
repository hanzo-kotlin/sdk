/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * The per-request spend-cap verdict for a (project, service) scope, evaluated over the org's spend-alert rows (most-restrictive-wins). Fails CLOSED: if a hard-enforceable row's spend cannot be computed the verdict denies. 
 *
 * @param allow Whether the request is permitted.
 * @param reason The cap/funds verdict reason. \"\" = allowed (funded and under every covering cap). `spend_cap` = funded but a HARD-enforced per-scope cap would be exceeded (the tenant's OWN policy ceiling). `insufficient_balance` = out of prepaid funds. `spend_cap` and `insufficient_balance` are DISTINCT — one is policy, one is funds. The `/spend-alerts/authorize` endpoint (a pure cap check) emits only \"\" or `spend_cap`; the cloud money-path composite (metering) adds `insufficient_balance`. 
 * @param capCents The tightest violated cap's ceiling in USD cents (0 when allowed).
 * @param spentCents Period spend for the scope in USD cents at decision time.
 * @param warnPct Utilization percent of the most-utilized covering cap when at/over its soft threshold (0 otherwise, and 0 on a deny).
 */


data class BillingCapVerdict (

    /* Whether the request is permitted. */
    @SerializedName("allow")
    val allow: kotlin.Boolean,

    /* The cap/funds verdict reason. \"\" = allowed (funded and under every covering cap). `spend_cap` = funded but a HARD-enforced per-scope cap would be exceeded (the tenant's OWN policy ceiling). `insufficient_balance` = out of prepaid funds. `spend_cap` and `insufficient_balance` are DISTINCT — one is policy, one is funds. The `/spend-alerts/authorize` endpoint (a pure cap check) emits only \"\" or `spend_cap`; the cloud money-path composite (metering) adds `insufficient_balance`.  */
    @SerializedName("reason")
    val reason: kotlin.String,

    /* The tightest violated cap's ceiling in USD cents (0 when allowed). */
    @SerializedName("capCents")
    val capCents: kotlin.Long? = null,

    /* Period spend for the scope in USD cents at decision time. */
    @SerializedName("spentCents")
    val spentCents: kotlin.Long? = null,

    /* Utilization percent of the most-utilized covering cap when at/over its soft threshold (0 otherwise, and 0 on a deny). */
    @SerializedName("warnPct")
    val warnPct: kotlin.Int? = null

) : kotlin.collections.HashMap<String, kotlin.Any>() {


}

