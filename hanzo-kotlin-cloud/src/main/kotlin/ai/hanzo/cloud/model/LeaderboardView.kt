/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.LeaderboardRow
import ai.hanzo.cloud.model.SelfRank

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param available Available is false when the usage warehouse is not connected or its rollup is not ready. Rows is then empty because nothing could be read — not because nobody used anything. Show that difference; never render an unavailable board as a real one.
 * @param end End is the EXCLUSIVE upper bound of the window, \"2006-01-02\" — the day after the last one counted. A board through today reports tomorrow's date here.
 * @param metric Metric echoes the value ranked: tokens|requests|cost.
 * @param period Period is the window's canonical label: day|week|month|all. The server resolves aliases (7d, 30d, today, …) to these, so this may differ from what was sent.
 * @param rows Rows are the ranked subjects, best first, at most the requested limit of them. Always a list, never null: an empty one means nothing was read, not an error.
 * @param scope Scope echoes the board that was served: personal|org|global.
 * @param self Self is the caller's own standing, reported even when they fall outside Rows. Absent when the caller's ledger identity cannot be resolved, or when the query behind it failed — never faked to keep the shape tidy.
 * @param source Source names the table these numbers were aggregated from (the derived daily rollup, hanzo.usage_rollup_daily), so an operator can tell exactly what was read.
 * @param start Start is the first day counted, \"2006-01-02\" inclusive. Empty for period=all, which has no lower bound at all.
 * @param subject Subject is what the rows stand for — \"user\" on a personal or org board, \"org\" on the global one. It tells a client whether Handle names a person or a company.
 * @param total Total is how many subjects were ranked in the window — the org's active users, or the active/opted-in orgs on the global board. It is the universe the ranks are out of, so it is normally larger than len(rows).
 */


data class LeaderboardView (

    /* Available is false when the usage warehouse is not connected or its rollup is not ready. Rows is then empty because nothing could be read — not because nobody used anything. Show that difference; never render an unavailable board as a real one. */
    @SerializedName("available")
    val available: kotlin.Boolean? = null,

    /* End is the EXCLUSIVE upper bound of the window, \"2006-01-02\" — the day after the last one counted. A board through today reports tomorrow's date here. */
    @SerializedName("end")
    val end: kotlin.String? = null,

    /* Metric echoes the value ranked: tokens|requests|cost. */
    @SerializedName("metric")
    val metric: kotlin.String? = null,

    /* Period is the window's canonical label: day|week|month|all. The server resolves aliases (7d, 30d, today, …) to these, so this may differ from what was sent. */
    @SerializedName("period")
    val period: kotlin.String? = null,

    /* Rows are the ranked subjects, best first, at most the requested limit of them. Always a list, never null: an empty one means nothing was read, not an error. */
    @SerializedName("rows")
    val rows: kotlin.collections.List<LeaderboardRow>? = null,

    /* Scope echoes the board that was served: personal|org|global. */
    @SerializedName("scope")
    val scope: kotlin.String? = null,

    /* Self is the caller's own standing, reported even when they fall outside Rows. Absent when the caller's ledger identity cannot be resolved, or when the query behind it failed — never faked to keep the shape tidy. */
    @SerializedName("self")
    val self: SelfRank? = null,

    /* Source names the table these numbers were aggregated from (the derived daily rollup, hanzo.usage_rollup_daily), so an operator can tell exactly what was read. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Start is the first day counted, \"2006-01-02\" inclusive. Empty for period=all, which has no lower bound at all. */
    @SerializedName("start")
    val start: kotlin.String? = null,

    /* Subject is what the rows stand for — \"user\" on a personal or org board, \"org\" on the global one. It tells a client whether Handle names a person or a company. */
    @SerializedName("subject")
    val subject: kotlin.String? = null,

    /* Total is how many subjects were ranked in the window — the org's active users, or the active/opted-in orgs on the global board. It is the universe the ranks are out of, so it is normally larger than len(rows). */
    @SerializedName("total")
    val total: kotlin.Int? = null

) {


}

