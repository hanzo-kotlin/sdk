/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param language Language narrows the search to a locale, BCP-47-ish (\"en\", \"ja\"). Empty means no narrowing.
 * @param maxSources MaxSources caps how many pages are read. Empty means the mode's own budget.
 * @param mode Mode is how much work to do: `search` (fast, one pass), `news` (recency biased), `research` (a plan and several rounds) or `deep` (the widest survey). Empty means research.
 * @param q Q is the question, in plain language. Required.
 * @param sources Sources narrows where the evidence comes from: any of `web`, `news`, `academic`, `github`, `reddit`, `x`. Each becomes a site-scoped search, so `[\"x\"]` researches X/Twitter posts rather than the open web.
 */


data class WebQuestion (

    /* Language narrows the search to a locale, BCP-47-ish (\"en\", \"ja\"). Empty means no narrowing. */
    @SerializedName("language")
    val language: kotlin.String? = null,

    /* MaxSources caps how many pages are read. Empty means the mode's own budget. */
    @SerializedName("max_sources")
    val maxSources: kotlin.Int? = null,

    /* Mode is how much work to do: `search` (fast, one pass), `news` (recency biased), `research` (a plan and several rounds) or `deep` (the widest survey). Empty means research. */
    @SerializedName("mode")
    val mode: kotlin.String? = null,

    /* Q is the question, in plain language. Required. */
    @SerializedName("q")
    val q: kotlin.String? = null,

    /* Sources narrows where the evidence comes from: any of `web`, `news`, `academic`, `github`, `reddit`, `x`. Each becomes a site-scoped search, so `[\"x\"]` researches X/Twitter posts rather than the open web. */
    @SerializedName("sources")
    val sources: kotlin.collections.List<kotlin.String>? = null

) {


}

