/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param balanceCents BalanceCents is the account balance AFTER the grant, in whole cents.
 * @param balanceExact BalanceExact is that same balance at full 18-decimal precision, so a sub-cent debit is visible rather than rounded away.
 * @param currency Currency is the lower-cased ISO code the grant was denominated in.
 * @param grantedCents GrantedCents is the amount actually credited.
 * @param org Org is the tenant whose ledger was credited.
 * @param source Source is the money bucket: \"trial\" (non-cash comp) or \"prepaid\" (real money).
 * @param subject Subject is the ACCOUNT the credit landed on inside that ledger: the org slug for a pooled org, \"<org>/<name>\" for a member of a per-member one. It is echoed because the operator does not choose it — account.Payer does — so naming a member of a pooled org credits the pool and the receipt has to say so.
 * @param transactionId TransactionID is the ledger entry id, for reconciliation against commerce.
 */


data class CloudGrantResult (

    /* BalanceCents is the account balance AFTER the grant, in whole cents. */
    @SerializedName("balanceCents")
    val balanceCents: kotlin.Int? = null,

    /* BalanceExact is that same balance at full 18-decimal precision, so a sub-cent debit is visible rather than rounded away. */
    @SerializedName("balanceExact")
    val balanceExact: kotlin.String? = null,

    /* Currency is the lower-cased ISO code the grant was denominated in. */
    @SerializedName("currency")
    val currency: kotlin.String? = null,

    /* GrantedCents is the amount actually credited. */
    @SerializedName("grantedCents")
    val grantedCents: kotlin.Int? = null,

    /* Org is the tenant whose ledger was credited. */
    @SerializedName("org")
    val org: kotlin.String? = null,

    /* Source is the money bucket: \"trial\" (non-cash comp) or \"prepaid\" (real money). */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Subject is the ACCOUNT the credit landed on inside that ledger: the org slug for a pooled org, \"<org>/<name>\" for a member of a per-member one. It is echoed because the operator does not choose it — account.Payer does — so naming a member of a pooled org credits the pool and the receipt has to say so. */
    @SerializedName("subject")
    val subject: kotlin.String? = null,

    /* TransactionID is the ledger entry id, for reconciliation against commerce. */
    @SerializedName("transactionId")
    val transactionId: kotlin.String? = null

) {


}

