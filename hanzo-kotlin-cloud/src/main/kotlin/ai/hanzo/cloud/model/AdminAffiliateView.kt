/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accruedCents AccruedCents is lifetime commission accrued, in cents. It only grows — a payout moves paidCents, never this.
 * @param approvedAt ApprovedAt is when staff approved, Unix seconds UTC. 0 means never approved.
 * @param code Code is the minted referral code, the slug the ?aff link carries. Empty until approval mints it. Codes are one global namespace across all affiliates.
 * @param createdAt CreatedAt is when the org applied, Unix seconds UTC.
 * @param id ID is the affiliate's server-minted handle, \"aff_\"-prefixed — the id the approve, suspend, rate and payout routes address.
 * @param org Org is the partner's own org slug. It appears ONLY on this cross-tenant admin view; no partner-facing read ever names another org.
 * @param paidCents PaidCents is lifetime commission already paid out, in cents — credits grants and record-only cash disbursements alike.
 * @param pendingCents PendingCents is accrued minus paid, in cents: what is still owed, and the hard ceiling the next payout is reserved against. Never negative.
 * @param rateBps RateBps is this affiliate's DIRECT (level 1) commission rate in basis points OF Hanzo's margin (2000 = 20% of margin, never of the customer's bill). Levels 2 and 3 are platform-wide switches and are not carried per affiliate.
 * @param referredCount ReferredCount is how many orgs this affiliate is the DIRECT referrer of, counted from the attribution edges. It is 0 on the single-affiliate answers (approve, suspend, rate, payout), which do not run the count.
 * @param requestedCode RequestedCode is the vanity code the applicant asked for. A request, not an allocation: approval mints a different slug if this one was taken. Absent when none was asked for.
 * @param status Status is \"applied\", \"approved\" or \"suspended\". Only \"approved\" resolves for attribution and accrues; \"suspended\" stops future earning and claws nothing back.
 * @param suspendedAt SuspendedAt is when staff suspended, Unix seconds UTC. 0 means never suspended; it is not cleared by a later re-approval.
 */


data class AdminAffiliateView (

    /* AccruedCents is lifetime commission accrued, in cents. It only grows — a payout moves paidCents, never this. */
    @SerializedName("accruedCents")
    val accruedCents: kotlin.Int? = null,

    /* ApprovedAt is when staff approved, Unix seconds UTC. 0 means never approved. */
    @SerializedName("approvedAt")
    val approvedAt: kotlin.Int? = null,

    /* Code is the minted referral code, the slug the ?aff link carries. Empty until approval mints it. Codes are one global namespace across all affiliates. */
    @SerializedName("code")
    val code: kotlin.String? = null,

    /* CreatedAt is when the org applied, Unix seconds UTC. */
    @SerializedName("createdAt")
    val createdAt: kotlin.Int? = null,

    /* ID is the affiliate's server-minted handle, \"aff_\"-prefixed — the id the approve, suspend, rate and payout routes address. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Org is the partner's own org slug. It appears ONLY on this cross-tenant admin view; no partner-facing read ever names another org. */
    @SerializedName("org")
    val org: kotlin.String? = null,

    /* PaidCents is lifetime commission already paid out, in cents — credits grants and record-only cash disbursements alike. */
    @SerializedName("paidCents")
    val paidCents: kotlin.Int? = null,

    /* PendingCents is accrued minus paid, in cents: what is still owed, and the hard ceiling the next payout is reserved against. Never negative. */
    @SerializedName("pendingCents")
    val pendingCents: kotlin.Int? = null,

    /* RateBps is this affiliate's DIRECT (level 1) commission rate in basis points OF Hanzo's margin (2000 = 20% of margin, never of the customer's bill). Levels 2 and 3 are platform-wide switches and are not carried per affiliate. */
    @SerializedName("rateBps")
    val rateBps: kotlin.Int? = null,

    /* ReferredCount is how many orgs this affiliate is the DIRECT referrer of, counted from the attribution edges. It is 0 on the single-affiliate answers (approve, suspend, rate, payout), which do not run the count. */
    @SerializedName("referredCount")
    val referredCount: kotlin.Int? = null,

    /* RequestedCode is the vanity code the applicant asked for. A request, not an allocation: approval mints a different slug if this one was taken. Absent when none was asked for. */
    @SerializedName("requestedCode")
    val requestedCode: kotlin.String? = null,

    /* Status is \"applied\", \"approved\" or \"suspended\". Only \"approved\" resolves for attribution and accrues; \"suspended\" stops future earning and claws nothing back. */
    @SerializedName("status")
    val status: kotlin.String? = null,

    /* SuspendedAt is when staff suspended, Unix seconds UTC. 0 means never suspended; it is not cleared by a later re-approval. */
    @SerializedName("suspendedAt")
    val suspendedAt: kotlin.Int? = null

) {


}

