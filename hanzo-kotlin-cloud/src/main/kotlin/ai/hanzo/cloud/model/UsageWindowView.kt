/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param account Account is the linked provider account the window belongs to.
 * @param cachedInputTokens CachedInputTokens is the prompt tokens served from the provider's cache; omitted when unknown.
 * @param confidence Confidence says how much the counters beside it mean — a meter that reported only a percentage leaves them at zero, and this is how a reader tells that from a true zero.
 * @param costCents CostCents is what the window cost on the PROVIDER's own plan, in US cents. It is not a Hanzo charge.
 * @param costLimitCents CostLimitCents is the plan's spend ceiling for the window, in US cents.
 * @param currency Currency is the provider's currency when it is not US cents.
 * @param inputTokens InputTokens is prompt tokens consumed in the window; omitted when unknown.
 * @param lane Lane is the meter lane this instance belongs to, e.g. a provider's own rolling-window meter.
 * @param machine Machine is the host whose meter reported the window.
 * @param outputTokens OutputTokens is completion tokens produced in the window; omitted when unknown.
 * @param plan Plan is the subscription plan the account is on, as the provider names it.
 * @param requests Requests is how many requests were made in the window; omitted when the meter did not report it.
 * @param resetsAt ResetsAt is when this window rolls over, RFC3339 UTC; omitted when unknown.
 * @param synthetic Synthetic marks an instance the meter inferred rather than read.
 * @param totalTokens TotalTokens is the window's total tokens; omitted when unknown.
 * @param usedPct UsedPct is how much of the window's allowance is consumed, 0–100.
 * @param window Window is the window class: 6h, day, week or month.
 * @param windowMinutes WindowMinutes is the window's real length in minutes when the meter reported one; omitted when it did not.
 * @param windowStart WindowStart is when this window opened, RFC3339 UTC; omitted when unknown.
 */


data class UsageWindowView (

    /* Account is the linked provider account the window belongs to. */
    @SerializedName("account")
    val account: kotlin.String? = null,

    /* CachedInputTokens is the prompt tokens served from the provider's cache; omitted when unknown. */
    @SerializedName("cachedInputTokens")
    val cachedInputTokens: kotlin.Int? = null,

    /* Confidence says how much the counters beside it mean — a meter that reported only a percentage leaves them at zero, and this is how a reader tells that from a true zero. */
    @SerializedName("confidence")
    val confidence: kotlin.String? = null,

    /* CostCents is what the window cost on the PROVIDER's own plan, in US cents. It is not a Hanzo charge. */
    @SerializedName("costCents")
    val costCents: kotlin.Int? = null,

    /* CostLimitCents is the plan's spend ceiling for the window, in US cents. */
    @SerializedName("costLimitCents")
    val costLimitCents: kotlin.Int? = null,

    /* Currency is the provider's currency when it is not US cents. */
    @SerializedName("currency")
    val currency: kotlin.String? = null,

    /* InputTokens is prompt tokens consumed in the window; omitted when unknown. */
    @SerializedName("inputTokens")
    val inputTokens: kotlin.Int? = null,

    /* Lane is the meter lane this instance belongs to, e.g. a provider's own rolling-window meter. */
    @SerializedName("lane")
    val lane: kotlin.String? = null,

    /* Machine is the host whose meter reported the window. */
    @SerializedName("machine")
    val machine: kotlin.String? = null,

    /* OutputTokens is completion tokens produced in the window; omitted when unknown. */
    @SerializedName("outputTokens")
    val outputTokens: kotlin.Int? = null,

    /* Plan is the subscription plan the account is on, as the provider names it. */
    @SerializedName("plan")
    val plan: kotlin.String? = null,

    /* Requests is how many requests were made in the window; omitted when the meter did not report it. */
    @SerializedName("requests")
    val requests: kotlin.Int? = null,

    /* ResetsAt is when this window rolls over, RFC3339 UTC; omitted when unknown. */
    @SerializedName("resetsAt")
    val resetsAt: kotlin.String? = null,

    /* Synthetic marks an instance the meter inferred rather than read. */
    @SerializedName("synthetic")
    val synthetic: kotlin.Boolean? = null,

    /* TotalTokens is the window's total tokens; omitted when unknown. */
    @SerializedName("totalTokens")
    val totalTokens: kotlin.Int? = null,

    /* UsedPct is how much of the window's allowance is consumed, 0–100. */
    @SerializedName("usedPct")
    val usedPct: java.math.BigDecimal? = null,

    /* Window is the window class: 6h, day, week or month. */
    @SerializedName("window")
    val window: kotlin.String? = null,

    /* WindowMinutes is the window's real length in minutes when the meter reported one; omitted when it did not. */
    @SerializedName("windowMinutes")
    val windowMinutes: kotlin.Int? = null,

    /* WindowStart is when this window opened, RFC3339 UTC; omitted when unknown. */
    @SerializedName("windowStart")
    val windowStart: kotlin.String? = null

) {


}

