/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.DeviceSignals

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param product Product is the licensed commerce product the caller wants a token for.
 * @param fingerprint Fingerprint is a previously-registered device binding value, as returned by POST /v1/licensing/fingerprint. Leave it empty and pass Signals to bind the device at issue time instead.
 * @param holder Holder overrides who the token is issued to; defaults to the caller's own validated subject. It NAMES the token's bearer and grants nothing on its own — the entitlement checked is always the caller's org's.
 * @param release Release scopes the token to one signed binary release, recorded as a \"release:<id>\" feature so a single bad release can be revoked on its own.
 * @param signals Signals binds the device at issue time, as an alternative to a pre-registered fingerprint. The raw signals are never stored or echoed — they are folded immediately into the one-way binding value.
 * @param ttlSeconds TTLSeconds requests a token lifetime in seconds. It is clamped to the deployment maximum AND to the entitlement's own expiry — a token never outlives the subscription that paid for it.
 */


data class IssueRequest (

    /* Product is the licensed commerce product the caller wants a token for. */
    @SerializedName("product")
    val product: kotlin.String,

    /* Fingerprint is a previously-registered device binding value, as returned by POST /v1/licensing/fingerprint. Leave it empty and pass Signals to bind the device at issue time instead. */
    @SerializedName("fingerprint")
    val fingerprint: kotlin.String? = null,

    /* Holder overrides who the token is issued to; defaults to the caller's own validated subject. It NAMES the token's bearer and grants nothing on its own — the entitlement checked is always the caller's org's. */
    @SerializedName("holder")
    val holder: kotlin.String? = null,

    /* Release scopes the token to one signed binary release, recorded as a \"release:<id>\" feature so a single bad release can be revoked on its own. */
    @SerializedName("release")
    val release: kotlin.String? = null,

    /* Signals binds the device at issue time, as an alternative to a pre-registered fingerprint. The raw signals are never stored or echoed — they are folded immediately into the one-way binding value. */
    @SerializedName("signals")
    val signals: DeviceSignals? = null,

    /* TTLSeconds requests a token lifetime in seconds. It is clamped to the deployment maximum AND to the entitlement's own expiry — a token never outlives the subscription that paid for it. */
    @SerializedName("ttl_seconds")
    val ttlSeconds: kotlin.Int? = null

) {


}

