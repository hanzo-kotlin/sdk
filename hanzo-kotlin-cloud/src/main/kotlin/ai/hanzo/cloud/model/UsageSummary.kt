/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.Accounts
import ai.hanzo.cloud.model.LLM
import ai.hanzo.cloud.model.Sources
import ai.hanzo.cloud.model.Spend
import ai.hanzo.cloud.model.UsageScope

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accounts Accounts is the caller's own linked provider accounts beside the org's Hanzo-routed usage, labelled row by row and never summed together.
 * @param end End is the window's exclusive end, RFC3339 UTC.
 * @param interval Interval is the bucket width the spend series is gap-filled at.
 * @param llm LLM is the org's Hanzo-routed inference totals from the warehouse.
 * @param range Range is the window label that was served.
 * @param scope Scope is the tenant and subject the roll-up was answered for.
 * @param sources Sources says which upstreams actually answered, so a zero can be read as \"no data yet\" rather than as a measurement.
 * @param spend Spend is the categorized cost roll-up from the billing ledger.
 * @param start Start is the window's inclusive start, RFC3339 UTC.
 */


data class UsageSummary (

    /* Accounts is the caller's own linked provider accounts beside the org's Hanzo-routed usage, labelled row by row and never summed together. */
    @SerializedName("accounts")
    val accounts: Accounts? = null,

    /* End is the window's exclusive end, RFC3339 UTC. */
    @SerializedName("end")
    val end: kotlin.String? = null,

    /* Interval is the bucket width the spend series is gap-filled at. */
    @SerializedName("interval")
    val interval: kotlin.String? = null,

    /* LLM is the org's Hanzo-routed inference totals from the warehouse. */
    @SerializedName("llm")
    val llm: LLM? = null,

    /* Range is the window label that was served. */
    @SerializedName("range")
    val range: kotlin.String? = null,

    /* Scope is the tenant and subject the roll-up was answered for. */
    @SerializedName("scope")
    val scope: UsageScope? = null,

    /* Sources says which upstreams actually answered, so a zero can be read as \"no data yet\" rather than as a measurement. */
    @SerializedName("sources")
    val sources: Sources? = null,

    /* Spend is the categorized cost roll-up from the billing ledger. */
    @SerializedName("spend")
    val spend: Spend? = null,

    /* Start is the window's inclusive start, RFC3339 UTC. */
    @SerializedName("start")
    val start: kotlin.String? = null

) {


}

