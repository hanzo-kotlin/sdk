/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param blend Blend is how much of a closing window folds into the reference: 1 replaces it outright, less makes the reference expensive to move.
 * @param depth Depth is how deep each tree is. With Trees it sets how finely the space is partitioned, and therefore how much history it takes to fill.
 * @param family Family is the KIND of model this candidate is: `halfspace` is an ensemble of half-space trees whose masses are counters, and it is the family this search grid ranks. The parameters below are that family's own — a family that does not partition space with trees has different ones — so read them against this.
 * @param review Review is the appetite this shape was tried at.
 * @param trees Trees is how many half-space trees the ensemble holds.
 * @param window Window is how many events make one reference window.
 */


data class RiskTopology (

    /* Blend is how much of a closing window folds into the reference: 1 replaces it outright, less makes the reference expensive to move. */
    @SerializedName("blend")
    val blend: java.math.BigDecimal? = null,

    /* Depth is how deep each tree is. With Trees it sets how finely the space is partitioned, and therefore how much history it takes to fill. */
    @SerializedName("depth")
    val depth: kotlin.Int? = null,

    /* Family is the KIND of model this candidate is: `halfspace` is an ensemble of half-space trees whose masses are counters, and it is the family this search grid ranks. The parameters below are that family's own — a family that does not partition space with trees has different ones — so read them against this. */
    @SerializedName("family")
    val family: kotlin.String? = null,

    /* Review is the appetite this shape was tried at. */
    @SerializedName("review")
    val review: java.math.BigDecimal? = null,

    /* Trees is how many half-space trees the ensemble holds. */
    @SerializedName("trees")
    val trees: kotlin.Int? = null,

    /* Window is how many events make one reference window. */
    @SerializedName("window")
    val window: kotlin.Int? = null

) {


}

