/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.ProjectsUpdateRepo

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param cacheControl CacheControl replaces the Cache-Control policy the edge serves this site's HTML under. Absent leaves it.
 * @param description Description replaces the one-line summary. Absent leaves it.
 * @param framework Framework replaces the build hint. It affects the NEXT build only — nothing already deployed is rebuilt.
 * @param hidden Hidden is MODERATION, and the only admin-gated field on this body: it pulls a public project out of the catalogue from admin.hanzo.ai without editing the publisher's own visibility choice, so un-hiding restores exactly what they asked for. A tenant sending it is ignored.
 * @param hiddenReason HiddenReason records WHY moderation hid it, so the action can be explained and reviewed later. Admin-gated like hidden itself.
 * @param license License is the terms that upstream work carries, with the same clear-versus- leave rule.
 * @param name Name replaces the display name. Absent leaves it; the slug never moves with it.
 * @param repo 
 * @param slug Slug is the project to update, from the path. The URL is the addressing authority — a `slug` in the body cannot move the write to another project.
 * @param tags Tags sets the site's browser tag config: platform slug → non-secret pixel id (e.g. {\"ga4\":\"G-…\",\"meta\":\"…\"}). track.js injects these first-party and the server CAPI reads them, per site. Absent LEAVES them; a present object REPLACES the set (send {} to clear). The ids are public — they ship in the page — so this is not the SECRET path (a CAPI token is sealed via POST /v1/destination).
 * @param upstream Upstream credits the third-party work this project was published from, and is settable after the fact because the live demos are the ones that most need crediting. An explicit empty string CLEARS the credit; absent leaves it.
 * @param visibility Visibility flips an existing project between \"public\" and \"private\". Same ONE rule as at create: public is free, private needs a paid plan.
 */


data class ProjectsUpdate (

    /* CacheControl replaces the Cache-Control policy the edge serves this site's HTML under. Absent leaves it. */
    @SerializedName("cacheControl")
    val cacheControl: kotlin.String? = null,

    /* Description replaces the one-line summary. Absent leaves it. */
    @SerializedName("description")
    val description: kotlin.String? = null,

    /* Framework replaces the build hint. It affects the NEXT build only — nothing already deployed is rebuilt. */
    @SerializedName("framework")
    val framework: kotlin.String? = null,

    /* Hidden is MODERATION, and the only admin-gated field on this body: it pulls a public project out of the catalogue from admin.hanzo.ai without editing the publisher's own visibility choice, so un-hiding restores exactly what they asked for. A tenant sending it is ignored. */
    @SerializedName("hidden")
    val hidden: kotlin.Boolean? = null,

    /* HiddenReason records WHY moderation hid it, so the action can be explained and reviewed later. Admin-gated like hidden itself. */
    @SerializedName("hiddenReason")
    val hiddenReason: kotlin.String? = null,

    /* License is the terms that upstream work carries, with the same clear-versus- leave rule. */
    @SerializedName("license")
    val license: kotlin.String? = null,

    /* Name replaces the display name. Absent leaves it; the slug never moves with it. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    @SerializedName("repo")
    val repo: ProjectsUpdateRepo? = null,

    /* Slug is the project to update, from the path. The URL is the addressing authority — a `slug` in the body cannot move the write to another project. */
    @SerializedName("slug")
    val slug: kotlin.String? = null,

    /* Tags sets the site's browser tag config: platform slug → non-secret pixel id (e.g. {\"ga4\":\"G-…\",\"meta\":\"…\"}). track.js injects these first-party and the server CAPI reads them, per site. Absent LEAVES them; a present object REPLACES the set (send {} to clear). The ids are public — they ship in the page — so this is not the SECRET path (a CAPI token is sealed via POST /v1/destination). */
    @SerializedName("tags")
    val tags: kotlin.collections.Map<kotlin.String, kotlin.String>? = null,

    /* Upstream credits the third-party work this project was published from, and is settable after the fact because the live demos are the ones that most need crediting. An explicit empty string CLEARS the credit; absent leaves it. */
    @SerializedName("upstream")
    val upstream: kotlin.String? = null,

    /* Visibility flips an existing project between \"public\" and \"private\". Same ONE rule as at create: public is free, private needs a paid plan. */
    @SerializedName("visibility")
    val visibility: kotlin.String? = null

) {


}

