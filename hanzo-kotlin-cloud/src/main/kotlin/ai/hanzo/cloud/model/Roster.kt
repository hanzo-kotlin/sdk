/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.Market
import ai.hanzo.cloud.model.Reach

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param chains Chains is `[]` where the registry answered and named none, and `null` where it did not answer — never absent, because a missing key and an empty list read alike and only one of them means \"there are none\". Each row carries its OWN reach for its figures, so a chain whose indexer is down is one row saying so.
 * @param reach Reach is how far the read of the REGISTRY got. It governs the list: a registry that did not answer yields no rows, and the reason it did not is here rather than in an empty array a caller would read as \"no chains exist\".
 */


data class Roster (

    /* Chains is `[]` where the registry answered and named none, and `null` where it did not answer — never absent, because a missing key and an empty list read alike and only one of them means \"there are none\". Each row carries its OWN reach for its figures, so a chain whose indexer is down is one row saying so. */
    @SerializedName("chains")
    val chains: kotlin.collections.List<Market>? = null,

    /* Reach is how far the read of the REGISTRY got. It governs the list: a registry that did not answer yields no rows, and the reason it did not is here rather than in an empty array a caller would read as \"no chains exist\". */
    @SerializedName("reach")
    val reach: Reach? = null

) {


}

