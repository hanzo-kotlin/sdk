/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.CloudSampleReq

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param account Account is the linked account the window was metered from.
 * @param cachedInputTokens CachedInputTokens is the prompt tokens the provider served from cache.
 * @param confidence Confidence says how much the counters below mean.
 * @param costCents CostCents is what the window cost on the PROVIDER's own plan, in US cents.
 * @param costLimitCents CostLimitCents is the plan's spend ceiling for the window, in US cents.
 * @param currency Currency is the provider's currency when it is not US cents.
 * @param inputTokens InputTokens is prompt tokens consumed in the window.
 * @param kind Kind is subscription or apikey. Empty is accepted; anything else is refused.
 * @param lane Lane is the meter lane within the account.
 * @param machine Machine is the host whose meter read the window. Required on every sample.
 * @param outputTokens OutputTokens is completion tokens produced in the window.
 * @param plan Plan is the provider plan the account is on, e.g. a Claude Max plan.
 * @param provider Provider is the upstream the account belongs to, e.g. anthropic. Required on every sample.
 * @param requests Requests is how many requests the window covers.
 * @param resetsAt ResetsAt is when the measured window rolls over, RFC3339.
 * @param samples Samples is the batch form: every lane a poller measured, in one call. When it is non-empty the top-level sample fields are ignored.
 * @param synthetic Synthetic marks a window the meter inferred rather than read.
 * @param totalTokens TotalTokens is the window's total tokens.
 * @param usedPct UsedPct is how much of the window's allowance is consumed, 0–100.
 * @param window Window is the window class: 6h, day, week or month. Required, and a class this surface does not know is refused rather than rewritten.
 * @param windowMinutes WindowMinutes is the window's real length in minutes, as the meter reports it.
 * @param windowStart WindowStart is when the measured window opened, RFC3339. This is how a backfill states WHICH window it measured; the server always owns the observation clock, so there is no timestamp field.
 */


data class CloudReportReq (

    /* Account is the linked account the window was metered from. */
    @SerializedName("account")
    val account: kotlin.String? = null,

    /* CachedInputTokens is the prompt tokens the provider served from cache. */
    @SerializedName("cachedInputTokens")
    val cachedInputTokens: kotlin.Int? = null,

    /* Confidence says how much the counters below mean. */
    @SerializedName("confidence")
    val confidence: kotlin.String? = null,

    /* CostCents is what the window cost on the PROVIDER's own plan, in US cents. */
    @SerializedName("costCents")
    val costCents: kotlin.Int? = null,

    /* CostLimitCents is the plan's spend ceiling for the window, in US cents. */
    @SerializedName("costLimitCents")
    val costLimitCents: kotlin.Int? = null,

    /* Currency is the provider's currency when it is not US cents. */
    @SerializedName("currency")
    val currency: kotlin.String? = null,

    /* InputTokens is prompt tokens consumed in the window. */
    @SerializedName("inputTokens")
    val inputTokens: kotlin.Int? = null,

    /* Kind is subscription or apikey. Empty is accepted; anything else is refused. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Lane is the meter lane within the account. */
    @SerializedName("lane")
    val lane: kotlin.String? = null,

    /* Machine is the host whose meter read the window. Required on every sample. */
    @SerializedName("machine")
    val machine: kotlin.String? = null,

    /* OutputTokens is completion tokens produced in the window. */
    @SerializedName("outputTokens")
    val outputTokens: kotlin.Int? = null,

    /* Plan is the provider plan the account is on, e.g. a Claude Max plan. */
    @SerializedName("plan")
    val plan: kotlin.String? = null,

    /* Provider is the upstream the account belongs to, e.g. anthropic. Required on every sample. */
    @SerializedName("provider")
    val provider: kotlin.String? = null,

    /* Requests is how many requests the window covers. */
    @SerializedName("requests")
    val requests: kotlin.Int? = null,

    /* ResetsAt is when the measured window rolls over, RFC3339. */
    @SerializedName("resetsAt")
    val resetsAt: kotlin.String? = null,

    /* Samples is the batch form: every lane a poller measured, in one call. When it is non-empty the top-level sample fields are ignored. */
    @SerializedName("samples")
    val samples: kotlin.collections.List<CloudSampleReq>? = null,

    /* Synthetic marks a window the meter inferred rather than read. */
    @SerializedName("synthetic")
    val synthetic: kotlin.Boolean? = null,

    /* TotalTokens is the window's total tokens. */
    @SerializedName("totalTokens")
    val totalTokens: kotlin.Int? = null,

    /* UsedPct is how much of the window's allowance is consumed, 0–100. */
    @SerializedName("usedPct")
    val usedPct: java.math.BigDecimal? = null,

    /* Window is the window class: 6h, day, week or month. Required, and a class this surface does not know is refused rather than rewritten. */
    @SerializedName("window")
    val window: kotlin.String? = null,

    /* WindowMinutes is the window's real length in minutes, as the meter reports it. */
    @SerializedName("windowMinutes")
    val windowMinutes: kotlin.Int? = null,

    /* WindowStart is when the measured window opened, RFC3339. This is how a backfill states WHICH window it measured; the server always owns the observation clock, so there is no timestamp field. */
    @SerializedName("windowStart")
    val windowStart: kotlin.String? = null

) {


}

