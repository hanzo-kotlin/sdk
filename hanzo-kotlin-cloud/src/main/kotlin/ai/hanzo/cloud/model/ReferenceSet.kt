/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.ReferenceSource

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param age Age is how long ago that was.
 * @param asOf AsOf is when the OLDEST contributing publisher was current, RFC 3339. The oldest and not the newest: a set is exactly as fresh as its weakest source.
 * @param propertyKeys Keys is how many members the baseline carries.
 * @param kind Kind is how the baseline comes to exist: fetch (downloaded from a publisher), local (computed here), attest (held by the component that screens against it, freshness reported), or seam (declared and NOT held, because the source needs a licence we do not have).
 * @param match Match is how a key is tested: exact, domain, net, digits, pattern or range.
 * @param maxAge MaxAge is how old this set may be before it is stale.
 * @param overrides Overrides is how many entries YOUR org has laid over this baseline.
 * @param refusal Refusal names why the set cannot be relied on, when it cannot: never loaded, held elsewhere, or a licence we do not hold. Non-empty means a lookup against this set will not answer, rather than answering clean.
 * @param set Set is the name this set is addressed by.
 * @param sources Sources is each contributing publisher, its licence and its own freshness.
 * @param stale Stale is whether it is past that bound. A stale set still answers and says so, because yesterday's list beats none.
 * @param version Version is the exact baseline consulted — every contributing publisher and its content digest. A decision records this and an auditor resolves it back.
 * @param what What the set holds, in one sentence.
 */


data class ReferenceSet (

    /* Age is how long ago that was. */
    @SerializedName("age")
    val age: kotlin.String? = null,

    /* AsOf is when the OLDEST contributing publisher was current, RFC 3339. The oldest and not the newest: a set is exactly as fresh as its weakest source. */
    @SerializedName("asOf")
    val asOf: kotlin.String? = null,

    /* Keys is how many members the baseline carries. */
    @SerializedName("keys")
    val propertyKeys: kotlin.Int? = null,

    /* Kind is how the baseline comes to exist: fetch (downloaded from a publisher), local (computed here), attest (held by the component that screens against it, freshness reported), or seam (declared and NOT held, because the source needs a licence we do not have). */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Match is how a key is tested: exact, domain, net, digits, pattern or range. */
    @SerializedName("match")
    val match: kotlin.String? = null,

    /* MaxAge is how old this set may be before it is stale. */
    @SerializedName("maxAge")
    val maxAge: kotlin.String? = null,

    /* Overrides is how many entries YOUR org has laid over this baseline. */
    @SerializedName("overrides")
    val overrides: kotlin.Int? = null,

    /* Refusal names why the set cannot be relied on, when it cannot: never loaded, held elsewhere, or a licence we do not hold. Non-empty means a lookup against this set will not answer, rather than answering clean. */
    @SerializedName("refusal")
    val refusal: kotlin.String? = null,

    /* Set is the name this set is addressed by. */
    @SerializedName("set")
    val set: kotlin.String? = null,

    /* Sources is each contributing publisher, its licence and its own freshness. */
    @SerializedName("sources")
    val sources: kotlin.collections.List<ReferenceSource>? = null,

    /* Stale is whether it is past that bound. A stale set still answers and says so, because yesterday's list beats none. */
    @SerializedName("stale")
    val stale: kotlin.Boolean? = null,

    /* Version is the exact baseline consulted — every contributing publisher and its content digest. A decision records this and an auditor resolves it back. */
    @SerializedName("version")
    val version: kotlin.String? = null,

    /* What the set holds, in one sentence. */
    @SerializedName("what")
    val what: kotlin.String? = null

) {


}

