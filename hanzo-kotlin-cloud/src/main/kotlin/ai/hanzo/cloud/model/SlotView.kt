/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.RegistrationView

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param blsPubkey BLSPubkey is the node's BLS public key, hex.
 * @param crName CRName is the LuxNetwork custom resource that materializes the node.
 * @param createdAt CreatedAt is when the slot was first claimed, as a Unix timestamp.
 * @param namespace Namespace is the Kubernetes namespace the node's CR lives in.
 * @param network Network is the luxd network slug the node joins.
 * @param nodeID NodeID is the luxd node id derived from the sealed staking identity. It is stable across re-claims of the same slot.
 * @param nodeStatus NodeStatus is the provisioning state of the node: \"node_created\" once the CR is applied, \"node_pending\" when no cluster is reachable (the slot is still claimed and the keys are still sealed).
 * @param registration Registration is the queued owner-gated registration, absent until one exists.
 * @param slot Slot is the validator slot number — the same value as tokenId, under the name the portal reads.
 * @param tokenId TokenID is the GenesisNFT token id that IS this slot.
 * @param updatedAt UpdatedAt is when the slot last changed, as a Unix timestamp.
 * @param wallet Wallet is the lowercase Ethereum address that proved ownership of the NFT.
 */


data class SlotView (

    /* BLSPubkey is the node's BLS public key, hex. */
    @SerializedName("blsPubkey")
    val blsPubkey: kotlin.String? = null,

    /* CRName is the LuxNetwork custom resource that materializes the node. */
    @SerializedName("crName")
    val crName: kotlin.String? = null,

    /* CreatedAt is when the slot was first claimed, as a Unix timestamp. */
    @SerializedName("createdAt")
    val createdAt: kotlin.Int? = null,

    /* Namespace is the Kubernetes namespace the node's CR lives in. */
    @SerializedName("namespace")
    val namespace: kotlin.String? = null,

    /* Network is the luxd network slug the node joins. */
    @SerializedName("network")
    val network: kotlin.String? = null,

    /* NodeID is the luxd node id derived from the sealed staking identity. It is stable across re-claims of the same slot. */
    @SerializedName("nodeID")
    val nodeID: kotlin.String? = null,

    /* NodeStatus is the provisioning state of the node: \"node_created\" once the CR is applied, \"node_pending\" when no cluster is reachable (the slot is still claimed and the keys are still sealed). */
    @SerializedName("nodeStatus")
    val nodeStatus: kotlin.String? = null,

    /* Registration is the queued owner-gated registration, absent until one exists. */
    @SerializedName("registration")
    val registration: RegistrationView? = null,

    /* Slot is the validator slot number — the same value as tokenId, under the name the portal reads. */
    @SerializedName("slot")
    val slot: kotlin.Int? = null,

    /* TokenID is the GenesisNFT token id that IS this slot. */
    @SerializedName("tokenId")
    val tokenId: kotlin.Int? = null,

    /* UpdatedAt is when the slot last changed, as a Unix timestamp. */
    @SerializedName("updatedAt")
    val updatedAt: kotlin.Int? = null,

    /* Wallet is the lowercase Ethereum address that proved ownership of the NFT. */
    @SerializedName("wallet")
    val wallet: kotlin.String? = null

) {


}

