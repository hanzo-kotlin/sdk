/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.Remittance

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accruedCents AccruedCents is lifetime commission accrued, in cents.
 * @param code Code is the minted referral code; empty until staff approve.
 * @param defaultRateBps DefaultRateBps is the direct rate a new affiliate would get, answered only to a caller that has not applied.
 * @param handle Handle is the opt-in public leaderboard name; empty means opted out.
 * @param id ID is the affiliate's server-minted handle, \"aff_\"-prefixed — what staff approve, suspend, re-rate and pay against. Absent until the org applies.
 * @param isAffiliate IsAffiliate says whether the caller org has an affiliate record at all. It is the ONE field an org that never applied gets besides defaultRateBps: on false, read nothing else here — every other field is absent, not zero.
 * @param link Link is the shareable ?aff URL; empty until a code is minted.
 * @param marginBps MarginBps is the platform gross-margin fraction commission is a rate OF.
 * @param paidCents PaidCents is lifetime commission already paid out, in cents.
 * @param payouts Payouts is the payout history, newest rows bounded.
 * @param pendingCents PendingCents is accrued minus paid — what the platform still owes.
 * @param rateBps RateBps is the affiliate's own direct commission rate, in basis points.
 * @param referredCount ReferredCount is how many orgs this affiliate has referred.
 * @param requestedCode RequestedCode is the vanity code asked for at apply time — a request, not an allocation. Approval mints `code`, which may be a different slug if this one was already taken.
 * @param status Status is \"applied\", \"approved\" or \"suspended\". Only an approved affiliate has a code that resolves for attribution and accrues commission; suspended keeps what it already earned but stops earning more.
 */


data class AffiliateStanding (

    /* AccruedCents is lifetime commission accrued, in cents. */
    @SerializedName("accruedCents")
    val accruedCents: kotlin.Int? = null,

    /* Code is the minted referral code; empty until staff approve. */
    @SerializedName("code")
    val code: kotlin.String? = null,

    /* DefaultRateBps is the direct rate a new affiliate would get, answered only to a caller that has not applied. */
    @SerializedName("defaultRateBps")
    val defaultRateBps: kotlin.Int? = null,

    /* Handle is the opt-in public leaderboard name; empty means opted out. */
    @SerializedName("handle")
    val handle: kotlin.String? = null,

    /* ID is the affiliate's server-minted handle, \"aff_\"-prefixed — what staff approve, suspend, re-rate and pay against. Absent until the org applies. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* IsAffiliate says whether the caller org has an affiliate record at all. It is the ONE field an org that never applied gets besides defaultRateBps: on false, read nothing else here — every other field is absent, not zero. */
    @SerializedName("isAffiliate")
    val isAffiliate: kotlin.Boolean? = null,

    /* Link is the shareable ?aff URL; empty until a code is minted. */
    @SerializedName("link")
    val link: kotlin.String? = null,

    /* MarginBps is the platform gross-margin fraction commission is a rate OF. */
    @SerializedName("marginBps")
    val marginBps: kotlin.Int? = null,

    /* PaidCents is lifetime commission already paid out, in cents. */
    @SerializedName("paidCents")
    val paidCents: kotlin.Int? = null,

    /* Payouts is the payout history, newest rows bounded. */
    @SerializedName("payouts")
    val payouts: kotlin.collections.List<Remittance>? = null,

    /* PendingCents is accrued minus paid — what the platform still owes. */
    @SerializedName("pendingCents")
    val pendingCents: kotlin.Int? = null,

    /* RateBps is the affiliate's own direct commission rate, in basis points. */
    @SerializedName("rateBps")
    val rateBps: kotlin.Int? = null,

    /* ReferredCount is how many orgs this affiliate has referred. */
    @SerializedName("referredCount")
    val referredCount: kotlin.Int? = null,

    /* RequestedCode is the vanity code asked for at apply time — a request, not an allocation. Approval mints `code`, which may be a different slug if this one was already taken. */
    @SerializedName("requestedCode")
    val requestedCode: kotlin.String? = null,

    /* Status is \"applied\", \"approved\" or \"suspended\". Only an approved affiliate has a code that resolves for attribution and accrues commission; suspended keeps what it already earned but stops earning more. */
    @SerializedName("status")
    val status: kotlin.String? = null

) {


}

