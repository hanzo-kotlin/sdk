/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param branches Branches are the repo's branch names. Read live, so the detail view carries them and a list row does not.
 * @param cloneUrl CloneURL is the HTTPS smart-HTTP remote `git clone` takes.
 * @param createdAt CreatedAt is RFC 3339 UTC.
 * @param defaultBranch DefaultBranch is where HEAD points on a fresh repo (\"main\").
 * @param description Description is the caller-supplied blurb (max 4KiB).
 * @param head Head is the resolved HEAD commit, empty on an empty repo.
 * @param id ID is the repo's stable, prefixed identifier (\"repo_\" + 128 random bits).
 * @param name Name is the org-unique handle, and the last path segment of both URLs below.
 * @param org Org owns the repo — the gateway-minted X-Org-Id, and the isolation key.
 * @param project Project is the optional sub-scope the repo lives in; absent for the org's default scope.
 * @param `public` Public grants ANONYMOUS read (fetch) only; push and the whole control plane stay org-authed.
 * @param sizeBytes SizeBytes is the repo's measured on-disk size, re-measured on create, after each push, and after a gc. This is the number billing meters.
 * @param sshUrl SSHURL is the scp-style SSH remote (git@host:org/repo.git).
 * @param updatedAt UpdatedAt is RFC 3339 UTC, empty until the first write.
 */


data class RepoView (

    /* Branches are the repo's branch names. Read live, so the detail view carries them and a list row does not. */
    @SerializedName("branches")
    val branches: kotlin.collections.List<kotlin.String>? = null,

    /* CloneURL is the HTTPS smart-HTTP remote `git clone` takes. */
    @SerializedName("cloneUrl")
    val cloneUrl: kotlin.String? = null,

    /* CreatedAt is RFC 3339 UTC. */
    @SerializedName("createdAt")
    val createdAt: kotlin.String? = null,

    /* DefaultBranch is where HEAD points on a fresh repo (\"main\"). */
    @SerializedName("defaultBranch")
    val defaultBranch: kotlin.String? = null,

    /* Description is the caller-supplied blurb (max 4KiB). */
    @SerializedName("description")
    val description: kotlin.String? = null,

    /* Head is the resolved HEAD commit, empty on an empty repo. */
    @SerializedName("head")
    val head: kotlin.String? = null,

    /* ID is the repo's stable, prefixed identifier (\"repo_\" + 128 random bits). */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Name is the org-unique handle, and the last path segment of both URLs below. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Org owns the repo — the gateway-minted X-Org-Id, and the isolation key. */
    @SerializedName("org")
    val org: kotlin.String? = null,

    /* Project is the optional sub-scope the repo lives in; absent for the org's default scope. */
    @SerializedName("project")
    val project: kotlin.String? = null,

    /* Public grants ANONYMOUS read (fetch) only; push and the whole control plane stay org-authed. */
    @SerializedName("public")
    val `public`: kotlin.Boolean? = null,

    /* SizeBytes is the repo's measured on-disk size, re-measured on create, after each push, and after a gc. This is the number billing meters. */
    @SerializedName("sizeBytes")
    val sizeBytes: kotlin.Int? = null,

    /* SSHURL is the scp-style SSH remote (git@host:org/repo.git). */
    @SerializedName("sshUrl")
    val sshUrl: kotlin.String? = null,

    /* UpdatedAt is RFC 3339 UTC, empty until the first write. */
    @SerializedName("updatedAt")
    val updatedAt: kotlin.String? = null

) {


}

