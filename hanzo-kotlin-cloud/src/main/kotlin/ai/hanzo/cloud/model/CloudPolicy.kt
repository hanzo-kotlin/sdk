/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param cachePaths CachePaths overrides CacheTTLSec per path PREFIX (key \"/v1/models\" → seconds). The longest matching prefix wins.
 * @param cacheTtlSec CacheTTLSec is the org's default edge-cache TTL for its responses, in seconds; 0 means no caching. Unset inherits the platform default.
 * @param corsOrigins CORSOrigins is the PLATFORM-scope CORS allowlist EdgeCORS admits: an exact origin, a bare host, or a \"*.host\" wildcard. Writable only by a SuperAdmin — CORS is evaluated before identity, so it has no tenant to scope to.
 * @param methods Methods is the allowlist of HTTP methods the edge accepts for this org. Empty means all are accepted.
 * @param orgRpm OrgRPM is the org's OWN authenticated rate ceiling, requests per minute, as ScopeRateLimit enforces it. Unset inherits the platform default, then the static boot default.
 * @param perIpRpm PerIPRPM is the PLATFORM-scope pre-auth flood cap: requests EdgeRateLimit admits per WindowSec from one client IP. SuperAdmin-only, same reason.
 * @param updatedAt UpdatedAt is the unix second this policy row was last written. Server-stamped; a client-supplied value is ignored.
 * @param updatedBy UpdatedBy is the validated user id that wrote this policy row. Server-stamped; a client-supplied value is ignored.
 * @param windowSec WindowSec is the window PerIPRPM is counted over, in seconds. SuperAdmin-only.
 */


data class CloudPolicy (

    /* CachePaths overrides CacheTTLSec per path PREFIX (key \"/v1/models\" → seconds). The longest matching prefix wins. */
    @SerializedName("cache_paths")
    val cachePaths: kotlin.collections.Map<kotlin.String, kotlin.Int>? = null,

    /* CacheTTLSec is the org's default edge-cache TTL for its responses, in seconds; 0 means no caching. Unset inherits the platform default. */
    @SerializedName("cache_ttl_sec")
    val cacheTtlSec: kotlin.Int? = null,

    /* CORSOrigins is the PLATFORM-scope CORS allowlist EdgeCORS admits: an exact origin, a bare host, or a \"*.host\" wildcard. Writable only by a SuperAdmin — CORS is evaluated before identity, so it has no tenant to scope to. */
    @SerializedName("cors_origins")
    val corsOrigins: kotlin.collections.List<kotlin.String>? = null,

    /* Methods is the allowlist of HTTP methods the edge accepts for this org. Empty means all are accepted. */
    @SerializedName("methods")
    val methods: kotlin.collections.List<kotlin.String>? = null,

    /* OrgRPM is the org's OWN authenticated rate ceiling, requests per minute, as ScopeRateLimit enforces it. Unset inherits the platform default, then the static boot default. */
    @SerializedName("org_rpm")
    val orgRpm: kotlin.Int? = null,

    /* PerIPRPM is the PLATFORM-scope pre-auth flood cap: requests EdgeRateLimit admits per WindowSec from one client IP. SuperAdmin-only, same reason. */
    @SerializedName("per_ip_rpm")
    val perIpRpm: kotlin.Int? = null,

    /* UpdatedAt is the unix second this policy row was last written. Server-stamped; a client-supplied value is ignored. */
    @SerializedName("updated_at")
    val updatedAt: kotlin.Int? = null,

    /* UpdatedBy is the validated user id that wrote this policy row. Server-stamped; a client-supplied value is ignored. */
    @SerializedName("updated_by")
    val updatedBy: kotlin.String? = null,

    /* WindowSec is the window PerIPRPM is counted over, in seconds. SuperAdmin-only. */
    @SerializedName("window_sec")
    val windowSec: kotlin.Int? = null

) {


}

