/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param free Free reports a line that costs nothing because a coupon or a promotion made it so, rather than because its price is zero.
 * @param id ID is the line's identity — the variant id when the line is a variant, otherwise the product id. It is what a subsequent set call addresses.
 * @param kind Kind is \"variant\" when this line is a specific sellable variant and \"product\" when it is the product itself.
 * @param name Name is the item's display name, cached onto the line when it was added so a cart renders without a second read.
 * @param priceCents PriceCents is the unit price in whole cents, cached at the moment the line was added. The line's contribution to the cart is this times Quantity.
 * @param quantity Quantity is how many units of this item the cart holds.
 * @param sku SKU is the line's stock-keeping unit — the variant's when it has one, otherwise the product's. Empty when neither carries one.
 */


data class CartItem (

    /* Free reports a line that costs nothing because a coupon or a promotion made it so, rather than because its price is zero. */
    @SerializedName("free")
    val free: kotlin.Boolean? = null,

    /* ID is the line's identity — the variant id when the line is a variant, otherwise the product id. It is what a subsequent set call addresses. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Kind is \"variant\" when this line is a specific sellable variant and \"product\" when it is the product itself. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Name is the item's display name, cached onto the line when it was added so a cart renders without a second read. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* PriceCents is the unit price in whole cents, cached at the moment the line was added. The line's contribution to the cart is this times Quantity. */
    @SerializedName("priceCents")
    val priceCents: kotlin.Int? = null,

    /* Quantity is how many units of this item the cart holds. */
    @SerializedName("quantity")
    val quantity: kotlin.Int? = null,

    /* SKU is the line's stock-keeping unit — the variant's when it has one, otherwise the product's. Empty when neither carries one. */
    @SerializedName("sku")
    val sku: kotlin.String? = null

) {


}

