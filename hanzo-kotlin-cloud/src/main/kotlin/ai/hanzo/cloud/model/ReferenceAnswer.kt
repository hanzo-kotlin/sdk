/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param age Age is how old that is, as a duration.
 * @param asOf AsOf is when the oldest contributing publisher was current, RFC 3339.
 * @param from From is override or baseline — which plane answered.
 * @param hit Hit is whether the key is a member. It is meaningful ONLY when Refusal is empty: false with a refusal means the set could not be consulted, which is not the same as the key being clean.
 * @param key Key is the key as asked.
 * @param matched Matched is the member that covered the key, which for a domain or a network is the enclosing entry rather than the key itself.
 * @param refusal Refusal is why the set could not be consulted, when it could not: never loaded, held elsewhere, or a source we hold no licence for. Non-empty means Hit must not be read as an answer.
 * @param score Score is the published risk weight where the source expresses one.
 * @param set Set is the set consulted.
 * @param stale Stale is whether the set is past its freshness bound. A stale set still answers — yesterday's list beats none — and this is how a decision knows it leaned on one.
 * @param `value` Value is what the publisher says about the member — class, operator, scheme, region.
 * @param verdict Verdict is the tenant's own allow or deny, present only for an override. The baseline never carries one: it states facts and leaves the decision to the caller's policy.
 * @param version Version is the exact baseline version consulted, composed of each contributing publisher and its content digest. It is what makes a decision reproducible: an auditor takes this string and knows precisely what was consulted.
 */


data class ReferenceAnswer (

    /* Age is how old that is, as a duration. */
    @SerializedName("age")
    val age: kotlin.String? = null,

    /* AsOf is when the oldest contributing publisher was current, RFC 3339. */
    @SerializedName("asOf")
    val asOf: kotlin.String? = null,

    /* From is override or baseline — which plane answered. */
    @SerializedName("from")
    val from: kotlin.String? = null,

    /* Hit is whether the key is a member. It is meaningful ONLY when Refusal is empty: false with a refusal means the set could not be consulted, which is not the same as the key being clean. */
    @SerializedName("hit")
    val hit: kotlin.Boolean? = null,

    /* Key is the key as asked. */
    @SerializedName("key")
    val key: kotlin.String? = null,

    /* Matched is the member that covered the key, which for a domain or a network is the enclosing entry rather than the key itself. */
    @SerializedName("matched")
    val matched: kotlin.String? = null,

    /* Refusal is why the set could not be consulted, when it could not: never loaded, held elsewhere, or a source we hold no licence for. Non-empty means Hit must not be read as an answer. */
    @SerializedName("refusal")
    val refusal: kotlin.String? = null,

    /* Score is the published risk weight where the source expresses one. */
    @SerializedName("score")
    val score: java.math.BigDecimal? = null,

    /* Set is the set consulted. */
    @SerializedName("set")
    val set: kotlin.String? = null,

    /* Stale is whether the set is past its freshness bound. A stale set still answers — yesterday's list beats none — and this is how a decision knows it leaned on one. */
    @SerializedName("stale")
    val stale: kotlin.Boolean? = null,

    /* Value is what the publisher says about the member — class, operator, scheme, region. */
    @SerializedName("value")
    val `value`: kotlin.collections.Map<kotlin.String, kotlin.String>? = null,

    /* Verdict is the tenant's own allow or deny, present only for an override. The baseline never carries one: it states facts and leaves the decision to the caller's policy. */
    @SerializedName("verdict")
    val verdict: kotlin.String? = null,

    /* Version is the exact baseline version consulted, composed of each contributing publisher and its content digest. It is what makes a decision reproducible: an auditor takes this string and knows precisely what was consulted. */
    @SerializedName("version")
    val version: kotlin.String? = null

) {


}

