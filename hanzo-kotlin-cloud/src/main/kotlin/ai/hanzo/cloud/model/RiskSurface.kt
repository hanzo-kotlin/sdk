/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param folded Folded is how many buckets of the tenant's own feature surface were folded into the model when it became resident.
 * @param gap Gap says why the fold did not happen or did not complete, when that is the case. An empty surface and an unreachable warehouse are different facts and a model must not report them as the same one.
 * @param refused Refused is how many buckets of this organisation's own surface the fold could not fold, because a subject on them is longer than this plane's own field bound. It is history the model does not have, said out loud.
 * @param replayed Replayed is how many of this organisation's own recorded observations rebuilt its sliding aggregates when the model became resident. It is what says a rollout was a rebuild rather than a blindness: the aggregates are a projection of a durable record, so a restart costs a replay and not a control.
 * @param rolled Rolled is how many windows of this organisation's own source planes — product events, captured failures, metered inference — were rolled up into its feature surface before that fold. Zero with no gap means the surface was already current, which is a different fact from the rollup never running.
 * @param window Window is the lookback the fold covered.
 */


data class RiskSurface (

    /* Folded is how many buckets of the tenant's own feature surface were folded into the model when it became resident. */
    @SerializedName("folded")
    val folded: kotlin.Int? = null,

    /* Gap says why the fold did not happen or did not complete, when that is the case. An empty surface and an unreachable warehouse are different facts and a model must not report them as the same one. */
    @SerializedName("gap")
    val gap: kotlin.String? = null,

    /* Refused is how many buckets of this organisation's own surface the fold could not fold, because a subject on them is longer than this plane's own field bound. It is history the model does not have, said out loud. */
    @SerializedName("refused")
    val refused: kotlin.Int? = null,

    /* Replayed is how many of this organisation's own recorded observations rebuilt its sliding aggregates when the model became resident. It is what says a rollout was a rebuild rather than a blindness: the aggregates are a projection of a durable record, so a restart costs a replay and not a control. */
    @SerializedName("replayed")
    val replayed: kotlin.Int? = null,

    /* Rolled is how many windows of this organisation's own source planes — product events, captured failures, metered inference — were rolled up into its feature surface before that fold. Zero with no gap means the surface was already current, which is a different fact from the rollup never running. */
    @SerializedName("rolled")
    val rolled: kotlin.Int? = null,

    /* Window is the lookback the fold covered. */
    @SerializedName("window")
    val window: kotlin.String? = null

) {


}

