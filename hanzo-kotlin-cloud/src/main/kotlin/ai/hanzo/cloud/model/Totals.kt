/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accruedCents AccruedCents is lifetime commission accrued summed over those rows, in cents.
 * @param applied Applied is how many of those rows are still awaiting approval — no code, no accrual yet.
 * @param approved Approved is how many are approved: the only rows whose code resolves for attribution and whose balance can still grow.
 * @param paidCents PaidCents is lifetime commission already paid out summed over those rows, in cents.
 * @param pendingCents PendingCents is accrued minus paid summed over those rows, in cents — the outstanding liability across the page.
 * @param suspended Suspended is how many were suspended. What they already accrued stays accrued and stays payable.
 * @param total Total is how many affiliate rows this page covered, at every status. It is the page, not the table: a limit that truncates truncates this too.
 */


data class Totals (

    /* AccruedCents is lifetime commission accrued summed over those rows, in cents. */
    @SerializedName("accruedCents")
    val accruedCents: kotlin.Int? = null,

    /* Applied is how many of those rows are still awaiting approval — no code, no accrual yet. */
    @SerializedName("applied")
    val applied: kotlin.Int? = null,

    /* Approved is how many are approved: the only rows whose code resolves for attribution and whose balance can still grow. */
    @SerializedName("approved")
    val approved: kotlin.Int? = null,

    /* PaidCents is lifetime commission already paid out summed over those rows, in cents. */
    @SerializedName("paidCents")
    val paidCents: kotlin.Int? = null,

    /* PendingCents is accrued minus paid summed over those rows, in cents — the outstanding liability across the page. */
    @SerializedName("pendingCents")
    val pendingCents: kotlin.Int? = null,

    /* Suspended is how many were suspended. What they already accrued stays accrued and stays payable. */
    @SerializedName("suspended")
    val suspended: kotlin.Int? = null,

    /* Total is how many affiliate rows this page covered, at every status. It is the page, not the table: a limit that truncates truncates this too. */
    @SerializedName("total")
    val total: kotlin.Int? = null

) {


}

