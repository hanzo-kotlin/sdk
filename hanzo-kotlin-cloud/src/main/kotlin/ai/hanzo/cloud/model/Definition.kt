/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param name Name is the function's org-unique handle and the segment that addresses it, matching ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. The names that would shadow a collection route are reserved.
 * @param code Code is the source to run, capped so one function cannot amplify the store.
 * @param envNames EnvNames are the secret NAMES to mount. Values live in the secret store and are never carried here.
 * @param environment Environment is a second spelling of runtime, accepted so a console that says \"environment\" needs no translation.
 * @param handler Handler is the entry point within the code.
 * @param image Image names a prebuilt image to run instead of source.
 * @param memoryLimit MemoryLimit is the memory the function runs with, defaulting to 256Mi. It is also the multiplier on the GB-seconds compute charge.
 * @param namespace Namespace groups functions for display. It is cosmetic — the org is the isolation key — and is normalised to a DNS-safe label.
 * @param runtime Runtime is the language the code runs under: node, python or deno.
 * @param target Target is where the function runs: sandbox (the default) or fleet, the org's own GPU fleet. fleet supports runtime=python only.
 * @param timeoutSec TimeoutSec is the per-invocation deadline, defaulting to 30 and clamped at 900 — a larger value is capped rather than reset to the default.
 */


data class Definition (

    /* Name is the function's org-unique handle and the segment that addresses it, matching ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. The names that would shadow a collection route are reserved. */
    @SerializedName("name")
    val name: kotlin.String,

    /* Code is the source to run, capped so one function cannot amplify the store. */
    @SerializedName("code")
    val code: kotlin.String? = null,

    /* EnvNames are the secret NAMES to mount. Values live in the secret store and are never carried here. */
    @SerializedName("envNames")
    val envNames: kotlin.collections.List<kotlin.String>? = null,

    /* Environment is a second spelling of runtime, accepted so a console that says \"environment\" needs no translation. */
    @SerializedName("environment")
    val environment: kotlin.String? = null,

    /* Handler is the entry point within the code. */
    @SerializedName("handler")
    val handler: kotlin.String? = null,

    /* Image names a prebuilt image to run instead of source. */
    @SerializedName("image")
    val image: kotlin.String? = null,

    /* MemoryLimit is the memory the function runs with, defaulting to 256Mi. It is also the multiplier on the GB-seconds compute charge. */
    @SerializedName("memoryLimit")
    val memoryLimit: kotlin.String? = null,

    /* Namespace groups functions for display. It is cosmetic — the org is the isolation key — and is normalised to a DNS-safe label. */
    @SerializedName("namespace")
    val namespace: kotlin.String? = null,

    /* Runtime is the language the code runs under: node, python or deno. */
    @SerializedName("runtime")
    val runtime: kotlin.String? = null,

    /* Target is where the function runs: sandbox (the default) or fleet, the org's own GPU fleet. fleet supports runtime=python only. */
    @SerializedName("target")
    val target: kotlin.String? = null,

    /* TimeoutSec is the per-invocation deadline, defaulting to 30 and clamped at 900 — a larger value is capped rather than reset to the default. */
    @SerializedName("timeoutSec")
    val timeoutSec: kotlin.Int? = null

) {


}

