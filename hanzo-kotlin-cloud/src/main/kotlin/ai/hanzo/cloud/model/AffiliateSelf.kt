/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.LevelView
import ai.hanzo.cloud.model.Remittance

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accruedCents AccruedCents is lifetime commission accrued, in cents. It only grows — a payout is recorded against paidCents and never reduces this.
 * @param code Code is the minted referral code, the slug the ?aff link carries. Absent until staff approve; codes live in ONE global namespace across all affiliates.
 * @param defaultRateBps DefaultRateBps is the direct rate a new affiliate starts at, in basis points of margin (2000 = 20%). Answered ONLY to a caller that has not applied, as the quote beside `schedule`.
 * @param downlineTotal DownlineTotal counts every org in the caller's downline across the levels.
 * @param handle Handle is the opt-in public leaderboard name. Empty means opted out: the caller keeps its rank and still sees its own row, it is just not listed.
 * @param id ID is the affiliate's server-minted handle, \"aff_\"-prefixed. Absent until the org applies.
 * @param isAffiliate IsAffiliate says whether the caller org has an affiliate record. On false the answer carries the rate SCHEDULE and the default rate instead of a downline, so the console can show what the caller would earn.
 * @param levels Levels is the caller's downline per upline level, with the rate paid there.
 * @param link Link is the shareable ?aff URL built from the code. Empty until a code is minted, since there is nothing to share before approval.
 * @param marginBps MarginBps is the platform gross-margin fraction, in basis points, that every rate here is a rate OF. Read live per request, so it is the value in force now, not the one that applied to commission already accrued.
 * @param paidCents PaidCents is lifetime commission already paid out, in cents — credits grants and record-only cash disbursements alike.
 * @param payouts Payouts is the payout history, newest first, bounded to the last 100 rows.
 * @param pendingCents PendingCents is accrued minus paid, in cents — what the platform still owes and the ceiling on the next payout. Never negative.
 * @param rateBps RateBps is the caller's OWN direct (level 1) commission rate, in basis points of margin. Levels 2 and 3 are platform-wide and appear in `levels`.
 * @param schedule Schedule is the rate schedule quoted to a caller that has not applied.
 * @param status Status is \"applied\", \"approved\" or \"suspended\"; absent for a caller that never applied. Only \"approved\" mints links and accrues.
 */


data class AffiliateSelf (

    /* AccruedCents is lifetime commission accrued, in cents. It only grows — a payout is recorded against paidCents and never reduces this. */
    @SerializedName("accruedCents")
    val accruedCents: kotlin.Int? = null,

    /* Code is the minted referral code, the slug the ?aff link carries. Absent until staff approve; codes live in ONE global namespace across all affiliates. */
    @SerializedName("code")
    val code: kotlin.String? = null,

    /* DefaultRateBps is the direct rate a new affiliate starts at, in basis points of margin (2000 = 20%). Answered ONLY to a caller that has not applied, as the quote beside `schedule`. */
    @SerializedName("defaultRateBps")
    val defaultRateBps: kotlin.Int? = null,

    /* DownlineTotal counts every org in the caller's downline across the levels. */
    @SerializedName("downlineTotal")
    val downlineTotal: kotlin.Int? = null,

    /* Handle is the opt-in public leaderboard name. Empty means opted out: the caller keeps its rank and still sees its own row, it is just not listed. */
    @SerializedName("handle")
    val handle: kotlin.String? = null,

    /* ID is the affiliate's server-minted handle, \"aff_\"-prefixed. Absent until the org applies. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* IsAffiliate says whether the caller org has an affiliate record. On false the answer carries the rate SCHEDULE and the default rate instead of a downline, so the console can show what the caller would earn. */
    @SerializedName("isAffiliate")
    val isAffiliate: kotlin.Boolean? = null,

    /* Levels is the caller's downline per upline level, with the rate paid there. */
    @SerializedName("levels")
    val levels: kotlin.collections.List<LevelView>? = null,

    /* Link is the shareable ?aff URL built from the code. Empty until a code is minted, since there is nothing to share before approval. */
    @SerializedName("link")
    val link: kotlin.String? = null,

    /* MarginBps is the platform gross-margin fraction, in basis points, that every rate here is a rate OF. Read live per request, so it is the value in force now, not the one that applied to commission already accrued. */
    @SerializedName("marginBps")
    val marginBps: kotlin.Int? = null,

    /* PaidCents is lifetime commission already paid out, in cents — credits grants and record-only cash disbursements alike. */
    @SerializedName("paidCents")
    val paidCents: kotlin.Int? = null,

    /* Payouts is the payout history, newest first, bounded to the last 100 rows. */
    @SerializedName("payouts")
    val payouts: kotlin.collections.List<Remittance>? = null,

    /* PendingCents is accrued minus paid, in cents — what the platform still owes and the ceiling on the next payout. Never negative. */
    @SerializedName("pendingCents")
    val pendingCents: kotlin.Int? = null,

    /* RateBps is the caller's OWN direct (level 1) commission rate, in basis points of margin. Levels 2 and 3 are platform-wide and appear in `levels`. */
    @SerializedName("rateBps")
    val rateBps: kotlin.Int? = null,

    /* Schedule is the rate schedule quoted to a caller that has not applied. */
    @SerializedName("schedule")
    val schedule: kotlin.collections.List<LevelView>? = null,

    /* Status is \"applied\", \"approved\" or \"suspended\"; absent for a caller that never applied. Only \"approved\" mints links and accrues. */
    @SerializedName("status")
    val status: kotlin.String? = null

) {


}

