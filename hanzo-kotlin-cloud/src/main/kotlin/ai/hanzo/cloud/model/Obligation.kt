/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param amountCents AmountCents is what it costs each period.
 * @param asOf AsOf is when that amount was last checked against its source, RFC 3339 date.
 * @param code Code names the obligation so a caller can branch without reading prose.
 * @param every Every is how often it falls due — \"yearly\" for every obligation known here.
 * @param label Label is what the payer sees.
 * @param minimum Minimum marks a floor rather than a fixed price — a franchise tax that scales with shares or assets is quoted at its minimum, and an entity past the threshold owes more. Saying so is the difference between a quote and a number someone later disputes.
 * @param passThrough PassThrough marks money we collect and remit to the state rather than keep.
 * @param source Source names the authority that publishes a pass-through amount, so the figure can be checked without first working out who would know.
 * @param stale Stale reports that AsOf is older than the review window. It tells; it does not block.
 */


data class Obligation (

    /* AmountCents is what it costs each period. */
    @SerializedName("amountCents")
    val amountCents: kotlin.Int? = null,

    /* AsOf is when that amount was last checked against its source, RFC 3339 date. */
    @SerializedName("asOf")
    val asOf: kotlin.String? = null,

    /* Code names the obligation so a caller can branch without reading prose. */
    @SerializedName("code")
    val code: kotlin.String? = null,

    /* Every is how often it falls due — \"yearly\" for every obligation known here. */
    @SerializedName("every")
    val every: kotlin.String? = null,

    /* Label is what the payer sees. */
    @SerializedName("label")
    val label: kotlin.String? = null,

    /* Minimum marks a floor rather than a fixed price — a franchise tax that scales with shares or assets is quoted at its minimum, and an entity past the threshold owes more. Saying so is the difference between a quote and a number someone later disputes. */
    @SerializedName("minimum")
    val minimum: kotlin.Boolean? = null,

    /* PassThrough marks money we collect and remit to the state rather than keep. */
    @SerializedName("passThrough")
    val passThrough: kotlin.Boolean? = null,

    /* Source names the authority that publishes a pass-through amount, so the figure can be checked without first working out who would know. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Stale reports that AsOf is older than the review window. It tells; it does not block. */
    @SerializedName("stale")
    val stale: kotlin.Boolean? = null

) {


}

