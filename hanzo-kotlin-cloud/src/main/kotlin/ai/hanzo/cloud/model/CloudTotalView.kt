/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param confidence Confidence says how much the counters mean; a percentage-only meter leaves them at zero.
 * @param costCents CostCents is the row's cost in US cents. For an \"account\" row this is the PROVIDER's own charge, not a Hanzo one.
 * @param provider Provider is the upstream the usage was measured against.
 * @param requests Requests is how many requests the row covers.
 * @param scope Scope is whose row it is: \"user\" for the caller's own linked accounts, \"org\" for the whole tenant's Hanzo-routed usage.
 * @param source Source is where the row came from: \"account\" is the provider's own meter on the caller's linked account, \"hanzo\" is Hanzo-routed inference. The two are never summed.
 * @param tokens Tokens is the total tokens the row covers.
 * @param usedPct UsedPct is how much of a plan window the row consumed, 0–100. It is a share, never money.
 * @param window Window is the meter window class the row rolls up, when it has one.
 * @param windows Windows is how many window instances rolled up into the row.
 */


data class CloudTotalView (

    /* Confidence says how much the counters mean; a percentage-only meter leaves them at zero. */
    @SerializedName("confidence")
    val confidence: kotlin.String? = null,

    /* CostCents is the row's cost in US cents. For an \"account\" row this is the PROVIDER's own charge, not a Hanzo one. */
    @SerializedName("costCents")
    val costCents: kotlin.Int? = null,

    /* Provider is the upstream the usage was measured against. */
    @SerializedName("provider")
    val provider: kotlin.String? = null,

    /* Requests is how many requests the row covers. */
    @SerializedName("requests")
    val requests: kotlin.Int? = null,

    /* Scope is whose row it is: \"user\" for the caller's own linked accounts, \"org\" for the whole tenant's Hanzo-routed usage. */
    @SerializedName("scope")
    val scope: kotlin.String? = null,

    /* Source is where the row came from: \"account\" is the provider's own meter on the caller's linked account, \"hanzo\" is Hanzo-routed inference. The two are never summed. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Tokens is the total tokens the row covers. */
    @SerializedName("tokens")
    val tokens: kotlin.Int? = null,

    /* UsedPct is how much of a plan window the row consumed, 0–100. It is a share, never money. */
    @SerializedName("usedPct")
    val usedPct: java.math.BigDecimal? = null,

    /* Window is the meter window class the row rolls up, when it has one. */
    @SerializedName("window")
    val window: kotlin.String? = null,

    /* Windows is how many window instances rolled up into the row. */
    @SerializedName("windows")
    val windows: kotlin.Int? = null

) {


}

