/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param `open` Open is how many items are still work: everything whose status does not end it. It is the number a channel header shows.
 * @param room Room is the room these counts are for, echoed back as it was resolved.
 * @param status Status is the count per board column, carrying EVERY column this surface knows — an empty column reads 0 rather than being absent, so a caller can render the board without inventing the vocabulary. The keys are the same closed set every other operation here validates against.
 * @param total Total is every item bound to this room, settled ones included, so Total minus Open is what the room has finished.
 * @param updated Updated is when anything in this room's work last moved, in unix seconds. ABSENT when the room has no work at all: zero would read as the epoch, and a room nobody has filed anything in has no last activity rather than an infinitely old one. Total is 0 in exactly that case.
 */


data class RoomWork (

    /* Open is how many items are still work: everything whose status does not end it. It is the number a channel header shows. */
    @SerializedName("open")
    val `open`: kotlin.Int? = null,

    /* Room is the room these counts are for, echoed back as it was resolved. */
    @SerializedName("room")
    val room: kotlin.String? = null,

    /* Status is the count per board column, carrying EVERY column this surface knows — an empty column reads 0 rather than being absent, so a caller can render the board without inventing the vocabulary. The keys are the same closed set every other operation here validates against. */
    @SerializedName("status")
    val status: kotlin.collections.Map<kotlin.String, kotlin.Int>? = null,

    /* Total is every item bound to this room, settled ones included, so Total minus Open is what the room has finished. */
    @SerializedName("total")
    val total: kotlin.Int? = null,

    /* Updated is when anything in this room's work last moved, in unix seconds. ABSENT when the room has no work at all: zero would read as the epoch, and a room nobody has filed anything in has no last activity rather than an infinitely old one. Total is 0 in exactly that case. */
    @SerializedName("updated")
    val updated: kotlin.Int? = null

) {


}

