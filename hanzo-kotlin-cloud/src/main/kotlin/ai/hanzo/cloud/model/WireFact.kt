/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param at At is when the thing was so, RFC 3339, as the asserter gave it.
 * @param by By is the identity that filed it — `owner` or `owner/user` — stamped from the validated principal at the write, never from the body.
 * @param confidence Confidence in [0,1] as the asserter gave it; absent is 0. It breaks a tie between two assertions equally knowable and decides nothing else.
 * @param entity Entity is the thing described, in the organization's own namespace.
 * @param evidence Evidence points at the record the claim came from. Absent when the asserter gave none.
 * @param id ID is the assertion's content address, minted by the server from what was asserted. Two callers who assert the identical thing land on one ID and one row; changing any asserted field makes a different ID and a second row.
 * @param knowable Knowable is the first instant this plane could have answered with the assertion, RFC 3339: the later of Seen and the server's clock at the write. Derived and never supplied, which is what stops history filed today from being backdated into a past read.
 * @param names Names true means the assertion is an edge and Value is an entity. A walk reads only these.
 * @param relation Relation is what was asserted of it.
 * @param seen Seen is when the asserter says it became knowable, RFC 3339. Provenance only — Knowable is what an as-of read is bounded by.
 * @param source Source names who asserted, as the caller gave it. This plane ranks no source above another, so it never outweighs a later Knowable.
 * @param `value` Value is what the relation points at: another entity's key when Names is true, otherwise a scalar.
 */


data class WireFact (

    /* At is when the thing was so, RFC 3339, as the asserter gave it. */
    @SerializedName("at")
    val at: kotlin.String? = null,

    /* By is the identity that filed it — `owner` or `owner/user` — stamped from the validated principal at the write, never from the body. */
    @SerializedName("by")
    val by: kotlin.String? = null,

    /* Confidence in [0,1] as the asserter gave it; absent is 0. It breaks a tie between two assertions equally knowable and decides nothing else. */
    @SerializedName("confidence")
    val confidence: java.math.BigDecimal? = null,

    /* Entity is the thing described, in the organization's own namespace. */
    @SerializedName("entity")
    val entity: kotlin.String? = null,

    /* Evidence points at the record the claim came from. Absent when the asserter gave none. */
    @SerializedName("evidence")
    val evidence: kotlin.String? = null,

    /* ID is the assertion's content address, minted by the server from what was asserted. Two callers who assert the identical thing land on one ID and one row; changing any asserted field makes a different ID and a second row. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Knowable is the first instant this plane could have answered with the assertion, RFC 3339: the later of Seen and the server's clock at the write. Derived and never supplied, which is what stops history filed today from being backdated into a past read. */
    @SerializedName("knowable")
    val knowable: kotlin.String? = null,

    /* Names true means the assertion is an edge and Value is an entity. A walk reads only these. */
    @SerializedName("names")
    val names: kotlin.Boolean? = null,

    /* Relation is what was asserted of it. */
    @SerializedName("relation")
    val relation: kotlin.String? = null,

    /* Seen is when the asserter says it became knowable, RFC 3339. Provenance only — Knowable is what an as-of read is bounded by. */
    @SerializedName("seen")
    val seen: kotlin.String? = null,

    /* Source names who asserted, as the caller gave it. This plane ranks no source above another, so it never outweighs a later Knowable. */
    @SerializedName("source")
    val source: kotlin.String? = null,

    /* Value is what the relation points at: another entity's key when Names is true, otherwise a scalar. */
    @SerializedName("value")
    val `value`: kotlin.String? = null

) {


}

