/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.Arm

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param id ID is the experiment's slug, claimed once per project. It must match [A-Za-z0-9][A-Za-z0-9._-]{0,127}.
 * @param metricEvent MetricEvent is the event that counts as a conversion — the analysis numerator.
 * @param variants Arms are the arms, at least two. Weights that are all zero become an even split; otherwise they must sum to 100, and at most one arm may be flagged control.
 * @param exposureEvent ExposureEvent is the event that marks a subject as enrolled — the analysis denominator — defaulting to the SDK's $feature_flag_called marker.
 * @param flagKey FlagKey names the assignment flag this experiment writes, defaulting to exp_<id>. It must be a slug.
 * @param name Name is free text for a reader; the id is what addresses the experiment.
 * @param subjectKind SubjectKind is the unit assigned and measured: user (the default), org, session or audience.
 */


data class CreateBody (

    /* ID is the experiment's slug, claimed once per project. It must match [A-Za-z0-9][A-Za-z0-9._-]{0,127}. */
    @SerializedName("id")
    val id: kotlin.String,

    /* MetricEvent is the event that counts as a conversion — the analysis numerator. */
    @SerializedName("metricEvent")
    val metricEvent: kotlin.String,

    /* Arms are the arms, at least two. Weights that are all zero become an even split; otherwise they must sum to 100, and at most one arm may be flagged control. */
    @SerializedName("variants")
    val variants: kotlin.collections.List<Arm>,

    /* ExposureEvent is the event that marks a subject as enrolled — the analysis denominator — defaulting to the SDK's $feature_flag_called marker. */
    @SerializedName("exposureEvent")
    val exposureEvent: kotlin.String? = null,

    /* FlagKey names the assignment flag this experiment writes, defaulting to exp_<id>. It must be a slug. */
    @SerializedName("flagKey")
    val flagKey: kotlin.String? = null,

    /* Name is free text for a reader; the id is what addresses the experiment. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* SubjectKind is the unit assigned and measured: user (the default), org, session or audience. */
    @SerializedName("subjectKind")
    val subjectKind: kotlin.String? = null

) {


}

