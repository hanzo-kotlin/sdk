/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param character Character is a 0-based UTF-16 code-unit offset within Line, per the LSP specification — not a byte offset and not a rune index.
 * @param line Line is 0-based, per the LSP specification.
 * @param path Path is the repo-relative file, e.g. \"apps/lsp/lsp.go\".
 * @param relation Relation refines locate: definition, reference, type or implementation. Empty means definition. Every other op ignores it.
 * @param repo Repo is the repository NAME within the caller's own org, e.g. \"cloud\". Not a URL and not an owner/name pair: the owner is the validated principal's org, so this names a repository the caller already owns.
 * @param rev Rev is a branch, tag or commit sha. Empty means the default branch. It is resolved to a commit before anything else happens, so an answer is always about one immutable tree.
 */


data class Query (

    /* Character is a 0-based UTF-16 code-unit offset within Line, per the LSP specification — not a byte offset and not a rune index. */
    @SerializedName("character")
    val character: kotlin.Int? = null,

    /* Line is 0-based, per the LSP specification. */
    @SerializedName("line")
    val line: kotlin.Int? = null,

    /* Path is the repo-relative file, e.g. \"apps/lsp/lsp.go\". */
    @SerializedName("path")
    val path: kotlin.String? = null,

    /* Relation refines locate: definition, reference, type or implementation. Empty means definition. Every other op ignores it. */
    @SerializedName("relation")
    val relation: kotlin.String? = null,

    /* Repo is the repository NAME within the caller's own org, e.g. \"cloud\". Not a URL and not an owner/name pair: the owner is the validated principal's org, so this names a repository the caller already owns. */
    @SerializedName("repo")
    val repo: kotlin.String? = null,

    /* Rev is a branch, tag or commit sha. Empty means the default branch. It is resolved to a commit before anything else happens, so an answer is always about one immutable tree. */
    @SerializedName("rev")
    val rev: kotlin.String? = null

) {


}

