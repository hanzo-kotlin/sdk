/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param host Host is the exact hostname this route matches, lowercased with any trailing dot stripped. It is a GLOBALLY unique claim — one route across the whole edge may hold a host, so no tenant can hijack another's.
 * @param id ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars. A create that omits it gets a generated one.
 * @param middlewares Middlewares are the ids of the edge transforms to apply, in this order, before the request reaches the service. At most 16.
 * @param pathPrefix PathPrefix narrows the match to requests under this path; it must start with \"/\". Empty matches every path on the host.
 * @param priority Priority orders routes that share a host: higher wins, and equal priorities fall back to the longer PathPrefix.
 * @param service Service is the id of the backend pool this route dispatches to. A route naming a service that does not exist is skipped at compile, not served.
 * @param tls TLS asks the edge to terminate TLS for Host with an ACME-managed certificate.
 */


data class Route (

    /* Host is the exact hostname this route matches, lowercased with any trailing dot stripped. It is a GLOBALLY unique claim — one route across the whole edge may hold a host, so no tenant can hijack another's. */
    @SerializedName("host")
    val host: kotlin.String? = null,

    /* ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars. A create that omits it gets a generated one. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Middlewares are the ids of the edge transforms to apply, in this order, before the request reaches the service. At most 16. */
    @SerializedName("middlewares")
    val middlewares: kotlin.collections.List<kotlin.String>? = null,

    /* PathPrefix narrows the match to requests under this path; it must start with \"/\". Empty matches every path on the host. */
    @SerializedName("pathPrefix")
    val pathPrefix: kotlin.String? = null,

    /* Priority orders routes that share a host: higher wins, and equal priorities fall back to the longer PathPrefix. */
    @SerializedName("priority")
    val priority: kotlin.Int? = null,

    /* Service is the id of the backend pool this route dispatches to. A route naming a service that does not exist is skipped at compile, not served. */
    @SerializedName("service")
    val service: kotlin.String? = null,

    /* TLS asks the edge to terminate TLS for Host with an ACME-managed certificate. */
    @SerializedName("tls")
    val tls: kotlin.Boolean? = null

) {


}

