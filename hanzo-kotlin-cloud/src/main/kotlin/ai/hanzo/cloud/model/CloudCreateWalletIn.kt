/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accountId AccountID is the account this wallet belongs to. Required, and it must be an account of the caller's own org — an unknown one is a 404.
 * @param agent Agent optionally narrows the wallet to one agent within the org. It becomes a segment of the key ref, so it must be a url-safe segment with no slash.
 * @param chain Chain is the EVM chain this wallet is for, as \"eip155:<n>\" or a bare decimal chain id. Optional; a Safe defaults to the Hanzo L1 (36963).
 * @param custody Custody selects the signing backend: \"kms\" (in-process, always available), \"mpc\" or \"treasury\" (the deployed MPC ring), or \"safe\" (a Safe smart wallet owned by an MPC key). Empty uses the deployment's default. A backend that is not configured fails CLOSED with 503 rather than fabricating a signature.
 * @param name Name is the wallet's display label. Optional.
 * @param tier Tier is the MPC wallet tier: hot, warm, cold, gas, bridge, contract_admin, validator, quarantine or disaster_recovery. Empty defaults to hot.
 */


data class CloudCreateWalletIn (

    /* AccountID is the account this wallet belongs to. Required, and it must be an account of the caller's own org — an unknown one is a 404. */
    @SerializedName("accountId")
    val accountId: kotlin.String? = null,

    /* Agent optionally narrows the wallet to one agent within the org. It becomes a segment of the key ref, so it must be a url-safe segment with no slash. */
    @SerializedName("agent")
    val agent: kotlin.String? = null,

    /* Chain is the EVM chain this wallet is for, as \"eip155:<n>\" or a bare decimal chain id. Optional; a Safe defaults to the Hanzo L1 (36963). */
    @SerializedName("chain")
    val chain: kotlin.String? = null,

    /* Custody selects the signing backend: \"kms\" (in-process, always available), \"mpc\" or \"treasury\" (the deployed MPC ring), or \"safe\" (a Safe smart wallet owned by an MPC key). Empty uses the deployment's default. A backend that is not configured fails CLOSED with 503 rather than fabricating a signature. */
    @SerializedName("custody")
    val custody: kotlin.String? = null,

    /* Name is the wallet's display label. Optional. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Tier is the MPC wallet tier: hot, warm, cold, gas, bridge, contract_admin, validator, quarantine or disaster_recovery. Empty defaults to hot. */
    @SerializedName("tier")
    val tier: kotlin.String? = null

) {


}

