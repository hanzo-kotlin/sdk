/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param database Database is the logical database, collection, index or bucket this resource resolves to on its backend.
 * @param host Host is the address that actually routes to this resource — a dedicated instance's own in-cluster Service, or the public gateway for a shared one.
 * @param id ID is the resource's server-minted handle, \"rs_\"-prefixed.
 * @param kind Kind is the product: sql, vector, datastore, kv, search, s3 or docdb.
 * @param name Name is the org-unique slug the caller provisioned the resource under.
 * @param port Port is the port a client connects to on Host.
 * @param status Status is \"ready\", or \"provisioning\" while a dedicated instance is still being materialized. A dedicated resource's status is reconciled from the operator's live CR before this is answered, so it is never a stale ready.
 * @param username Username is the credential's user, for the kinds that mint one per resource. Absent for a kind whose backend authenticates with a shared, out-of-band key.
 */


data class ProvisionedResource (

    /* Database is the logical database, collection, index or bucket this resource resolves to on its backend. */
    @SerializedName("database")
    val database: kotlin.String? = null,

    /* Host is the address that actually routes to this resource — a dedicated instance's own in-cluster Service, or the public gateway for a shared one. */
    @SerializedName("host")
    val host: kotlin.String? = null,

    /* ID is the resource's server-minted handle, \"rs_\"-prefixed. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Kind is the product: sql, vector, datastore, kv, search, s3 or docdb. */
    @SerializedName("kind")
    val kind: kotlin.String? = null,

    /* Name is the org-unique slug the caller provisioned the resource under. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* Port is the port a client connects to on Host. */
    @SerializedName("port")
    val port: kotlin.Int? = null,

    /* Status is \"ready\", or \"provisioning\" while a dedicated instance is still being materialized. A dedicated resource's status is reconciled from the operator's live CR before this is answered, so it is never a stale ready. */
    @SerializedName("status")
    val status: kotlin.String? = null,

    /* Username is the credential's user, for the kinds that mint one per resource. Absent for a kind whose backend authenticates with a shared, out-of-band key. */
    @SerializedName("username")
    val username: kotlin.String? = null

) {


}

