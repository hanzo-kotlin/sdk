/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param at At is the unix second the genesis root was computed.
 * @param block Block is the L1 block the anchoring transaction landed in. Set only once the receipt has been read; absent otherwise.
 * @param chainId ChainID is the EVM chain the root is committed to — the Hanzo L1 by default.
 * @param note Note explains an unanchored genesis honestly — anchor wiring absent, or the submit error — rather than reporting a commit that did not happen.
 * @param root Root is the 0x-prefixed keccak256 root of the founding allocation. It is ALWAYS computed, whether or not the on-chain anchor is wired, because the root is the tamper-evident witness.
 * @param status Status is pending (root computed, not yet on-chain) or anchored (committed).
 * @param txHash TxHash is the L1 transaction hash of the anchoring commit. Empty until anchored.
 */


data class Genesis (

    /* At is the unix second the genesis root was computed. */
    @SerializedName("at")
    val at: kotlin.Int? = null,

    /* Block is the L1 block the anchoring transaction landed in. Set only once the receipt has been read; absent otherwise. */
    @SerializedName("block")
    val block: kotlin.Int? = null,

    /* ChainID is the EVM chain the root is committed to — the Hanzo L1 by default. */
    @SerializedName("chainId")
    val chainId: kotlin.Int? = null,

    /* Note explains an unanchored genesis honestly — anchor wiring absent, or the submit error — rather than reporting a commit that did not happen. */
    @SerializedName("note")
    val note: kotlin.String? = null,

    /* Root is the 0x-prefixed keccak256 root of the founding allocation. It is ALWAYS computed, whether or not the on-chain anchor is wired, because the root is the tamper-evident witness. */
    @SerializedName("root")
    val root: kotlin.String? = null,

    /* Status is pending (root computed, not yet on-chain) or anchored (committed). */
    @SerializedName("status")
    val status: kotlin.String? = null,

    /* TxHash is the L1 transaction hash of the anchoring commit. Empty until anchored. */
    @SerializedName("txHash")
    val txHash: kotlin.String? = null

) {


}

