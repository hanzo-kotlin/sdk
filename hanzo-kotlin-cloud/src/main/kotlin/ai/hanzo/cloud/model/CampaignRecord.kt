/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.ChannelSpec

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param audience Audience is an opaque reference to the segment this campaign targets. It is stored and echoed but not yet handed to the executors — a channel targets through the provider account it runs under — so it is documentation for now. Absent when never set.
 * @param budget Budget is the campaign's total budget in CENTS, handed to each executor as the budget for its channel. 0 means none was set.
 * @param channels Channels are the fan-out targets, at most one per kind and at most 12, each carrying its own post-launch state. Empty means nothing to launch, which is what makes a launch of this campaign a 400.
 * @param content Content is the ordered creative set, at most 32, empty entries dropped. Content[0] is the creative that runs; the rest are A/B variants a wired experiment can assign per launch.
 * @param createdAt CreatedAt is when the campaign was created, in unix seconds. Server-set.
 * @param id ID is the campaign's server-minted handle — \"cmp_\" and 128 random bits — and the id every other campaign call is addressed by. Never read off the wire: a create that sends one has it ignored.
 * @param name Name is the campaign's display name. Required on write, trimmed, and capped at 2048 characters.
 * @param scheduleAt ScheduleAt is when the campaign should run, in unix seconds. 0 (absent) means launch immediately. It is passed to each executor; nothing in this service wakes up to launch it for you.
 * @param status Status is the lifecycle state, server-owned and never accepted from a caller. Four values actually occur: draft (inert and fully mutable — nothing is sent and no budget is committed), live, paused and failed. After a fan-out live means AT LEAST ONE channel launched — read the channel rows for the rest — and failed means none did.
 * @param updatedAt UpdatedAt is the last write in unix seconds — an edit, a launch or a pause. Server-set on every save.
 */


data class CampaignRecord (

    /* Audience is an opaque reference to the segment this campaign targets. It is stored and echoed but not yet handed to the executors — a channel targets through the provider account it runs under — so it is documentation for now. Absent when never set. */
    @SerializedName("audience")
    val audience: kotlin.String? = null,

    /* Budget is the campaign's total budget in CENTS, handed to each executor as the budget for its channel. 0 means none was set. */
    @SerializedName("budget")
    val budget: kotlin.Int? = null,

    /* Channels are the fan-out targets, at most one per kind and at most 12, each carrying its own post-launch state. Empty means nothing to launch, which is what makes a launch of this campaign a 400. */
    @SerializedName("channels")
    val channels: kotlin.collections.List<ChannelSpec>? = null,

    /* Content is the ordered creative set, at most 32, empty entries dropped. Content[0] is the creative that runs; the rest are A/B variants a wired experiment can assign per launch. */
    @SerializedName("content")
    val content: kotlin.collections.List<kotlin.String>? = null,

    /* CreatedAt is when the campaign was created, in unix seconds. Server-set. */
    @SerializedName("createdAt")
    val createdAt: kotlin.Int? = null,

    /* ID is the campaign's server-minted handle — \"cmp_\" and 128 random bits — and the id every other campaign call is addressed by. Never read off the wire: a create that sends one has it ignored. */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Name is the campaign's display name. Required on write, trimmed, and capped at 2048 characters. */
    @SerializedName("name")
    val name: kotlin.String? = null,

    /* ScheduleAt is when the campaign should run, in unix seconds. 0 (absent) means launch immediately. It is passed to each executor; nothing in this service wakes up to launch it for you. */
    @SerializedName("scheduleAt")
    val scheduleAt: kotlin.Int? = null,

    /* Status is the lifecycle state, server-owned and never accepted from a caller. Four values actually occur: draft (inert and fully mutable — nothing is sent and no budget is committed), live, paused and failed. After a fan-out live means AT LEAST ONE channel launched — read the channel rows for the rest — and failed means none did. */
    @SerializedName("status")
    val status: kotlin.String? = null,

    /* UpdatedAt is the last write in unix seconds — an edit, a launch or a pause. Server-set on every save. */
    @SerializedName("updatedAt")
    val updatedAt: kotlin.Int? = null

) {


}

