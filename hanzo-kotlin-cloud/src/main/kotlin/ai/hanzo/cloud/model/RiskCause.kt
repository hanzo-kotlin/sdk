/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param baseline Baseline is the number it was measured against — always this organisation's own history, never a fixed limit and never another organisation's.
 * @param citation Citation is where those words come from, so the claim is checkable rather than asserted — which is what a chargeback network or a regulator asks for.
 * @param feature Feature is the dimension that contributed.
 * @param indicator Indicator is the supervisor's own words for the thing being looked for.
 * @param observed Observed is the raw number the coordinate was computed from.
 * @param severity Severity is how much weight this dimension carries.
 * @param share Share is this feature's part of the score, in [0,1]. Zero across every cause means no single feature accounts for the alert and the combination does; the causes are then ordered by how far each sits from unremarkable.
 * @param typology Typology is the laundering or abuse pattern this dimension detects.
 * @param unit Unit is how to read Observed, which is what turns a coordinate into a sentence.
 * @param without Without is the score the same event would have received with this coordinate at its neutral value — the counterfactual itself.
 */


data class RiskCause (

    /* Baseline is the number it was measured against — always this organisation's own history, never a fixed limit and never another organisation's. */
    @SerializedName("baseline")
    val baseline: java.math.BigDecimal? = null,

    /* Citation is where those words come from, so the claim is checkable rather than asserted — which is what a chargeback network or a regulator asks for. */
    @SerializedName("citation")
    val citation: kotlin.String? = null,

    /* Feature is the dimension that contributed. */
    @SerializedName("feature")
    val feature: kotlin.String? = null,

    /* Indicator is the supervisor's own words for the thing being looked for. */
    @SerializedName("indicator")
    val indicator: kotlin.String? = null,

    /* Observed is the raw number the coordinate was computed from. */
    @SerializedName("observed")
    val observed: java.math.BigDecimal? = null,

    /* Severity is how much weight this dimension carries. */
    @SerializedName("severity")
    val severity: kotlin.String? = null,

    /* Share is this feature's part of the score, in [0,1]. Zero across every cause means no single feature accounts for the alert and the combination does; the causes are then ordered by how far each sits from unremarkable. */
    @SerializedName("share")
    val share: java.math.BigDecimal? = null,

    /* Typology is the laundering or abuse pattern this dimension detects. */
    @SerializedName("typology")
    val typology: kotlin.String? = null,

    /* Unit is how to read Observed, which is what turns a coordinate into a sentence. */
    @SerializedName("unit")
    val unit: kotlin.String? = null,

    /* Without is the score the same event would have received with this coordinate at its neutral value — the counterfactual itself. */
    @SerializedName("without")
    val without: java.math.BigDecimal? = null

) {


}

