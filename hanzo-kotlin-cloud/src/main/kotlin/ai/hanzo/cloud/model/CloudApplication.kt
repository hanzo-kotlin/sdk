/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model

import ai.hanzo.cloud.model.CloudScreenResult
import ai.hanzo.cloud.model.CloudStageEvent

import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param company Company is the applicant's company name.
 * @param companyId CompanyID is the CRM Company minted for this lead at intake, so the startup also appears in the org's standard CRM tabs. Empty when that best-effort projection did not run.
 * @param contactId ContactID is the CRM Contact minted for this lead at intake. Empty when that best-effort projection did not run.
 * @param contactName ContactName is the person who applied.
 * @param createdAt CreatedAt is the unix second the application arrived. Server-owned.
 * @param email Email is the applicant's email — half of the (email, company) key a resubmission refreshes instead of duplicating.
 * @param events Events is the append-only stage-transition log, oldest first.
 * @param id ID is the server-minted application id (\"appl_\" + 128 random bits).
 * @param metadata Metadata is the FULL submitted form, every field, including the arrays the promoted columns above do not carry (tier1Investors, useCases) and the deterministic tier1Matched list.
 * @param reason Reason is why the application was rejected, required to reject. Empty otherwise.
 * @param role Role is the applicant's role at their company.
 * @param screen Screen is the AI screen. It runs after intake, so a freshly created application carries a \"pending\" screen.
 * @param stage Stage is the pipeline stage: applied, screened, qualified, credits-offered, onboarded or rejected. Server-owned — it starts at \"applied\" and moves only through the transition machine.
 * @param tier1 Tier1 is whether the applicant is tier-1 backed, derived deterministically at intake from the submitted fund list — independent of the AI screen.
 * @param updatedAt UpdatedAt is the unix second of the last write. Server-owned.
 * @param website Website is the applicant's website as submitted.
 */


data class CloudApplication (

    /* Company is the applicant's company name. */
    @SerializedName("company")
    val company: kotlin.String? = null,

    /* CompanyID is the CRM Company minted for this lead at intake, so the startup also appears in the org's standard CRM tabs. Empty when that best-effort projection did not run. */
    @SerializedName("companyId")
    val companyId: kotlin.String? = null,

    /* ContactID is the CRM Contact minted for this lead at intake. Empty when that best-effort projection did not run. */
    @SerializedName("contactId")
    val contactId: kotlin.String? = null,

    /* ContactName is the person who applied. */
    @SerializedName("contactName")
    val contactName: kotlin.String? = null,

    /* CreatedAt is the unix second the application arrived. Server-owned. */
    @SerializedName("createdAt")
    val createdAt: kotlin.Int? = null,

    /* Email is the applicant's email — half of the (email, company) key a resubmission refreshes instead of duplicating. */
    @SerializedName("email")
    val email: kotlin.String? = null,

    /* Events is the append-only stage-transition log, oldest first. */
    @SerializedName("events")
    val events: kotlin.collections.List<CloudStageEvent>? = null,

    /* ID is the server-minted application id (\"appl_\" + 128 random bits). */
    @SerializedName("id")
    val id: kotlin.String? = null,

    /* Metadata is the FULL submitted form, every field, including the arrays the promoted columns above do not carry (tier1Investors, useCases) and the deterministic tier1Matched list. */
    @SerializedName("metadata")
    val metadata: kotlin.collections.Map<kotlin.String, kotlin.Any>? = null,

    /* Reason is why the application was rejected, required to reject. Empty otherwise. */
    @SerializedName("reason")
    val reason: kotlin.String? = null,

    /* Role is the applicant's role at their company. */
    @SerializedName("role")
    val role: kotlin.String? = null,

    /* Screen is the AI screen. It runs after intake, so a freshly created application carries a \"pending\" screen. */
    @SerializedName("screen")
    val screen: CloudScreenResult? = null,

    /* Stage is the pipeline stage: applied, screened, qualified, credits-offered, onboarded or rejected. Server-owned — it starts at \"applied\" and moves only through the transition machine. */
    @SerializedName("stage")
    val stage: kotlin.String? = null,

    /* Tier1 is whether the applicant is tier-1 backed, derived deterministically at intake from the submitted fund list — independent of the AI screen. */
    @SerializedName("tier1")
    val tier1: kotlin.Boolean? = null,

    /* UpdatedAt is the unix second of the last write. Server-owned. */
    @SerializedName("updatedAt")
    val updatedAt: kotlin.Int? = null,

    /* Website is the applicant's website as submitted. */
    @SerializedName("website")
    val website: kotlin.String? = null

) {


}

