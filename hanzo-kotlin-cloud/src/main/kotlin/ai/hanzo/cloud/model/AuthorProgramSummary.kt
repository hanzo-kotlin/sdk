/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.hanzo.cloud.model


import com.google.gson.annotations.SerializedName

/**
 * 
 *
 * @param accruedCents AccruedCents is the page's lifetime royalty accrued, in integer USD cents.
 * @param approved Approved is how many are admitted and accruing.
 * @param connected Connected is how many of those are enrolled but not yet admitted to earning.
 * @param paidCents PaidCents is what has been RECORDED as paid across the page, in integer USD cents. Recorded, not settled: the money leaves in a human's hands.
 * @param pendingCents PendingCents is what the platform still owes across the page, in integer USD cents — the sum of each author's own accrued − paid, each floored at zero.
 * @param suspended Suspended is how many have been stopped from accruing further. An author holds exactly one status, so the three buckets never overlap and connected + approved + suspended = total.
 * @param total Total is how many author records this response actually carried. The roll-up is folded over the SAME page as authors — newest first, bounded by limit (default 500, ceiling 1000) — so on a program larger than the page it summarizes that page, not the fleet.
 */


data class AuthorProgramSummary (

    /* AccruedCents is the page's lifetime royalty accrued, in integer USD cents. */
    @SerializedName("accruedCents")
    val accruedCents: kotlin.Int? = null,

    /* Approved is how many are admitted and accruing. */
    @SerializedName("approved")
    val approved: kotlin.Int? = null,

    /* Connected is how many of those are enrolled but not yet admitted to earning. */
    @SerializedName("connected")
    val connected: kotlin.Int? = null,

    /* PaidCents is what has been RECORDED as paid across the page, in integer USD cents. Recorded, not settled: the money leaves in a human's hands. */
    @SerializedName("paidCents")
    val paidCents: kotlin.Int? = null,

    /* PendingCents is what the platform still owes across the page, in integer USD cents — the sum of each author's own accrued − paid, each floored at zero. */
    @SerializedName("pendingCents")
    val pendingCents: kotlin.Int? = null,

    /* Suspended is how many have been stopped from accruing further. An author holds exactly one status, so the three buckets never overlap and connected + approved + suspended = total. */
    @SerializedName("suspended")
    val suspended: kotlin.Int? = null,

    /* Total is how many author records this response actually carried. The roll-up is folded over the SAME page as authors — newest first, bounded by limit (default 500, ceiling 1000) — so on a program larger than the page it summarizes that page, not the fleet. */
    @SerializedName("total")
    val total: kotlin.Int? = null

) {


}

